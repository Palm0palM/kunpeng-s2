# C65 independent source review

Reviewed by `/root/c59_diag_review`, who did not author C65 source or AX checkers. Read actual candidate, patch, STATIC_REVIEW and root design obligations. Source111581B, SHA a855c14b81c00f3d398ac36c5ece6726e15235f18ebf3fe4746570da25a7f874. A separate lightweight text reversal removed conditional omp.h/target query and restored only the original seven-row dispatcher: full file reproduced C7 byte-for-byte. Existing computational helpers, other dispatch, official benchmark/runner/README unchanged. No operator compiled, imported or executed.

No source correctness blocker found. `partition_block` is outside the parallel region and explicitly shared. The single selected worker writes positive3*local_lanes; no nowait, so its implicit barrier publishes one common B before any division/partition. Each thread obtains actual team/tid inside that same region. Default private automatic locals are distinct per worker. Varying worker VL does not vary partition boundaries: unchanged helpers process each positive slice with their own VL and tail fallback; differing VL can affect performance, not ownership.

For N=groups*tiles_per_group, P=actual team, q=N/P,r=N%P, worker t starts t*q+min(t,r) and owns q+(t<r) tiles. Consecutive endpoints match; endpoints0/N and lengths nonnegative partition the domain exactly once, including P>N empty workers. In the loop, cursor<finish and first_tile<T imply both finish-cursor and T-first_tile positive; take is positive and cannot cross the group boundary. cursor advances exactly take. Thus each helper call is one nonempty group/worker intersection and preserves ownership.

CONVINT is actual signed int; dimensions are validated before subtraction. Positive oh/ow fit INT_MAX. On the asserted64-bit size_t path, N<=ceil(INT_MAX/7)*INT_MAX<2^63; worker*q and all cursor/end sums are<=N. group*7<oh. first_tile<T implies first_column<ow; last_tile<=T and final boundary chooses ow before multiplication, otherwise last_tile*B<ow. Therefore0<slice_width<=ow<=INT_MAX. Row offset and input/output strides remain original, and both base/dst receive identical column shift. Full/partial group branch decomposition is unchanged; no overlapping output float or new input address is introduced. Valid original array storage remains a caller requirement.

The target-SVE/noinline scalar-return query is called only inside the existing HWCAP_SVE and kh>=7/oh>=7 branch. Generic conv2d contains no direct SVE intrinsic. `_OPENMP` guards both omp.h and runtime symbols; without OpenMP, ignored pragmas execute one lexical block, the assignment still establishes B, and team/worker are1/0. There is no new reduction, partial sum, mutable global or per-output synchronization.

These are source/partition conclusions, not measured correctness, speed or compiler evidence. AX must independently verify actual1/4/38 teams, uniform observed VL for entry prediction, fewer tiles than threads, row/column tails, exact-once output writes and bitwise reference. Actual generated dispatcher/target query and strict flags remain to be reviewed after scheduled-node output. Source arithmetic remains C7 separate mul/add; no FMA was added.
