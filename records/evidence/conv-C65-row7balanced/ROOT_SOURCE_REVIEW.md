# Root C65 source review

Read complete candidate.patch and STATIC_REVIEW.md after preparation. Actual source implements the quotient/remainder and coalesced group intervals described in sep13ax-root-design-review.md. OpenMP single has braces and an implicit barrier; partition_block is outside parallel and explicitly shared. team/id come from the actual parallel team, with conditional omp.h and no-OpenMP1/0 constants. SVE lanes helper has scalar size_t return, noinline target arch=armv8-a+sve, and is called only inside the original HWCAP_SVE+kh>=7+oh>=7 branch. The metadata helper does not compute outputs.

The candidate asserts64-bit size_t/32-bit dimensions in the AArch64 SVE branch. It computes endcolumn as output_stride when lasttile reaches rounded tile count, before any rounded multiplication. firstcolumn<ow,0<slice_width<=ow,group start<oh. Original remaining-row decomposition is preserved with same stride/output_stride and adjusted firstcolumn. cursor advances a strictly positive take and stops exactly at worker finish. Original helpers and other dispatch bytes unchanged per source audit. No target numerical or performance claim yet. AX will exercise real parallel writes and actual VL/team geometry.
