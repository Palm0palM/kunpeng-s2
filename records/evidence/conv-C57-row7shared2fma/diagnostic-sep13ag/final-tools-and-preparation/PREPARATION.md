# AG numerical feasibility preparation

Draft remote wrapper only, not executed or transmitted. Source/checkpoint C57 is owned by Q and remains pending final identity. No submission manifest or job exists. Uses the original official benchmark, unchanged strict compiler flags and twelve original case calls. All predetermined numerical FAIL logs are retained; scheduler, process and numerical results remain separate. No artificial bitwise or FMA0 acceptance gate applies to this separate explicit-FMA experiment. Existing AE contract remains unchanged.

The allocation guard and tee/exit trap are copied from the reviewed AE wrapper. Production run.sh is retained as a source artifact, not altered. This diagnostic wrapper bypasses only its early termination after a FAIL so that all twelve results are collected. It does not change the benchmark or its timer. Candidate and reference assembly are retained for targeted evidence that explicit FMA occurred only where intended. Performance is not promoted from these diagnostic timings.

Transport, fixed source manifest and independent review remain pending. Root is the only future submitter; serialize after owned AF work and use existing strict SSH configuration. Main Codex usage must remain below40%; no reset card.
