Initial local registration was rejected because the parent had marked unmeasured records submitted, which is not an accepted pre-record status. No recorded_at, cases or evidence snapshot existed. Saved all prior administrative metadata, returned those unmeasured records to prepared and retained actual job IDs in cluster.json. No benchmark rerun, altered score, source or shared tool.
