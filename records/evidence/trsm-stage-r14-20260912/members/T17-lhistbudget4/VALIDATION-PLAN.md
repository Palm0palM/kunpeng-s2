# T17-lhistbudget4 待计算节点验证计划

当前仅准备，未编译或运行；不以静态审查替代实际正确性。所有题目检查与目标汇编生成仅在调度分配Linux aarch64、38CPU单NUMA、实际GCC12.3.1/KML25.1环境。

## 通用与预算门禁

新r14/preflight从r13 packed-aware四输入复制，原一般非dyadic RHS、1e-12精度、L/padding、no-op、1/4/38线程、无SVE、真实窄VL、全部分配失败、原/packed直接微核，以及原80组仅共享历史分配失败均保留。入口期望加入固定4MiB实际packed副本预算；不能把函数存在等同于每个小路径输入必用packed核。

两套直接微核仍各14例，运算顺序和直接签名不变。4095×9预算后packed期望0、原核510；4097×9仍大路径且panel/packed0。原<=257矩阵保持原预计入口。

新增正常边界m=1023/1024/1039/1040/1041，n=9，在1/4线程各运行五例；一个38线程进程执行1039×313和1040×313以覆盖门槛两侧及足够RHS面板。再用4线程分别运行上述五边界的仅worker X分配失败、no-SVE、实际窄VL；packed候选另执行五例仅共享分配失败。这些不是官方尺寸分派。

候选内posix_memalign在测试编译中被只读计数/受控失败包装，fixture分配不进入计数。正常预算内必须有且仅有一次主线程共享历史尝试且shape准确、分配成功；超预算必须观察共享历史尝试0，并验证worker X按预期调用成功。仅X失败时共享历史按预算正常、所有X失败且自然标量回退；仅共享失败时预算内失败一次后所有X成功，超预算无共享调用/无注入，X保持成功。必须保留实际新旧核入口和分配观测，不能只凭packed入口0宣称跳过分配。

同一新通用预检也用于T8(no-packed/no-budget)和T10(packed/unlimited)，便于同条件复核。固定预期：T8 14micro/433whole/52noop/sharedfail0；T10 28micro/518whole/64noop/sharedfail85；T17-lhistbudget4 28micro/518whole/64noop/sharedfail83（原80+新预算内3）。新增budget例数T8为27、packed成员为32。最终调用、完成标记及预算JSON字段以r14/preflight/README.md固定契约为准，root finish必须同时核对源特征、预算常量、实际分配及入口计数。

## 汇编与正式比较

保留原候选无插桩目标汇编；测试包装及-finstrument-functions只用于预检。原packed核、原panel核的历史FMA顺序及编译器约束不变，目标编译仍须确认。

正式四成员为T8/T10/T17/T18，均使用同一次分配的固定环境与原runner；每成员一套独立预热后保留三套完整官方用例，每套TEST_RUNS=3。T10对T17隔离预算，T17对T18隔离大宽核；晋级基线仍为实际current best，机理比较不单独授权晋级。全部分配、打包、同步和释放留在原计时内，不预判收益，不更改容差或排除慢样本。

T17大路径仍为T10/T8原实现，不需要wide32模块。

本轮只做本机轻量源码/文档编辑、直接字节与文本比较、整数推导及准备脚本的AST/bash-n；没有本机编译或执行题目、SSH、prepare/submit、登记/晋级、哈希计算或验证、比赛提交。源码尚未在目标节点编译或测试。所有实际检查必须在调度分配的计算节点运行，实际GCC12.3.1/KML25.1不等同指定KML25.2复验。
