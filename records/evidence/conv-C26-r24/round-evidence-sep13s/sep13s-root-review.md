# S root independent workflow review

Root read the complete S submit, record and compare tools and README. R definition reuse remains read-only on import. Fixed new order is C26-r24 / C51-r1 / C26-r25, three full suites each, 36 samples, unchanged R settings and own frozen Q diagnostic. Source parent and diagnostic source identity are explicit and distinct fields. The R48 initial comparison is rechecked against both C6 controls; C40 historical decisions remain unchanged.

S applies the original all-case spread and individual case regression rule to its own two fresh C6 controls only. In particular the first case is not omitted despite its approximately 0.9% initial regression. Failure is preserved without repeating the same confirmation. No samples are pooled with R. Successful S requires a separate original ZIP job before any root promotion.

The campaign is reserved under the workflow lock before source/control creation; an existing campaign or member prohibits resubmission. Serial gate checks known CONV jobs including any T reservation. Source, settings, scheduler, wrapper, expected cases and actual machine are checked before recording. Root authorizes one S --go invocation now; no T or U execution is authorized by this note. No operator compilation or tests have run locally.
