# C55 后续诊断边界建议（未执行）

本文件只提供待审矩阵，没有创建 harness/job 或固定 PASS 总数。必须关联 C55 自己的实际源，不能从 T/C52 或 AA/C54 借数值、汇编或 spill 结论。建议保留独立 scalar 逐位参考、输入/权重只读、分配两端 guard/canary、输出 poison，以及 VL字节16/32/64×线程1/4 的六配置。

|kw|三列 main 轮数|原 u1 余项数|重点|
|---:|---:|---:|---|
|1|0|1|main 跳过，单列余项|
|2|0|2|main 跳过，两次 u1|
|3|1|0|恰好一次三列 main，不能读取第4列|
|4|1|1|main 后一列余项|
|5|1|2|main 后两列余项|
|6|2|0|main 回边重复且无余项|
|7|2|1|多轮后单余列|
|8|2|2|多轮后最大余项|
|15|5|0|较长三列主循环|
|81|27|0|长循环与末列边界|

建议另加 kw79/80，分别覆盖长循环后1/2余列；最终矩阵须由根审查，以上尚无执行结果。三列展开的 kw3 阈值与父二列/四列版本不同，原有同名用例数量不能替代本版路径解释。

|组合|建议取值|覆盖目的|
|---|---|---|
|kh|6、7、8，另9/15/81|阈值前防御；一次/多次 shared 外层 t；较长 kernel及trailing转场|
|oh|1..15，21/22/28|7行阈值、余1..6、多组及四worker调度|
|ow|3L−1、3L、3L+1、6L−1、6L、6L+1|main跳过/恰好进入、横向tail、第二tile与reset；L=svcntw|
|新三列路径显式组合|kw1..8/15/81 与 kh>=7、oh>=7、ow>=3L|必须实际执行新三列 main 和余项，不能只有 fallback PASS|
|direct小kh防御|kh1..6、oh7/28、窄及足主块宽|确认未改防御；不是 shared3 覆盖|
|其它 fallback|沿用已有 invalid/non-SVE 独立项目并单列实际范围|未改文本不等于已执行该路径|

其中 h=oh+kh−1、w=ow+kw−1，保持真实 global stride；将最后 kernel coefficient 与最后 input load 放在分配边界前以检查 ik+2 和余项切换。入口计数和 worker mask 只能证明 helper/分派进入，需要尺寸及实际路径说明证明新 main 与余0/1/2均执行。若没有逐行 guard、sanitizer、direct ow1 或其它窄宽范围，应如实列为不足。

未来实际机器码审查必须覆盖所有 conv_sve_rowseven clone、13个语义阶段、三列 main、原 u1 remainder0..2、所有转场/reset/dispatch、ABI 与各种栈访问。编译器可能将余项降为直线或多个实际区间；应保存真实范围、按动态列工作量与路径解释，不能固定父 T 的14区间或照抄 AA 四列计数。源码 main 每轮63 mul/add 不证明实际指令数，更不证明零 spill 或提速。
