# AC 准备操作留痕

2026-09-13。本说明记录实际轻量准备操作类别和工具返回，不是执行AC诊断命令的日志。

1. 读取AGENTS、已读项目规则及AA成熟模板，查看共享工作区；确认AC路径原先不存在。root转达C55作者final/STOP后才复制。没有读取账号凭据。
2. 原生mkdir/cp创建AC目录、三工具和五文件；只对AC三工具及wrapper/env作身份与schema文本替换（外层exec chunk902593，exit0）。两个C checker完整复制，不修改。
3. apply_patch改driver固定AA/Y/X/T与明确AB reservation门禁；perl改accept/freezer的main3/maxrem2/path合同。shasum/wc及driver diff为纯文件检查；chunk1c32ff退出1由预期有差异的diff产生，不是工具或算子失败。
4. Python标准库仅对小文件read_bytes/hash/json/datetime/open('x')生成首次五文件SHA/bytes清单和初始prepared；没有import AC/AA工具或candidate，未编译/解析执行工具（chunke82d6a，exit0）。prepared中的时间是该文件操作实际UTC时间。
5. apply_patch写提交接口；shasum与三个cmp核对C55副本和两个AA checker，chunk52f10b退出0。此后已向root声明提交子集final/STOP，不再编辑其driver/source/首manifest/prepared/提交接口。
6. 读取accept/freezer完整diff和shared/schema核心；差异退出1是diff约定，未运行二者。只新增INTERFACE、STATIC_REVIEW、本说明及完整差异/文件身份清单，最后交root审。

没有SSH、调度预约、新job、编译、题目执行、基准、accept、freeze、包或晋级。没有AST/py_compile/导入/dry-run，也没有本机测试替代超算。后续示例入口只在SUBMISSION_INTERFACE供root审核，准备者不执行。当前主额度读12%，40%即停，不用重置卡。
