# C61 / AP resumed independent static review

Reviewer: `/root/c61_review_resume`, 2026-09-13. Scope: read-only source and diagnostic transport review; this file is the only edit. No operator compilation, execution, test, scheduler query, submission, fetch, acceptance, or performance measurement was performed. Local Python was used only for text parsing, byte comparisons, and static counts; neither driver nor acceptor was imported/executed.

Conclusion: **no static blocker to the root running AP's live serial gate and, if clear, submitting one own C61 diagnostic job.** This is not a correctness pass, speed result, or promotion approval. User has explicitly cancelled the 40% stopping limit; the old `<40` / `stop40` preparation notes in README/ROOT_REVIEW are historical and superseded. No reset cards and no local operator work remain binding.

## Candidate source

- Actual C58-r1 to C61 `conv2d.c` unified diff equals the saved `candidate.patch` exactly. Candidate SHA-256 is `40a3184f346983864593de73b3e3ade7973b1af9d2d273d566432e882a6904fa`; diagnostic source bytes equal production candidate bytes. The three companion files equal C58-r1.
- Only the shared seven-output, two-column body changes. For each column, three existing input vectors are loaded and each output row's one coefficient is used for its three accumulators before proceeding to another row. The same 21 independent accumulator chains receive column `ik` then `ik+1`, with separate multiplication and addition. No reduction reassociation or FMA introduction is present in source.
- Static pair counts: 42 `svmul_f32_x`, 42 `svadd_f32_x`, 6 `svld1`, 14 `svdup_n_f32`, and exactly two assignments for each of `a0` through `g2`. All 12 boundary unroll-2 hints remain. No tail predicate, address range, loop condition, dispatch, or remainder change.
- Existing `i + 3*lanes <= ow` and `column < kw` bounds cover the same full windows. Scope/lifetime reordering does not add any memory access. Actual GCC scheduling, stack/spills, and runtime correctness still require this candidate's own remote evidence.

## Driver and source wrapper

- Read both complete active Python files and the AN-to-AP driver/acceptor diffs; both Python files parse with `ast.parse`. Active files contain none of the accidental `SOURCE_NAOES`, `CAPCELLED`, `SVE_LAPES`, `OMP_DYNAOIC`, `AM_JOB_ID`, or `AN_SUMMARY` tokens. `SOURCE_NAMES`, cancellation statuses, `SVE_LANES`, and `OMP_DYNAMIC` are intact.
- The original serial predecessor is AO **1589611**, with order `C58-r4`, `C60-row7shared2ext`, `C58-r5`. Current campaign data is `complete`, expected 36 cases, and contains three versions each with four cases and three samples. Driver also queries the actual scheduler and requires terminal state plus integer job/system exits; campaign data alone cannot clear it.
- AP `job.json` did not exist at review. Exclusive reservation precedes upload and scheduler submission. An existing reservation or an ambiguous submission blocks resubmission; status/fetch reuse the recorded original ID. AP identity and actual job-ID association are separately required for acceptance. Do not invent or recycle the AO ID.
- Two checkers are byte-identical to exercised AN. Wrapper diff from AN consists only of candidate name and candidate source identity; `candidate.env` differs only in candidate name. Resource contract is 38 CPUs, 24576 MiB, one packed NUMA, 1800 seconds. Wrapper checks Linux AArch64 affinity count and one NUMA before building.
- GCC 10.3.1, generic CPU target, `-fno-fast-math`, `-ffp-contract=off`, OpenMP binding and dynamic-thread settings are unchanged. All compilation and checker runs occur inside the scheduled remote wrapper. Dispatch instrumentation is confined to its checker binary and is never timed.

## Returned evidence gate

- Six configurations are VL 16/32/64 bytes times threads 1/4. Each requires 5744 full, 1212 dispatch, and 432 direct cases, giving **44328** checks. Families, actual row-seven entry counts, worker masks, and actual VL/thread headers are checked. The unchanged guard uses bitwise reference comparison and protected input/output regions; this does not claim a sanitizer run.
- Acceptor requires exactly **19** ordered zero-exit stages, SUCCEEDED scheduler with both zero exits, wrapper zero exit, no summary issues or numerical FAIL/ERROR, exact three compiler argv and compiler version, requested resources, original/returned source association, and no fused instruction mnemonic.
- `AP_SUMMARY.json`, actual targeted assembly review, and root-returned review do not exist yet as completed evidence. Root must generate/read them from the new AP raw logs and actual assembly; file-presence checks alone are not a substitute for that review. The planned archive preserves source/raw/preparation and records performance as unmeasured.
- The acceptor's archive list predates this report. Root should retain this independent review with any later published/frozen audit evidence; omission would be a documentation gap, not a numerical test failure. I did not change the acceptor or any original preparation record.

Root next step: use the unchanged driver `gate`, resolve actual connection/scheduler status if needed, then a single `submit --go` only when clear. After successful completion, fetch the saved ID, review actual 44328/19-stage evidence and C61 assembly, and only then accept/freeze. A controlled performance comparison remains necessary before any best-version decision.

## AQ performance template follow-up

At root request, additionally read the complete `.runs/conv/sep13aq-performance.py`, its `.from-ao.patch`, and supporting group/wrapper setting flow. No script was imported or executed; Python AST parsing only passed. **No static blocker found for using AQ after AP is actually accepted.**

- Root submitted AP during this review. Its actual saved `job.json` now reports `job_id=1590209`, `submitted=true`, `submit_attempted=true`, and the correct C61 version. Active AQ now binds `AP_JOB_ID='1590209'`, matching that saved original ID. The preparation diff intentionally retains its earlier `None` and `Original bound AN` error-text snapshot; active AQ uses the actual ID and corrected `Original bound AP` message. This temporal difference is not an active token or identity bug.
- AQ order is uniquely `C58-r6`, `C61-row7shared2rowwise`, `C58-r7`. At follow-up review neither control nor its record existed and no AQ campaign existed. Both controls are copied from current confirmed C7. Original C61 creation identity is checked without assuming extended metadata equals original metadata; `pre-aq-experiment.json` preserves the current snapshot.
- Submission requires frozen AP status passed/complete, 44328 cases, actual original job association, same diagnosed C61 source, targeted and root assembly review files, and a fresh successful AP scheduler query with both zero exits. As with AP, root must actually read the review content before proceeding; file existence does not establish that by itself.
- C7's recorded `bench_repeats=3`, strict generic/38-thread environment, and scheduler settings must equal effective configuration before AQ reserves anything. `cluster_group.submit_group` preserves the supplied member order and runs all three sequentially within one allocation. Each member receives its own wrapper/settings, which loops three full suites. This yields four official cases times three suites times three members = 36 case samples.
- Result collection reuses the original one-group ID, requires SUCCEEDED plus job/system zero exits and wrapper zero, checks all standard checks/verified/repeats, same machine and settings across members, submitted source association, and exact raw-log parsed samples. Every member requires four cases with three samples and maximum error zero. Actual GCC 10.3.1, AArch64, generic CPU target, 38 threads, 38 allocated CPUs and NUMA identity are checked; both C7 control sources must equal the saved baseline.
- Both opening and closing `experiment.comparison` gates must be eligible before C61 is qualified for independent confirmation. No confirmation, promotion, or packaging occurs automatically. If either gate rejects C61, retain C7 and the full failed/slow experiment evidence.
- Campaign/member reservations precede submission and existing plan/member manifests block duplicate submission. An uncertain submission requires reconciliation of existing handles, never rerunning submit.
