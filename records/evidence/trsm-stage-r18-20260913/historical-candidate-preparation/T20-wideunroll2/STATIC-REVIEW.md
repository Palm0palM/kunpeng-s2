# T20 静态审查

root 对照当前基线逐字节核对：变化精确限定于update4x32_sve原k循环，函数前缀、最后16组C减sum存储和全部其它源码/四支持文件不变。新增循环有两组16显式FMA，单步尾有原16FMA；每个a_r_c仍先k再k+1，未拆分归约。两步第二组仅把四X行偏移和四L系数列偏移改为k+1。

count<=0时两个循环均不载入；count=1走原尾项；偶数只走双步，奇数最后走一个尾项。k在双步循环保持非负偶数，只有k+1<count时才加2；int count最大值时k+1与k+=2均不溢出，末尾递增最多到count。因此没有扩大有效载入范围，也没有对空或短count增加访问。函数签名、panel_stride/ldx、SVE/HWCAP/VL保护、KB256/CT64、分配/调用路径与小路径不变。

现有wide32直接检查确认为count={0,1,2,7,31,255,256}、padding={3,11}、packed/strided两布局，共28，覆盖本次双步与奇数尾；28整算子和56参数案例保留。没有执行本机预处理、编译、测试或哈希。静态审查通过，仍必须在超算通过这些检查及同分配三轮才能判断收益。
