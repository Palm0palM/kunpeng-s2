# T20-wideunroll2

父基线T19-control13已在作业1581760实测并晋级，实际实现为T19-panel8x16budget。只修改大路径update4x32_sve的k循环：显式两步展开，依次k和k+1，每个输出仍是同一条零起点FMA累加链，保留单步奇数尾项。没有双累加器归约、预取、浮点重新分组、块大小、指针接口、调用分派或小路径变更。

假设是减少每两个k步骤的循环控制和地址计算，给GCC12更多载入调度空间；收益未知，可能因代码大小或寄存器压力退化。当前目标汇编的原循环含16 FMA及重复地址计算，此次只有展开这一改动。此前T2-unroll使用旧NEON4×8/GCC10/OpenBLAS，大例快1.17%但未晋级；它不是本SVE4×32候选的性能证据。

源码已准备，尚未编译或运行。算子其他区域与当前已晋级T19逐字节一致；当前trsm与已发布ZIP保持原样。所有编译/直接核/整算子/官方性能与汇编检查仅在超算计算节点，不计算或验证哈希。
