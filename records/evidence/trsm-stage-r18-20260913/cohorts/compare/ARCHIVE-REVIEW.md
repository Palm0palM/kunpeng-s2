# r18 archival preparation review

Reviewed archive-r18.py against the executed r17 archiver. It now retains only the r18 comparison cohort, both separate repeat members and both full prior records. Before creating the destination it requires collected results, source/input snapshots, both wide preflight result sets, the comparison and the current records. Prior records must match the complete redacted r17 archived records from job 1581862; no timing samples are pooled. Historical T20 preparation is named explicitly. It excludes private connection/authentication and binary files, redacts personal/infrastructure identifiers, scans for actual private key/token material, and uses a fresh destination only. Archival creation does not validate measurements, claim remote source identity or promote anything. Parsed locally, not executed before result review.
