# T14：循环分配共享 L 打包任务

2026-09-12，r10 的 T8/T13 比较准备期间建立，尚未编译、测试、登记或提交作业。源码来自未晋级的 T10-lhistbarrier；当前晋级父基线仍为 T8-control12。

相对 T10 只改一处：solve_panel 的共享 packed_history 生产循环由 `#pragma omp for schedule(static)` 改为 `schedule(static,1)`。RHS 消费循环仍为 static，分配、布局、空 asm、算术、barrier、回退和大路径不变。五个源文件复制后仅有这一个 pragma 文本差异。

假设是减轻打包任务工作量不均：block b 复制 256*b 个 double，连续分配会把较大的后部块集中给少量线程。2432 行共有 151 个生产任务，38 线程下理论每线程 sum(b) 从 10..586 改为 228..376；512 行仅 31 个任务，分配关系不变。这是任务量推导，不是性能结果；共享打包的总复制量和分配开销仍完整保留。T10 原本已经并行生产，不能声称此次把串行打包改成并行。

应在下一轮同一调度分配下比较 T8、未改 T10 的同版本新 run 与 T14，先完成 packed-L、普通边界、线程与分配失败预检，再原官方 warm-up 及各三套完整用例。这样分别判断调度改动相对 T10 的作用，以及整体是否超过 T8。所有时间包含每次算子调用内部的打包与同步，不能复用跨调用缓存。若 r10 晋级改变当前基线，需先复核后续父版本和比较计划，不能直接登记本候选为新父版本的派生。

独立静态审查确认只改生产循环一处 pragma，各 block 写区间互不重叠，原隐式 barrier 保留。上述连续分配的精确范围按已经观察到的 GCC/libgomp 分块方式推导，并非所有 OpenMP 实现都保证相同线程编号分配。151 个任务的总 sum(b)=11476，循环分配不改变总工作量；31 个任务的总 sum(b)=496。

保留 KML25.1/GCC12 和指定 KML25.2 的区别；不在本机运行题目、不计算或验证哈希、不使用重置卡、不关机、不取消他人任务、不正式提交比赛。
