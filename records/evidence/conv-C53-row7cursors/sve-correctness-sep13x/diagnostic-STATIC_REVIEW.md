# X 准备静态审查

仅文件编辑、复制和文本比较；三个工具未导入或执行，没有编译/算子/SSH/job/accept/freeze。本文件是作者静态说明，等待root独立审查与唯一GO。

直接以已审Q driver/accept/freezer为模板，两个C检查器保留原字节及37128矩阵，不经T paired/odd结构。差异只有X/C53来源与固定SHA、固定W1582256/T1582134/S1582067和P/X预约的有界串行gate、13个u1算术工作量/游标实际解释、静态材料归档附件。wrapper只改候选名和固定C53源校验；浮点、编译、reference、分配与十九阶段不变。

固定W/T/S任何源文件缺失、未知/不同ID或实时非终态均阻止；P/X未知预约同样阻止。后续只有root显式submit --go可排他预约并提交一次；不明结果留存原件，status/fetch恢复同ID。无队友扫描、自动轮询、取消、性能提交或晋级。

C53作者与root的源审查确认游标公式：kernel+(6-r)*kw+(t-6)*kw+ik = kernel+(t-r)*kw+ik。每tile重置，kh7/8检查首次跨t，ow6L±1检查第二完整tile边界，kw1/2/3及较大奇偶保持u1。检查范围限制完整保留于INTERFACE；不把直接kh<7回退或套件入口累计当作shared动态覆盖证明。

接受仍须完整实际helper/clone、所有13阶段/转换/尾部/防御及dispatch；新增游标重置/跨t/one-past/标量地址栈解释。真实shared LSL/ADD计数从.s词法推导，七个kernel地址ADD的去留由人工对实际指令分类，不能从C源码推定。无预填机器范围、数量、ABI、spill或PASS。源相同的旧路径也需核对实际产物；Q/C51和C52/T结果不作为C53通过依据。

首次五文件source-hashes/source-manifest及prepared绑定最终C53源码。PREPARED_FILES仅清点本准备树，不含自身，不冒充目标机清单或运行结果。原文保存到Q-to-X.patch以便root审，未改变候选源码/record、W/T或提交包。
