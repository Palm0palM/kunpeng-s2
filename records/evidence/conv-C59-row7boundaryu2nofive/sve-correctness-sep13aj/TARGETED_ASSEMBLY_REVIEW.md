# AJ C59 目标汇编独立审查

审查对象为原作业 AJ1589289 返回的 `raw/conv2d-sve.s`，对照原 AH1583350 C58 汇编与 C59 源码。只做本地文本读取和计数，未运行编译、算子、测试、SSH、accept、freeze 或 record；未修改源文件或工具。

**结论：input_5 已回到单列循环，父版本该段每次成对迭代的 3 次 Z 栈读和 3 次 Z 栈写消失；共享两列主体与余一列仍无 Z spill。全函数仍存在 trailing 转场 spill，不能据此宣称性能提升。**

LOCAL_USER目标|AJ 实际范围LOCAL_USER静态指令数|FMUL/FADD|LD1W/LD1RW|Z 栈读/写LOCAL_USER
LOCAL_USER---LOCAL_USER---LOCAL_USER---:LOCAL_USER---:LOCAL_USER---:LOCAL_USER---:LOCAL_USER
|input_5 单列 `.L405`|6626–6681|55|18/18|3/6|0/0|
LOCAL_USER共享两列 `.L407`|6714–6837|123|42/42|6/14|0/0|
LOCAL_USER共享余一列 `.L409` 至回跳|6841–6905|63|21/21|3/7|0/0|
LOCAL_USER完整 `conv_sve_rowseven`|5705–8155|2270|492/492|138/164|6/12|

指令计数排除标签、汇编指示符和空行，包含 `ret`。根任务提供的 AJ 2269 数少计了无操作数的 `ret`（7879）；同口径父 AH 为 2448，所以完整 helper 减少 178 条。初次只读计数使用要求 mnemonic 后有空白的表达式，同样漏掉 ret；修正为接受行尾后复核，采用本表的 2270，不修改原始汇编。

源变化只有 input_5 `ik` 循环前的一条 `#pragma GCC unroll 2` 删除。该段源循环为 `ik=0; ik<kw; ++ik`，对六个输出各更新三个向量。实际 `.L510` 中 x1 初始化 0（6616）；`.L405` 用 x1 左移两位构造列字节偏移，三个 LD1W 与六个广播对应六输出×三向量；6659 的 x1 加 1 和 6680–6681 的 `cmp w19,w1; bgt .L405` 形成单列循环。三个新输出累计寄存器 z3/z12/z11 在入口置零，另外十五条累计链延续之前输出。这里确认目标语义形状及单列工作量，未对完整边界链和全部 dispatch 做新的穷尽证明。

共享主体的 x0 按 2 推进，x2 按 8 字节推进，生成两列共 42 次独立乘法及 42 次加法；余项 x1 按 1 推进，生成 21/21。两段均无 LDR/STR Z。数量与父 AH 的 6886–7009、7013–7077 一致，表示工作量与控制形状保持，不能解释成寄存器分配或机器码完全相同。扫描整个 AJ `.s`，浮点融合指令 FMLA/FMLS/FNMLA/FNMLS/FMADD/FMSUB/FNMADD/FNMSUB/FMAD/FMSB/FNMAD/FNMSB 均为 0。

帧为 **720 B + 2 VL**：5708 `addvl sp,sp,#-2` 和 5710 `sub sp,sp,#720`；返回与尾调用路径 7875/7877、8096/8098、8132/8134 配对恢复。父 AH 是 720 B + 3 VL。完整 helper 的 6 条 LDR Z 和 12 条 STR Z 全部使用由 `add x0,sp,720` 建立的基址，槽位为 `[x0]` 与 `[x0,#1,mul vl]`，所以确为栈槽而非输入输出内存访问。

AJ 栈读位于 7042/7043、7242/7244、7407/7408；栈写位于 6926/6928、7144/7145、7326/7327，以及互斥入口 7961/7962、7978/7979、7986/7988。它们仍保存或恢复 z9/z4，在 trailing 转场与奇偶入口出现。6/12 是静态出现数，不是一次执行的动态次数，也不能视为所有 spill 已消除；标量与地址栈访问仍非零。未见 Q 或 predicate 寄存器栈访问。

独立重数父 AH 完整 helper 确为 9 LDR Z / 15 STR Z；父 input_5 成对主体 6734–6851 为 117 条指令，6752/6764/6791 三次写，6778/6781/6792 三次读，基址由 6751/6755/6762 等 `sp+720` 建立，使用 0/1/2 VL 槽位。AJ 此输入阶段不再成对，且该段栈访问为零。因此可以确认这些临时乘积 spill 的消失，但不能把每个父成对迭代的 3/3 直接当作整个算子固定节省次数。

ABI 低 64 位 D8..D15 保持四对保存：5829、5834、5839、5844；恢复于 7843、7846、7849、7852。这与 Z spill 单独计数。父 AH 同为 D8..D15 四对。

本报告不含性能结论，不代替原数值检查、19 阶段退出和实际编译参数验收。是否提速须由同资源的独立性能轮次决定。
