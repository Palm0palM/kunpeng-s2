--- .runs/conv/sep13ah-checks/driver.py
+++ .runs/conv/sep13aj-checks/driver.py
@@ -1,4 +1,4 @@
-"""Prepared AH diagnostic transport for C58; explicit root --go only.
+"""Prepared AJ diagnostic transport for C59; explicit root --go only.
 
 No work occurs on import. status/fetch reuse a saved unique job; no local
 operator execution, automatic polling, acceptance, promotion or publication.
@@ -21,13 +21,15 @@
 sys.path.insert(0, str(ROOT / 'tools'))
 import cluster
 
-VERSIONS = ('C58-row7boundaryu2',)
-SOURCE_SHA256 = 'c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751'
+VERSIONS = ('C59-row7boundaryu2nofive',)
+SOURCE_SHA256 = '17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9'
 SOURCE_NAMES = {'conv2d.c', 'check_conv_guard.c', 'check_sve_dispatch.c', 'candidate.env', 'remote_job.sh'}
-SOURCE_PARENTS = {'C58-row7boundaryu2': 'C52-row7x3shared2'}
-PARENT_SHA256 = '8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7'
+SOURCE_PARENTS = {'C59-row7boundaryu2nofive': 'C58-row7boundaryu2'}
+# AK has priority. Bind its actual original ID only after root review; None blocks submit.
+AK_JOB_ID = None
+PARENT_SHA256 = 'c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751'
 PRODUCTION_NAMES = {'conv2d.c', 'README.md', 'bench_conv.c', 'run.sh'}
-COUNTS = {'C58-row7boundaryu2': dict(full_per_configuration=5744, dispatch_per_configuration=1212,
+COUNTS = {'C59-row7boundaryu2nofive': dict(full_per_configuration=5744, dispatch_per_configuration=1212,
     direct_per_configuration=432, configurations=6, total=44328)}
 RESOURCES = dict(cpus=38, memory_mb=24576, numa_count=1,
     numa_distribution='pack', walltime_seconds=1800)
@@ -37,7 +39,7 @@
     + [f'dispatch-vl{vl}-t{threads}' for vl in (16, 32, 64) for threads in (1, 4)]
     + ['build-assembly', 'complete'])
 TERMINAL = {'SUCCEEDED', 'FAILED', 'CANCELLED', 'CANCELED', 'TIMEOUT', 'TERMINATED'}
-# The frozen AH wrapper builds no retained production object for separate transfer.
+# The frozen AJ wrapper builds no retained production object for separate transfer.
 OBJECT_ARTIFACTS = {name: set() for name in VERSIONS}
 
 
@@ -80,16 +82,15 @@
 
 
 def serial_gate(cfg):
-    """Only original AG/AF plus present P/AH reservations; no peer scanning.
-
-    Actual terminal failures also clear the serial gate when both exit fields
-    are present. Root separately requires full original AG/AF result recording
-    before authorizing this one submission. Never cancel or adopt another ID.
+    """Only fixed AI/AH, future fixed AK and explicit P/AJ; no peer scanning.
+
+    Unknown reservations, missing AK binding, query failure or nonterminal
+    state block submit. Root additionally reviews complete results and quota.
     """
     identities, unresolved, ignored_prepared = {}, [], []
     for path, version in (
             (ROOT / '.runs/conv/sep13p-checks/C50-row4dupfencenomem/job.json', 'C50-row4dupfencenomem'),
-            (BASE / 'C58-row7boundaryu2/job.json', 'C58-row7boundaryu2')):
+            (BASE / 'C59-row7boundaryu2nofive/job.json', 'C59-row7boundaryu2nofive')):
         origin = str(path.relative_to(ROOT))
         if not path.exists():
             ignored_prepared.append(origin)
@@ -100,8 +101,13 @@
             unresolved.append(dict(path=origin, reason='Actual reservation has unknown identity or unreconciled job ID'))
             continue
         identities.setdefault(job_id, []).append(origin)
-    required = [('.runs/conv/sep13ag-fma/C57-row7shared2fma/job.json', 'job_id', '1583276', 'C57-row7shared2fma'),
-                ('.runs/conv/sep13af-campaign.json', 'performance_job', '1583230', None)]
+    required = [('.runs/conv/sep13ah-checks/C58-row7boundaryu2/job.json', 'job_id', '1583350', 'C58-row7boundaryu2'),
+                ('.runs/conv/sep13ai-campaign.json', 'performance_job', '1583408', None)]
+    if not isinstance(AK_JOB_ID, str) or not re.fullmatch(r'[0-9]+', AK_JOB_ID):
+        unresolved.append(dict(path='.runs/conv/sep13ak-campaign.json',
+            reason='AK priority: actual original job ID is not yet bound and reviewed'))
+    else:
+        required.append(('.runs/conv/sep13ak-campaign.json', 'performance_job', AK_JOB_ID, None))
     for origin, field, expected_id, version in required:
         path = ROOT / origin
         if not path.exists():
@@ -128,7 +134,7 @@
 def frozen_source(base, version):
     expected = read_json(base / 'source-hashes.json')
     require(set(expected) == SOURCE_NAMES, 'Unexpected frozen transport source set')
-    require(expected['conv2d.c'] == SOURCE_SHA256, 'AH requires its own root-reviewed C58 source')
+    require(expected['conv2d.c'] == SOURCE_SHA256, 'AJ requires its own root-reviewed C59 source')
     source = base / 'source'
     require(source.is_dir() and not source.is_symlink(), 'Source must be a real directory')
     actual = {}
@@ -141,7 +147,7 @@
     require(actual == expected, 'Prepared source changed; never silently refresh its frozen manifest')
     extra_manifest = base / 'source-manifest.json'
     require(extra_manifest.is_file() and not extra_manifest.is_symlink(),
-        'Frozen AH bytes/SHA source-manifest.json is required')
+        'Frozen AJ bytes/SHA source-manifest.json is required')
     require(read_json(extra_manifest) == {name: dict(bytes=sizes[name], sha256=actual[name]) for name in actual},
         'Supplementary bytes/SHA manifest mismatch')
     record = read_json(ROOT / 'records/experiments/conv' / (version + '.json'))
@@ -153,7 +159,7 @@
     require(record['source_hashes']['conv2d.c'] == expected['conv2d.c'], 'Candidate checkpoint/source mismatch')
     production = ROOT / '.runs/conv' / version / 'source'
     require(record.get('status') == 'prepared' and record.get('verified') is False,
-        'Original unmeasured prepared C58 record required')
+        'Original unmeasured prepared C59 record required')
     require(set(record['source_hashes']) == PRODUCTION_NAMES, 'Unexpected production source set')
     production_hashes = {name: hashlib.sha256((production / name).read_bytes()).hexdigest() for name in PRODUCTION_NAMES}
     require(production_hashes == record['source_hashes'] and production_hashes['conv2d.c'] == expected['conv2d.c'],
@@ -163,19 +169,19 @@
     parent_source = ROOT / '.runs/conv' / parent / 'source'
     parent_hashes = {name: hashlib.sha256((parent_source / name).read_bytes()).hexdigest() for name in PRODUCTION_NAMES}
     require(parent_hashes == parent_record['source_hashes'] and parent_hashes['conv2d.c'] == PARENT_SHA256,
-        'Original C52 source/record identity changed')
+        'Original C58 source/record identity changed')
     require(all(production_hashes[name] == parent_hashes[name] for name in PRODUCTION_NAMES if name != 'conv2d.c'),
-        'C58 must preserve original README/official benchmark/runner bytes')
-    parent_frozen = ROOT / '.runs/conv' / parent / 'sve-correctness-sep13t'
+        'C59 must preserve original README/official benchmark/runner bytes')
+    parent_frozen = ROOT / '.runs/conv' / parent / 'sve-correctness-sep13ah'
     parent_validation = read_json(parent_frozen / 'validation.json')
     parent_freeze = read_json(parent_frozen / 'freeze-source.json')
-    require(parent_validation.get('candidate') == parent and str(parent_validation.get('job_id')) == '1582134'
+    require(parent_validation.get('candidate') == parent and str(parent_validation.get('job_id')) == '1583350'
         and parent_validation.get('status') == 'passed' and parent_validation.get('complete') is True
-        and parent_validation.get('total_cases') == 37128, 'Original T parent diagnostic identity mismatch')
-    require(parent_freeze.get('candidate') == parent and str(parent_freeze.get('job_id')) == '1582134'
-        and parent_freeze.get('mode') == 'passed', 'Original T parent freeze missing')
+        and parent_validation.get('total_cases') == 44328, 'Original AH parent diagnostic identity mismatch')
+    require(parent_freeze.get('candidate') == parent and str(parent_freeze.get('job_id')) == '1583350'
+        and parent_freeze.get('mode') == 'passed', 'Original AH parent freeze missing')
     require(hashlib.sha256((parent_frozen / 'source/conv2d.c').read_bytes()).hexdigest()
-        == parent_validation['source_hashes']['conv2d.c'] == PARENT_SHA256, 'Original T frozen source identity mismatch')
+        == parent_validation['source_hashes']['conv2d.c'] == PARENT_SHA256, 'Original AH frozen source identity mismatch')
     prepared = read_json(base / 'prepared.json')
     counts = COUNTS[version]
     require(prepared.get('candidate') == prepared.get('source_experiment') == version
@@ -200,9 +206,9 @@
     require(prepared.get('resources_required') == {key: value for key, value in RESOURCES.items() if key != 'walltime_seconds'},
         'Prepared resource expectations mismatch')
     require(prepared.get('expected_acc') == 3 and prepared.get('expected_checks') == counts,
-        'AH matrix schema mismatch')
+        'AJ matrix schema mismatch')
     require(prepared.get('rows_per_group') == 7 and prepared.get('vectors_per_row') == 3
-        and prepared.get('accumulators') == 21, 'C58 seven-row/three-vector shape mismatch')
+        and prepared.get('accumulators') == 21, 'C59 seven-row/three-vector shape mismatch')
     require(prepared.get('expected_rowseven_entries') == dict(dispatch_per_configuration=1236, direct_per_configuration=1080),
         'Rowseven entry expectations mismatch')
     require(prepared.get('full_matrix_counts') == dict(core=3888, narrow=144, small=720, larger=32, quad_boundary=960)
@@ -210,7 +216,7 @@
         and prepared.get('expected_worker_masks') == {'1': 1, '4': 15}, 'Full-matrix/worker expectations mismatch')
     require(prepared.get('production_source_hashes') == production_hashes
         and prepared.get('source_parent_source_hashes') == parent_hashes
-        and prepared.get('source_parent_diagnostic_job_id') == '1582134'
+        and prepared.get('source_parent_diagnostic_job_id') == '1583350'
         and prepared.get('source_parent_frozen_directory') == str(parent_frozen.relative_to(ROOT)),
         'Prepared current/parent production identity mismatch')
     require(prepared.get('expected_semantic_stage_count') == 13
@@ -218,12 +224,39 @@
         and prepared.get('shared_remainder_source_columns_per_iteration') == 1
         and prepared.get('shared_remainder_max_columns') == 1
         and prepared.get('boundary_source_loop_count') == 12
-        and prepared.get('boundary_source_unroll_hint') == 2,
-        'C58 source-only boundary pragma/shared2 expectations mismatch')
-    # No C56 fence or per-binary-block acceptance schema belongs to AH transport.
+        and prepared.get('boundary_source_unroll_hint') == 2
+        and prepared.get('boundary_unroll_hinted_loop_count') == 11
+        and prepared.get('boundary_unroll_excluded_stages') == ['input_5'],
+        'C59 source-only boundary pragma/shared2 expectations mismatch')
+    # No C56 fence or per-binary-block acceptance schema belongs to AJ transport.
     require('column_fence_contract' not in prepared and 'assembly_review_schema' not in prepared,
-        'AH must not inherit the unrelated AE fence/binary-block schema')
+        'AJ must not inherit the unrelated AE fence/binary-block schema')
     return expected
+
+
+def completed_ai_evidence():
+    """Read only the original immediate AI group; never recompute selection or rewrite it."""
+    path = ROOT / '.runs/conv/sep13ai-campaign.json'
+    campaign = read_json(path)
+    order = ['C26-r36', 'C58-row7boundaryu2', 'C26-r37']
+    require(campaign.get('performance_job') == '1583408'
+        and campaign.get('status') == 'performance_complete'
+        and campaign.get('measurement_order') == order
+        and campaign.get('expected_benchmark_cases') == 36
+        and [row.get('version') for row in campaign.get('results', [])] == order,
+        'Original AI result must be completely recorded before AJ')
+    for version, row in zip(order, campaign['results']):
+        record = read_json(ROOT / 'records/experiments/conv' / (version + '.json'))
+        require(record.get('version') == version and record.get('job_id') == '1583408'
+            and record.get('status') == 'passed' and record.get('verified') is True
+            and record.get('repeats') == 3 and len(record.get('cases', [])) == 4
+            and record.get('checks') == dict(scheduler=True, fetched_log=True, benchmark=True,
+                wrapper=True, source_hashes=True, machine=True), 'Original AI member incomplete')
+        samples = [case['times_ms'] for case in record['cases']]
+        require(all(len(values) == 3 for values in samples) and row['all_samples_ms'] == samples,
+            'Preserve all36 original AI samples')
+    return dict(path=str(path.relative_to(ROOT)), sha256=hashlib.sha256(path.read_bytes()).hexdigest(),
+        job_id='1583408', expected_samples=36, read_only=True)
 
 
 def logged_remote(cfg, command, log_path, **kwargs):
@@ -241,7 +274,7 @@
     parser.add_argument('version', choices=VERSIONS)
     parser.add_argument('config', help='Verified existing config; read only, never included in source or archive')
     parser.add_argument('action', choices=('gate', 'submit', 'status', 'fetch'))
-    parser.add_argument('--go', action='store_true', help='Explicit one-shot root AH submission authorization')
+    parser.add_argument('--go', action='store_true', help='Explicit one-shot root AJ submission authorization')
     args = parser.parse_args()
     require(args.action == 'submit' or not args.go, '--go is valid only for submit')
     base = BASE / args.version
@@ -258,16 +291,19 @@
     if args.action == 'submit':
         require(args.go, 'Explicit root --go required; no automatic startup')
         require(not manifest.exists(), 'Refusing second submit; reconcile/reuse this package job.json')
+        require(isinstance(AK_JOB_ID, str) and re.fullmatch(r'[0-9]+', AK_JOB_ID),
+            'AK priority: bind/review its actual original job ID before any AJ submit query')
+        ai_evidence = completed_ai_evidence()
         gate = serial_gate(cfg)
         save(base / ('gate-' + stamp() + '.json'), gate)
         require(gate['clear'], 'Known CONV work active/unresolved or query incomplete; retain gate evidence and stop')
         hashes = frozen_source(base, args.version)
         created = stamp()
-        remote_dir = cfg['remote_root'] + '/diagnostics/conv-sep13ah-' + args.version + '-' + created
+        remote_dir = cfg['remote_root'] + '/diagnostics/conv-sep13aj-' + args.version + '-' + created
         data = dict(version=args.version, remote_dir=remote_dir, submitted=False,
             submit_attempted=False, created_at=created, resources=RESOURCES,
             expected_checks=COUNTS[args.version], expected_stages=STAGES,
-            source_hashes=hashes, known_job_gate=gate, explicit_go=True, no_automatic_continuation=True)
+            source_hashes=hashes, known_job_gate=gate, completed_ai_evidence=ai_evidence, explicit_go=True, no_automatic_continuation=True)
         # Per-package exclusive reservation precedes upload/submission. It is retained on any failure.
         with manifest.open('x') as handle:
             json.dump(data, handle, indent=2)
@@ -279,7 +315,7 @@
         command = 'umask 077; mkdir -p ' + shlex.quote(remote_dir) + ' && tar -xzf - -C ' + shlex.quote(remote_dir)
         result = logged_remote(cfg, command, base / 'upload.log', input=archive.getvalue(), timeout=cfg['transfer_timeout'])
         require(result.returncode == 0, 'Upload failed; keep reservation/log and do not resubmit')
-        command = cluster.submit_command(cfg, remote_dir, 'conv-sep13ah-' + args.version)
+        command = cluster.submit_command(cfg, remote_dir, 'conv-sep13aj-' + args.version)
         data.update(command=command, submit_attempted=True)
         save(manifest, data)
         result = logged_remote(cfg, command, base / 'submit.log')
@@ -343,7 +379,7 @@
             path = dest / name
             if not path.exists():
                 path.write_bytes(content)
-    print('Fetched immutable AH diagnostic text/assembly. Fetch is not acceptance or performance.')
+    print('Fetched immutable AJ diagnostic text/assembly. Fetch is not acceptance or performance.')
 
 
 if __name__ == '__main__':
--- .runs/conv/sep13ah-checks/accept_and_freeze.py
+++ .runs/conv/sep13aj-checks/accept_and_freeze.py
@@ -1,23 +1,30 @@
-"""AH returned-log acceptance/archive only; no operator or network execution."""
+"""AJ returned-log acceptance/archive only; no operator or network execution."""
 from pathlib import Path
-import json,re,shlex,shutil,datetime
+import argparse,hashlib,json,re,shlex,shutil,datetime
 BASE=Path(__file__).resolve().parent
 ROOT=BASE.parents[2]
-VERSION='C58-row7boundaryu2'
+VERSION='C59-row7boundaryu2nofive'
+SOURCE_SHA256='17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9'
 B=BASE/VERSION
 RAW=B/'raw'
-FROZEN=ROOT/'.runs/conv'/VERSION/'sve-correctness-sep13ah'
+FROZEN=ROOT/'.runs/conv'/VERSION/'sve-correctness-sep13aj'
 def read(p):return json.loads(p.read_text())
 def need(ok,why):
     if not ok:raise ValueError(why)
 def main():
+    parser=argparse.ArgumentParser(description=__doc__)
+    parser.add_argument('--job-id',required=True,help='Original AJ ID actually saved after root one-shot submission')
+    job_id=parser.parse_args().job_id
+    need(re.fullmatch(r'[0-9]+',job_id) and job_id not in {'1583350','1583408'},'Own original AJ ID required')
     need(not FROZEN.exists() and not (B/'validation.json').exists(),'Already accepted/frozen; do not overwrite')
-    s=read(B/'AH_SUMMARY.json');j=read(B/'job.json');prepared=read(B/'prepared.json')
-    need(s['candidate']==j['version']==VERSION and s['job_id']==j['job_id']=='1583350','Original job identity')
-    status=j['scheduler_status'];need(status['status']=='SUCCEEDED' and status['jobExitCode']==status['systemExitCode']==0,'Actual terminal exits')
+    s=read(B/'AJ_SUMMARY.json');j=read(B/'job.json');prepared=read(B/'prepared.json')
+    need(s['candidate']==j['version']==VERSION and s['job_id']==j['job_id']==job_id,'Original job identity')
+    status=j['scheduler_status'];need(str(status.get('jobId'))==job_id,'Scheduler job association')
+    need(status['status']=='SUCCEEDED' and status['jobExitCode']==status['systemExitCode']==0,'Actual terminal exits')
     need(int((RAW/'exit-code.txt').read_text())==0 and s['wrapper_exit']==0 and s['issues']==[],'Wrapper/summary issues')
     stages=['allocation','compiler','manifest','build-guard']+[f'guard-vl{v}-t{t}' for v in (16,32,64) for t in (1,4)]+['build-dispatch']+[f'dispatch-vl{v}-t{t}' for v in (16,32,64) for t in (1,4)]+['build-assembly','complete']
     need(re.findall(r'^STAGE=(\S+) EXIT=(-?\d+)$',(RAW/'stage-exits.txt').read_text(),re.M)==[(x,'0') for x in stages],'All19 actual ordered stages')
+    need(s['stages']==[dict(stage=x,exit_code=0) for x in stages],'Summary stage association')
     configs=[]
     for v in (16,32,64):
         for t in (1,4):
@@ -44,13 +51,16 @@
     need(len(s['allowed_cpus'])==38 and s['requested_resources']==j['resources']==dict(cpus=38,memory_mb=24576,numa_count=1,numa_distribution='pack',walltime_seconds=1800),'Allocation')
     hashes=read(B/'source-hashes.json');need(hashes==prepared['source_hashes']==j['source_hashes'],'Source association')
     need({row['name']:row['sha256'] for row in s['source_files']}==hashes and all(row['all_manifests_and_transport_bytes_equal'] for row in s['source_files']),'Completed lifecycle source verification')
+    need(set(hashes)=={'conv2d.c','check_conv_guard.c','check_sve_dispatch.c','candidate.env','remote_job.sh'},'Exact five-file transport set')
+    need(hashes.get('conv2d.c')==SOURCE_SHA256,'Own C59 reviewed source identity')
+    need({name:hashlib.sha256((B/'source'/name).read_bytes()).hexdigest() for name in hashes}==hashes,'Actual original source hashes')
     # Lifecycle already verified remote manifest; require the exact returned bytes still match originals.
     for name in hashes:need((RAW/name).read_bytes()==(B/'source'/name).read_bytes(),'Returned source changed')
     assembly=(RAW/'conv2d-sve.s').read_text()
     need(not re.search(r'^\s*(?:fmla|fmls|fmad|fmsb|fnmla|fnmls|fnmad|fnmsb|fmadd|fmsub|fnmadd|fnmsub|fmlal2?|fmlsl2?|fmmla)\b',assembly,re.M),'Unexpected fused arithmetic')
     review=B/'TARGETED_ASSEMBLY_REVIEW.md'
     need(review.is_file() and (B/'ROOT_RETURNED_REVIEW.md').is_file(),'Root must first read targeted actual review')
-    result=dict(status='passed',complete=True,candidate=VERSION,job_id='1583350',scheduler=status,exit_code=0,total_cases=44328,full_cases=34464,dispatch_cases=7272,direct_cases=2592,runner_cases=0,configurations=configs,source_hashes=hashes,source_hashes_verified=True,compiler_version='10.3.1',build_commands=expected,stage_exits=s['stages'],allocation=s['scheduler_resources'],assembly=dict(fused_instructions=0,review='TARGETED_ASSEMBLY_REVIEW.md'),performance_measured=False,executed_locally=False,executed_remotely=True,sanitizer='NOT_RUN',issues=[],note='Original AH own numerical evidence; root-read targeted pragma review. No speed claim or automatic promotion.')
+    result=dict(status='passed',complete=True,candidate=VERSION,job_id=job_id,scheduler=status,exit_code=0,total_cases=44328,full_cases=34464,dispatch_cases=7272,direct_cases=2592,runner_cases=0,configurations=configs,source_hashes=hashes,source_hashes_verified=True,compiler_version='10.3.1',build_commands=expected,stage_exits=s['stages'],allocation=s['scheduler_resources'],assembly=dict(fused_instructions=0,review='TARGETED_ASSEMBLY_REVIEW.md'),performance_measured=False,executed_locally=False,executed_remotely=True,sanitizer='NOT_RUN',issues=[],note='Original AJ own numerical evidence; root-read actual input_5/shared2 spill and frame review. No speed claim or automatic promotion.')
     # Archive existing evidence without altering raw/prepared/creation metadata.
     temp=FROZEN.with_name(FROZEN.name+'.tmp');need(not temp.exists(),'Pending archive exists')
     shutil.copytree(B,temp)
@@ -58,9 +68,9 @@
     for name in ['driver.py','accept_and_freeze.py','README.md','ROOT_REVIEW.md','INDEPENDENT_TOOLS_REVIEW.md']:
         shutil.copy2(BASE/name,temp/('diagnostic-'+name))
     (temp/'raw/conv2d-sve.assembly.txt').write_bytes((RAW/'conv2d-sve.s').read_bytes())
-    freeze=dict(candidate=VERSION,job_id='1583350',mode='passed',frozen_at=datetime.datetime.now(datetime.timezone.utc).isoformat(),source_directory=str(B.relative_to(ROOT)),original_metadata_preserved=True,original_source_and_raw_preserved=True,operator_executed=False,assembly_available=True)
+    freeze=dict(candidate=VERSION,job_id=job_id,mode='passed',frozen_at=datetime.datetime.now(datetime.timezone.utc).isoformat(),source_directory=str(B.relative_to(ROOT)),original_metadata_preserved=True,original_source_and_raw_preserved=True,operator_executed=False,assembly_available=True)
     (temp/'freeze-source.json').write_text(json.dumps(freeze,indent=2)+'\n')
     temp.rename(FROZEN)
     (B/'validation.json').write_text(json.dumps(result,indent=2)+'\n')
-    print('AH1583350 accepted/frozen44328; performance unmeasured.')
+    print('AJ'+job_id+' accepted/frozen44328; performance unmeasured.')
 if __name__=='__main__':main()
--- .runs/conv/sep13ah-checks/README.md
+++ .runs/conv/sep13aj-checks/README.md
@@ -1,29 +1,28 @@
-# AH：C58 边界 unroll2 自身数值诊断（仅准备）
+# AJ：C59 自身诊断模板，prepared only
 
-唯一候选 C58-row7boundaryu2，source parent 为原 C52-row7x3shared2/T1582134；生产 conv2d.c 为108874字节，SHA `c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751`。root 已独立读12个 pragma hunks，并删除12行完整还原父字节、核对其他三文件。收到该审结论后才首次复制五文件及生成清单。本轮仅准备运输 driver、源码副本/清单/prepared、本说明和完整相对 AE 差异；没有导入或运行 driver、SSH、new/checkpoint、预约、数值或汇编执行。
+唯一候选 `C59-row7boundaryu2nofive`，source parent 为 `C58-row7boundaryu2` / 原 AH1583350。C59 只删除父源 input_5 前一行 `#pragma GCC unroll 2`，其余11条提示、shared2/u1、算术、dispatch 与三份生产配套文件原字节保留。最终 conv2d.c 为108841字节，SHA `17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9`。O 已 FINAL/STOP，root 独立源审通过后才首次复制本目录五文件和创建清单；不修改候选源码或记录。
 
-原 AE 两个 checker 完全不改：check_conv_guard.c SHA `ccbba2637255b5d2733dfbfdddd88afd0d09815d5759ca86c9e8c498475990d5`，check_sve_dispatch.c SHA `fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d`，另与 AE 冻结 source/raw 逐字核对。remote_job.sh 只把 C56 身份改 C58、源 SHA 改本版；candidate.env 只改身份。原19阶段、编译命令、FMA0路线、guard位比较、容差和计数不变；不把额外位比较/FMA0解释为官方题目的全面禁令。
+两份 checker 与 AH/原 AE 完全相同；remote_job.sh 仅替换候选身份与源 SHA，candidate.env 仅替换身份。原19阶段、GCC10.3.1/generic、38CPU/24576MiB/单 packed NUMA/1800秒、`-fno-fast-math -ffp-contract=off`、OpenMP绑定保持。完整44328矩阵为 VL16/32/64字节 × 线程1/4，各 full5744/dispatch1212/direct432，总34464/7272/2592，runner0；entries1236/1080、worker masks1/15。checker 的 `quad_boundary` 是既有网格名，不是候选展开因子。
 
-完整44328矩阵不变：VL16/32/64字节×线程1/4共六配置，各 full5744/dispatch1212/direct432，总34464/7272/2592，runner0；rowseven entries1236/1080，worker masks1/15。full家族 core3888/narrow144/small720/larger32/quad_boundary960，dispatch core972/quad_boundary240。quad_boundary 是原 checker 网格名，不能解释为本候选四列展开。原网格包含 kw1/2/3 与 kw4/5/6/7/8边界及长宽15/81；十二条 pragma 的真实偶数展开/奇数余项和寄存器影响仍未知。
+`prepared.json` 的 compiled/executed/verified/complete 为 false，actual_job_id 为 null；source-hashes/source-manifest 为首次五文件身份。driver 只读核对当前生产四文件、标准 `source_parent` 优先/`parent` 后备、原 C58/AH 的真实冻结源与本轮清单，绝不刷新 creation 元数据。AH 的 PASS 不等于 C59 的 PASS。
 
-资源保持38CPU、24576MiB、单 packed NUMA、1800秒、GCC10.3.1/generic，原 `-fno-fast-math -ffp-contract=off` 与 OpenMP绑定不变。wrapper先核对Linux/aarch64、实际affinity38和同一NUMA，再运行原stage序列；算子只在调度分配节点执行。生产 README/bench_conv/run.sh 与父四源/原record一致，driver另外核对新record的当前四文件；标准parent缺省兼容不改record或creation。
+提交串行范围只有原 AI1583408、AH1583350、明确 P/AJ job.json，以及优先的 AK。当前 `AK_JOB_ID=None` 是有意的不可提交状态；待 AK 实际原 ID 产生后，由 root 固定该值并审查这一身份差异，不能猜 ID。提交前检查原 AI campaign 已 complete、三个成员完整36样本和已记录退出校验；不重算或改其选优结论。AK 必须真实终态且有整数 job/system 退出，root另行确认其原结果完整。未知预约、身份变化、查询失败或非终态均阻止，不扫描队友历史。root 在 GO 前、完成后读取真实主额度；达到40%立即停止、不用 reset。脚本不自行访问账户额度，`--go` 不能替代该检查。
 
-source-hashes.json、source-manifest.json 是首次五文件SHA/字节清单，prepared.json 关联本版源与原父T，compiled/executed/verified/complete仍false、actual_job_id=null。driver核对原五文件、当前四源、父C52/T身份、六配置/44328/19stage/资源预期；任何差异不能静默刷新清单。父T37128仅证明父字节，不提供本版PASS。
+driver 仅显式 root `submit --go` 预约一次，先排他写 job.json，再上传和提交。任何失败/失联保留原预约及输出，恢复原 ID，绝不重复提交。status/fetch 复用该 ID；fetch 只在真实终态且 job/system 退出齐全时取回原件，包括真实失败。已存在 raw 只能与返回字节相同，不能覆盖不同证据。gate/status/fetch 均含网络，当前准备期间未执行。
 
-串行范围只有原 `.runs/conv/sep13ag-fma/C57-row7shared2fma/job.json` 的 AG1583276、sep13af-campaign.json 的 AF1583230，以及明确的 P/AH job.json。缺失固定证据、固定ID变化、未知预约身份/不明ID、查询失败、非终态或缺整数job/system退出码阻止。实际 FAILED 也能清除串行占用，不能把前一候选数值失败当资源仍占用；root另行要求原结果完整登记后才GO。不扫描队友，不查询旧AD/AC等历史链。
+`accept_and_freeze.py` 复用 AH 的简洁数值日志/归档逻辑，必须显式提供未来真实 `--job-id`，并与 AJ 原 job/summary/scheduler 三方一致；当前没有假填 AJ ID。它重新读原19有序 stage、六配置/44328、无 FAIL/ERROR、实际三编译 argv、资源/源关联、wrapper/job/system全0与实际整份 `.s` FMA0。先由独立审查者和 root 完成实际目标说明，再允许一次接受归档；验证失败保留原件，不能放宽数值条件。
 
-仅显式 root --go 可提交一次，先排他创建 AH job.json 再上传/提交；任何中断或不明返回都保留预留，恢复原ID，禁止重投。status只读原ID并保留查询失败，fetch要求真实终态与两个退出字段，含失败作业；返回原件只能首次落盘或确认原字节相同，不能覆盖不同raw。无自动轮询、接受、冻结、性能、确认、ZIP、晋级或发布。
+未来 `AJ_SUMMARY.json` 沿用 AH 生命周期摘要接口：candidate/job_id、wrapper_exit/issues、19个 `{stage,exit_code}`、actual_compile_argv、compiler_version、allowed_cpus、requested_resources、scheduler_resources，以及 source_files 的 name/sha256/all_manifests_and_transport_bytes_equal。摘要必须从该原作业返回日志产生，不能从 prepared 期望值填 PASS。接受器仍直接重读各原日志，不以摘要计数替代数值核对。
 
-**未来验收边界**：自己的原job/源清单/raw身份、完整六配置与44328各类计数、19有序stage、实际argv/资源、scheduler/job/system/wrapper全0仍必须核对；全源实际FMA0仍必需。该仅pragma改变采用简洁的实际边界展开/余项/栈审查，不预先要求13stage×binary-block的庞大schema。源码仍有13语义阶段、shared2/u1余0..1，边界源循环单列加unroll2提示；实际展开、PC/区域数、spill或速度不能预填。没有复制 AE accept_returned.py、freeze_returned.py，也没有 C56 fence 合同或 AE assembly_review_schema。数值/运输成功均不自动等于接受或性能通过。
+目标汇编审查只需实际 input_5 循环和 spill、shared2/u1 是否有新 spill、全 helper 帧/明显 Z/Q/predicate 栈及 ABI、全源 FMA0，与原 AH 返回汇编比较。无需建立13阶段逐 binary-block 巨型 schema；不预填实际展开、PC、零spill或提速。需要 `TARGETED_ASSEMBLY_REVIEW.md` 与 root 的 `ROOT_RETURNED_REVIEW.md`；两者只能在实际返回后生成。原11条提示可能改变 GCC 全局调度，删一提示不保证消除所有栈流量。
 
-未来仅供 root 审后授权使用，本次未执行：
+将来仅供 root 审查并授权后使用，本次未执行：
 
 ```text
-python3 .runs/conv/sep13ah-checks/driver.py C58-row7boundaryu2 config/conv-sep12.local.json submit --go
-python3 .runs/conv/sep13ah-checks/driver.py C58-row7boundaryu2 config/conv-sep12.local.json status
-python3 .runs/conv/sep13ah-checks/driver.py C58-row7boundaryu2 config/conv-sep12.local.json fetch
+python3 .runs/conv/sep13aj-checks/driver.py C59-row7boundaryu2nofive config/conv-sep12.local.json submit --go
+python3 .runs/conv/sep13aj-checks/driver.py C59-row7boundaryu2nofive config/conv-sep12.local.json status
+python3 .runs/conv/sep13aj-checks/driver.py C59-row7boundaryu2nofive config/conv-sep12.local.json fetch
+python3 .runs/conv/sep13aj-checks/accept_and_freeze.py --job-id <实际原AJ_ID>
 ```
 
-准备来源：driver纯文本草稿chunk ed3260/0；root源审只读chunk dc4759/0；首次五文件复制/清单chunk4115eb/0。首次只读查找误用 sep13ag-checks 路径，chunk6e578b/1原输出 `rg: .runs/conv/sep13ag-checks: IO error for operation on .runs/conv/sep13ag-checks: No such file or directory (os error 2)`；root给出实际 sep13ag-fma 路径后仅修正门禁文本，未查询/重试作业。driver完整相对AE diff只读chunk5d1ec6/1是预期差异，不是执行失败。
-
-AE-to-AH.patch 完整列出 driver、README、五文件、三个metadata的对应文本差异；两个checker无diff。原文件、冻结证据、candidate source/record/best和其他队友目录不改。主额度达到40%停止、不用重置卡；准备完成FINAL/STOP供root审查。
+未来 archive 为 `.runs/conv/C59-row7boundaryu2nofive/sve-correctness-sep13aj`；不存在/不能覆盖 validation 或冻结目录，先写独立临时目录再原子 rename。归档原source/raw、摘要、目标及root说明、工具/README/独立review，`.assembly.txt` 与原 `.s` 字节一致。未测性能，不自动创建性能组、确认、包、晋级或发布。
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/source/candidate.env
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/source/candidate.env
@@ -1,4 +1,4 @@
-CANDIDATE=C58-row7boundaryu2
+CANDIDATE=C59-row7boundaryu2nofive
 EXPECTED_ACC=3
 EXPECTED_FULL=5744
 EXPECTED_DISPATCH=1212
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/source/conv2d.c
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/source/conv2d.c
@@ -1442,7 +1442,6 @@
             const float *kd = kernel + ((size_t)2) * (size_t)kw;
             const float *ke = kernel + ((size_t)1) * (size_t)kw;
             const float *kf = kernel;
-            #pragma GCC unroll 2
             for (int ik = 0; ik < kw; ++ik) {
                 const svfloat32_t ak = svdup_n_f32(ka[ik]);
                 const svfloat32_t bk = svdup_n_f32(kb[ik]);
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/source/remote_job.sh
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/source/remote_job.sh
@@ -46,7 +46,7 @@
 print('NUMA_NODE=' + nodes[0])
 CHECK_ALLOCATION
 source candidate.env
-[[ "$CANDIDATE" == C58-row7boundaryu2 && "$EXPECTED_ACC" == 3 ]]
+[[ "$CANDIDATE" == C59-row7boundaryu2nofive && "$EXPECTED_ACC" == 3 ]]
 [[ "$EXPECTED_FULL" == 5744 && "$EXPECTED_DISPATCH" == 1212 && "$EXPECTED_DIRECT" == 432 && "$EXPECTED_TOTAL" == 44328 ]]
 [[ "$EXPECTED_DISPATCH_ROWSEVEN_ENTRIES" == 1236 && "$EXPECTED_DIRECT_ROWSEVEN_ENTRIES" == 1080 ]]
 printf 'CANDIDATE=%s\n' "$CANDIDATE"
@@ -60,7 +60,7 @@
 stage compiler compiler_check
 write_manifest() {
     sha256sum conv2d.c check_conv_guard.c check_sve_dispatch.c remote_job.sh candidate.env > source-sha256.txt LOCAL_USERLOCAL_USER return "$?"
-    printf '%s  conv2d.c\n' c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751 LOCAL_USER sha256sum -c - LOCAL_USERLOCAL_USER return "$?"
+    printf '%s  conv2d.c\n' 17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9 LOCAL_USER sha256sum -c - LOCAL_USERLOCAL_USER return "$?"
     cat source-sha256.txt
 }
 stage manifest write_manifest
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/prepared.json
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/prepared.json
@@ -1,21 +1,21 @@
 {
-  "candidate": "C58-row7boundaryu2",
+  "candidate": "C59-row7boundaryu2nofive",
   "status": "prepared",
   "complete": false,
-  "prepared_at": "2026-09-12T20:01:29.273970+00:00",
-  "source_parent": "C52-row7x3shared2",
-  "source_experiment": "C58-row7boundaryu2",
-  "source_sha256": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751",
-  "production_source_sha256": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751",
+  "prepared_at": "2026-09-12T20:37:41.104221+00:00",
+  "source_parent": "C58-row7boundaryu2",
+  "source_experiment": "C59-row7boundaryu2nofive",
+  "source_sha256": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9",
+  "production_source_sha256": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9",
   "source_hashes": {
-    "candidate.env": "1d4e60cff8b53cfd662af3ee48e16fbb1f21f9b9de6fd396aa7747e945582972",
+    "candidate.env": "595f45c73c6aed9ff7804bdf4ab2483a21376ef9bbb54fc6bdb078907eda1f1e",
     "check_conv_guard.c": "ccbba2637255b5d2733dfbfdddd88afd0d09815d5759ca86c9e8c498475990d5",
     "check_sve_dispatch.c": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d",
-    "conv2d.c": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751",
-    "remote_job.sh": "437843ec63b427d6800dd18390736ebd382ec81dbe6730d5a5c3985a68d9ba46"
+    "conv2d.c": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9",
+    "remote_job.sh": "f7c25ed914b9d376f0fe5a2b8cd87e31d5865eea083da05684f04aeca0fabf6c"
   },
-  "source_hash_scope": "First five-file AH transport byte-count/SHA manifest after root independent C58 source review; two original AE checker bytes.",
-  "guard_template": ".runs/conv/sep13ae-checks/C56-row7shared3fence/source",
+  "source_hash_scope": "First AJ five-file transport manifest after root independent C59 source review; both AH/AE checker bytes unchanged.",
+  "guard_template": ".runs/conv/sep13ah-checks/C58-row7boundaryu2/source",
   "guard_change": "No checker byte changes. Inherited quad_boundary/grid origin comments remain original; source main remains shared2.",
   "rows_per_group": 7,
   "vectors_per_row": 3,
@@ -148,29 +148,29 @@
   "executed": false,
   "verified": false,
   "local_compilation_or_tests_run": false,
-  "production_assembly_review": "Pending actual C58 GCC10.3.1 uninstrumented assembly: whole-source FMA0 and concise targeted boundary unroll/odd-remainder/stack review. No prefilled PCs, unroll decision, zero-spill result or speedup; no AE fence or full per-binary-block schema.",
-  "source_parent_frozen_directory": ".runs/conv/C52-row7x3shared2/sve-correctness-sep13t",
-  "source_parent_diagnostic_job_id": "1582134",
+  "production_assembly_review": "Pending own C59 GCC10.3.1 assembly: input_5 loop/spills, shared2/u1 new spills, helper frame/ABI and whole-source FMA0. No prefilled machine results or speed claim.",
+  "source_parent_frozen_directory": ".runs/conv/C58-row7boundaryu2/sve-correctness-sep13ah",
+  "source_parent_diagnostic_job_id": "1583350",
   "expected_semantic_stage_count": 13,
   "shared_kernel_columns_per_iteration": 2,
   "shared_remainder_source_columns_per_iteration": 1,
   "shared_remainder_max_columns": 1,
   "preparation_authorizes_compute": false,
   "performance_measured": false,
-  "scheduling_scope": "Only original AG1583276 and AF1583230 plus present P/AH reservations. Actual terminal with integer job/system exits; FAILED can clear serial gate. Root waits for complete original result recording before GO.",
-  "inherited_grid_name": "quad_boundary names the unchanged checker boundary grid only, not the C58 source unroll factor.",
-  "shared_width_coverage": "Original shared2 source remains unchanged: kw1 skips main; even/odd widths exercise main/u1 remainder. Boundary pragmas request unroll2 of twelve source loops; actual compiler paths unknown.",
+  "scheduling_scope": "AI1583408 complete original36 evidence, AH1583350 and explicit P/AJ reservations. AK has priority: fixed AK job ID must be supplied after it actually exists and independently reviewed; missing binding blocks submit. Root verifies quota below40 before GO.",
+  "inherited_grid_name": "quad_boundary names the unchanged checker grid, not the C59 unroll factor.",
+  "shared_width_coverage": "Shared2/u1 unchanged. Eleven boundary loops retain unroll2 hints; input_5 alone has no hint. Compiler decision and spills unknown.",
   "production_source_hashes": {
     "README.md": "97526cd2b7e9d54dc1fc3d83902e85fa4d729bcbc00178d243a4e22cf5912fe8",
     "bench_conv.c": "2548861ae7e29826e454b4c0b098d682f7f04996222e664cd1ea9bc92dd2e927",
-    "run.sh": "6aa53c3f5d3fc8ddf8ae6a898043f4f1c40f8778db5d71bb7be6087983349868",
-    "conv2d.c": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751"
+    "conv2d.c": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9",
+    "run.sh": "6aa53c3f5d3fc8ddf8ae6a898043f4f1c40f8778db5d71bb7be6087983349868"
   },
   "source_parent_source_hashes": {
     "README.md": "97526cd2b7e9d54dc1fc3d83902e85fa4d729bcbc00178d243a4e22cf5912fe8",
     "bench_conv.c": "2548861ae7e29826e454b4c0b098d682f7f04996222e664cd1ea9bc92dd2e927",
-    "run.sh": "6aa53c3f5d3fc8ddf8ae6a898043f4f1c40f8778db5d71bb7be6087983349868",
-    "conv2d.c": "8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7"
+    "conv2d.c": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751",
+    "run.sh": "6aa53c3f5d3fc8ddf8ae6a898043f4f1c40f8778db5d71bb7be6087983349868"
   },
   "boundary_source_loop_count": 12,
   "boundary_source_unroll_hint": 2,
@@ -178,6 +178,10 @@
     "Own original job/source/manifests/raw must match; no inherited parent PASS.",
     "Six full/dispatch/direct configurations and exact44328 counts, worker entries/masks and unchanged checks.",
     "All19 ordered stages, actual compiler/build argv/resources and scheduler/job/system/wrapper exits zero.",
-    "Whole original production assembly FMA0; concise actual boundary unroll/stack review, no prefilled machine result."
+    "Own whole-source FMA0 and concise actual input_5/shared2 spill/frame review read by root; no inherited AH machine conclusions."
+  ],
+  "boundary_unroll_hinted_loop_count": 11,
+  "boundary_unroll_excluded_stages": [
+    "input_5"
   ]
 }
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/source-hashes.json
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/source-hashes.json
@@ -1,7 +1,7 @@
 {
-  "candidate.env": "1d4e60cff8b53cfd662af3ee48e16fbb1f21f9b9de6fd396aa7747e945582972",
+  "candidate.env": "595f45c73c6aed9ff7804bdf4ab2483a21376ef9bbb54fc6bdb078907eda1f1e",
   "check_conv_guard.c": "ccbba2637255b5d2733dfbfdddd88afd0d09815d5759ca86c9e8c498475990d5",
   "check_sve_dispatch.c": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d",
-  "conv2d.c": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751",
-  "remote_job.sh": "437843ec63b427d6800dd18390736ebd382ec81dbe6730d5a5c3985a68d9ba46"
+  "conv2d.c": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9",
+  "remote_job.sh": "f7c25ed914b9d376f0fe5a2b8cd87e31d5865eea083da05684f04aeca0fabf6c"
 }
--- .runs/conv/sep13ah-checks/C58-row7boundaryu2/source-manifest.json
+++ .runs/conv/sep13aj-checks/C59-row7boundaryu2nofive/source-manifest.json
@@ -1,7 +1,7 @@
 {
   "candidate.env": {
-    "bytes": 205,
-    "sha256": "1d4e60cff8b53cfd662af3ee48e16fbb1f21f9b9de6fd396aa7747e945582972"
+    "bytes": 211,
+    "sha256": "595f45c73c6aed9ff7804bdf4ab2483a21376ef9bbb54fc6bdb078907eda1f1e"
   },
   "check_conv_guard.c": {
     "bytes": 8698,
@@ -12,11 +12,11 @@
     "sha256": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d"
   },
   "conv2d.c": {
-    "bytes": 108874,
-    "sha256": "c8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751"
+    "bytes": 108841,
+    "sha256": "17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9"
   },
   "remote_job.sh": {
-    "bytes": 3941,
-    "sha256": "437843ec63b427d6800dd18390736ebd382ec81dbe6730d5a5c3985a68d9ba46"
+    "bytes": 3947,
+    "sha256": "f7c25ed914b9d376f0fe5a2b8cd87e31d5865eea083da05684f04aeca0fabf6c"
   }
 }
--- /dev/null
+++ .runs/conv/sep13aj-checks/STATIC_REVIEW.md
@@ -0,0 +1,11 @@
+# AJ 静态准备说明
+
+只作文本准备；没有导入/运行 driver、接受器、AST/py_compile、编译/算子、SSH、new/checkpoint、预约或接受归档。所有源码、原记录、AH 冻结及队友文件未改。
+
+- 来源严格为最终 AH driver/wrapper/两checker/简接受器。五文件首次复制前只读核对 C59/父 C58 当前生产四文件和原 AH 冻结 conv；把删除的33字节 pragma 插回1445行，完整恢复父字节。
+- 两 checker 没有任何差异。wrapper 只有身份/SHA 两处差异，env 只有身份差异；原44328/19stage/flags/resources/位比较不改。额外 memcmp/FMA0 是当前实验路线，不解释为题目全面禁止 FMA。
+- driver 只改本版/父身份与 AH44328、11提示/input_5 排除字段，串行缩为 AI/AH/P/AJ 加优先 AK；只读检查 AI 完整36记录，保留原结论。`AK_JOB_ID=None` 有意阻止提交，未来只允许真实 ID 的 root 审核绑定。
+- 接受器保持 AH 数值解析，未来 ID 改为显式参数并核对原记录；另检查 scheduler jobId、摘要19stage一致、实际五文件SHA。依赖原生命周期摘要提供真实资源/manifest关联，依赖独立/root目标审查，不能把占位说明当实际结果。
+- prepared/source清单仅描述已准备字节和未来期望；未填 AJ job/PASS/spill/速度。接受归档目录不存在且没有 job/raw/validation，当前状态始终 prepared only。
+
+`AH-to-AJ.patch` 提供全部对应文本差异；`PREPARED_FILES.json` 是最终准备文件身份清单（排除自身）。quota由root真实读取并在40%停止，禁止reset。完成后停写供独立审查。
--- /dev/null
+++ .runs/conv/sep13aj-checks/PREPARATION.md
@@ -0,0 +1,7 @@
+# AJ 准备留痕
+
+root 授权首复制前已独立源审，O 随后确认 C59 全部源码/记录 FINAL。只读身份核对 chunk4c83c2/0；首次五文件复制、首清单/prepared、driver/acceptor纯文本生成 chunk44ea5e/0。该操作没有导入或调用生成的工具，未执行算子/SSH/new/checkpoint。后续仅静态文本核读和必要文档收尾。
+
+实际 C59：108841字节，SHA17e39de400213c6143450ca51436eecb780ee5806b8375fd38f0b6694b2f4be9。父 C58：SHAc8d3fb6dc2a3b02ba509b029efe802dc1ec37430467642d23d5815f6beaea751、AH1583350；生产三配套文件全同。checker各与 AH 冻结原字节全同，五文件SHA/大小见source-manifest.json。
+
+AK 尚无实际 ID，本模板暂以None硬阻止提交。当前只有准备文件，无 AJ job/raw/validation/冻结；未声称未来通过，未变更旧结论。准备过程没有失败的算子/作业或计算重试。
