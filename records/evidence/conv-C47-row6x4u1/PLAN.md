# C47-row6x4u1：六行块仅扩宽到四个SVE向量

状态：source-only prepared，尚未编译、专项诊断、汇编审查或性能测量；不创建诊断包、不SSH、不提交作业、不用重置卡。parent/source_parent均为C40-row6x3u1。正式最佳仍是C6。准备时G轮C40内部总中位数444.40 ms、对两端控制的噪声门槛均不合格；J1579660正在独立复测，不能把C40写成已晋级基线。

## 唯一变量与保留项

只在原conv_sve_rowsix内把block从3*svcntw()改成4*svcntw()，每行增加索引3的一个累加器、对应输入窗口更新及最终store，共18→24累加器。保留五个leading阶段、一个shared t循环、五个trailing阶段；每阶段仍ik单列递增。原n0/1/2更新逐字保留，新n3沿同一活动输出行映射复制n2。没有移动或合并系数广播，不额外展开kernel列，不加入asm、prefetch、lane multiply、FMA、部分和或重关联。

六行分派、大小kernel gate、原helpers、quad+pair横向尾部、最后1..5输出行处理、global输出行距、非SVE与invalid路径保持原字节。三个其它提交文件README.md/bench_conv.c/run.sh不改。该候选只与同轮C40对照可隔离六行内横向宽度，不能用与C41的不同tile形状比较归因。

## 待验证的取舍

| 完整shared迭代，源级计数 | C40 6×3VL | C47 6×4VL |
| --- | ---: | ---: |
| 输出向量乘加更新 | 18 | 24 |
| 输入向量加载 | 3 | 4 |
| 系数广播 | 6 | 6 |
| 独立mul / add | 18 / 18 | 24 / 24 |

每输出向量更新的输入加载率仍1/6；系数广播率从1/3降至1/4，单步回边可摊给24个而非18个更新。新增更宽的横向工作块和六个acc可能使调度/寄存器/代码体积变差，尾部切分也会改变，不能据理论计数预称提速。

原代码每个n窗口按六个输出行更新，因此源级共享处是24acc+6个系数+1input+1product，理想活跃需求已约32Z。另一种可能的编译器调度可保留4input、逐系数更新，预算约24acc+4input+1coef+1temp=30Z；这里只说明可能的调度，**没有改源码到该调度，也不能据30/32源级值宣称无spill**。GCC可提前加载、保留多个product或ABI状态，实际开销要看目标GCC10.3.1十一阶段及转场汇编。

## 后续边界与执行要求

根代理独立审查后再决定远端诊断。应覆盖VL16/32/64字节、1/4线程、ow在4VL与8VL前后/窄列/多块+尾、kh<6直接helper防御分支与kh6/7以上、kw1及奇偶、oh6分组剩余1..5行。原C40 26112项已通过不能代替本候选的诊断或新增4VL边界；完整quad/pair尾部必须重新验证。全部编译/逐位guard/汇编和性能工作只在调度计算节点执行。

如果诊断通过且根决定测速，需同分配C40/C47及未变C6控制，固定compiler/flags、38线程绑定、官方输入/完整独立重复与参考。保存失败和波动；当前未有C47速度或晋级结论。candidate.patch包含全部源改动，checkpoint record为最终source身份。
