# C46 independent source review

No source-level blocker found. This review reads the C42/C46 source diff and C46 PLAN/STATIC_REVIEW; it does not compile, run, submit or claim target acceptance. C42 job1579627 remains a failed first compilation with zero numerical cases and no production assembly.

The actual diff is confined to five inline-asm blocks in the quint shared middle. Each block has ten instruction-string lines, with three operands per line: all 150 `%Z[name]` spellings become `%[name]`. The `.s` suffix, operand names/order, 25 FMUL plus 25 independent FADD instructions, escaped newlines, declarations and every other source byte are preserved by this replacement. The five `+&w` accumulators, `=&w` scratch, six `w` inputs, `volatile` and `memory` clobber are unchanged.

The upstream GCC10 backend excerpts already verified and cited in PLAN are consistent with the proposed fix: [aarch64_print_operand](https://raw.githubusercontent.com/gcc-mirror/gcc/releases/gcc-10/gcc/config/aarch64/aarch64.c), default `case 0` around9827/9835–9839, prints a single-register SVE data-mode operand as `z%d`. `%[name]` selects this default renderer under the [named-operand syntax](https://gcc.gnu.org/onlinedocs/gcc/Extended-Asm.html); the template supplies `.s`. Every C46 operand is a single `svfloat32_t`, not an SVE tuple. The cited modifier switch has no `Z`, and its default error matches the actual C42 diagnostic. These are the author's retained upstream source references, not a saved copy of the installed target10.3.1 backend; this reviewer has not represented them as a new target compile result.

No input/kernel/output address, loop bound, step, helper interface, full5VL guard, tail/fallback or OMP dispatch changes. Each output keeps increasing kernel-row/kernel-column order, and each template still multiplies input by its coefficient before adding to that output's accumulator. The other eight quint phases, all older helpers, non-SVE path and benchmark/runner/README retain their previous content. Therefore the formatting fix introduces no new pointer range or arithmetic regrouping.

Early-clobber constraints still express nonoverlap with inputs; they do not prove that the target allocator accepts this register-pressure case. The nominal25acc+5coefficient+input+scratch budget is unchanged. Actual acceptance of all five blocks, printed Z-register operands, scratch disjointness from inputs and all live accumulators, concrete FMUL/FADD order, whole-source FMA absence, and spills through all nine stages/transitions remain target review requirements. A `memory` clobber is a compiler scheduling constraint, not a hardware fence.

Conclusion: suitable for a new prepared diagnostic package. Preserve failure if compilation still fails; do not weaken constraints or reuse C41 correctness/assembly results as C46 results. Formal best remains C6. Prepared source identity, transcribed from checkpoint: `a04c74c8701c394fd620d9fa91d421898cf4aa2b7c96b87de21cf301cd4e2e8d`.
