# C35-prefetch2 静态审查

审查范围仅为 C26-row4loads → C35-prefetch2 的轻量源码差异；没有执行编译、测试或算子。

- **定位正确：** 仅在 `conv_sve_rowquad` 现有 `t=3..kh-1` 共享中段顶部插入一个条件块，现有 `base/stride/i/lanes/kh/t` 均在作用域内。没有命中 rowpair 或 rowtriple 的循环。
- **先检查后形成地址：** `future` 声明完全位于 `kh-t>2` 分支内。成立时 `t+2<=kh-1`，int 加法和原数组内行地址成立；size_t 转换在乘行距之前。
- **四个起点：** 恰有四次 `__builtin_prefetch`，位置为 future+0/1/2/3×lanes，参数统一为读/高局部性 `(0,3)`。无权重提示、kw 内循环提示或额外第五向量地址。
- **水平与垂直范围：** `i+4*lanes<=ow` 保证末向量完整范围不超过输入行；`j+t+2<=j+kh-1` 不超过合法输入行。kh4/5 不触发，kh6 从 t3 开始触发一次；最后两行不会构造 future。
- **算法不变：** 插入点后的原 row、四组 kernel 指针、全部双步/单步累加、首尾阶段、尾部 helper、非 SVE 路径及调度保持原文。不引入 FMA、改容差或识别官方尺寸。
- **交付范围：** 新候选从父版本复制四个 source 文件，仅 `conv2d.c` 改动；`bench_conv.c`、`run.sh`、源码 `README.md` 保持父快照。PLAN 和本页只在新候选目录，不修改旧候选或正式 `conv/`。

静态结论：未发现新增非法指针形成或输出并行冲突。预取是否实际生成、是否导致 spill、是否影响速度均未知，必须等待根代理安排超算诊断与同资源测量；候选只登记 prepared，不登记通过或晋级。
