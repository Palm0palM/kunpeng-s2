# r12 CT 候选日志与汇编独立审查

作业 **1579645** 已为 SUCCEEDED，job/system exit 均0，cohort退出0。只读核对取回的原始日志与汇编，未发现阻塞缺陷。四成员均已 verified；正式结论继续保留 **T8-control12（T8-svepanel16）**。

## 正式与预热隔离

四成员各有一个完整预热套件3 PASS，合计12；各正式日志恰有按1/2/3顺序的三个完整官方套件9 PASS，合计36，全部原精度误差≤1e-12。正式日志不含 WARMUP 标记。按预先固定的 ABCD / BCDA / CDAB 顺序保留全部三轮，不排除慢样本；预热摘要、原日志与正式记录数量一致。

T8/T13使用 --repeat-existing，保留原实现身份和历史证据；T15/T16为新登记，parent均为当前T8。T8的取源目录source_from与历史实现lineage由私有record helper按原source_*身份恢复，不另建T8实现版本。四个正式record的实测数字与本次从原日志重建的中位数一致。

## CT 与补充检查

T13/T15/T16分别传入CT64/128/32。每个候选实际完成28组直接微核、28次整算子、56例入口参数检查，总参数观察463420次，错误0；shared packed分配失败、no-SVE和实际窄VL各1例。每候选七个进程均仅出现一次与配置相同的 `TILE_CONFIG_PASS KB=256 CT=...`，严格summary字段一致。原9个full尺寸全部保留，并追加4111×127/128/129；fallback smoke为4111×129。独立复核三个候选各15步骤退出0，分配失败实注入1次后新核实际入口30912；no-SVE及实际16-byte窄VL入口均0。整算子最大误差3.885780586188048e-16、最大残差8.034328967208903e-16。

## 原源码汇编

三个候选的 commands.txt 均表明 `-S` 输入是各自原 `source/trsm.c`，未使用测试插桩副本、-finstrument-functions或TRSM_TEST宏。冻结的T15/T16源码与T13仅CT enum值不同。

三份 `trsm-wide4x32.s` 的 **update4x32_sve 函数文本完全相同**，函数位于456–623行，共157条指令。热循环标签 `.L25` 在488行，指令489–522行共 **34条**：16 fmla、4 ld1d、3 ldr、1 ld1rd、3 mov、4 add、1 ptrue、1 cmp、1 bne。34是单次k迭代的静态指令数，不是整个函数的指令数。

全函数没有sp/wsp使用、栈帧内存访问或bl/blr调用，未见spill。16个累加器保持在z16–z31。这里仅审查目标计算节点实际生成的文本，没有本机重新编译。

CT改变大路径行/列任务划分及可能的复用机会；相同微核汇编不能预测整算子性能，也不能证明增大或减小CT必然更快。

## 实测结论

| 版本 | CT | 512×19968 ms | 2432×17024 ms | 17024×512 ms | 中位数合计 ms |
| --- | --- | ---: | ---: | ---: | ---: |
| T8-control12 | T8原核 | 19.01 | 224.62 | 194.97 | 438.60 |
| T13-sve4x32 | 64 | 18.73 | 218.87 | 181.03 | 418.63 |
| T15-svetile128 | 128 | 18.84 | 222.88 | 189.62 | 431.34 |
| T16-svetile32 | 32 | 18.73 | 218.22 | 185.05 | 422.00 |

| 候选 | 合计增益 | 本轮噪声门槛 | eligible |
| --- | ---: | ---: | --- |
| T13-sve4x32 | 4.5531% | 5.2604% | false |
| T15-svetile128 | 1.6553% | 5.2604% | false |
| T16-svetile32 | 3.7848% | 6.6355% | false |

CT64的T13在大用例181.03ms和合计418.63ms最快；中用例单项CT32的T16为218.22ms，略快于CT64的218.87ms。三个候选都未超过晋级门槛，保持T8。T13/T15的5.2604%门槛由本轮T8小用例波动决定，T16的6.6355%门槛由其中用例波动决定。性能判断来自同分配正式日志，不从汇编推断。

实际参考为KML25.1/GCC12.3.1，未宣称指定KML25.2复验或官方得分。完整逐成员指标、循环指令分类与相对证据路径见 [assembly-review-tile.json](assembly-review-tile.json)。本次仅写此md/json，未运行题目、SSH或哈希操作。
