# T16：T13 大路径 CT=32

2026-09-12，从T13-sve4x32复制五文件，仅把 `enum { KB = 256, CT = 64 };` 改为CT=32；尚未编译、测试、登记或提交。T13仍未晋级，当前最佳为T8-control12（T8-svepanel16）。

本候选与T15-svetile128考察同一分块参数的另一方向。CT同时决定大路径输出行块和列块；减小CT增加外层任务数与调度颗粒度，可能改善后段并行度，同时减少同一行组连续处理的列数。对17024×512首个更新，任务数从2096增到8384；最后非空更新从16增到64。这里只推导任务量，不声称已经改善缓存或运行时间。

32仍整除KB256，也是16/8/4行与32/8列微核的整数倍；所有内核、分配、打包、对角解、屏障、FMA顺序、线程、精度、尾部和小路径不变。与T13的源码差异仅这一处enum，非算子文件原样保留。

后续应与T8、未变T13和CT128在同一分配比较。宽预检使用T15计划的12个完整维度，1/4线程各一轮，另四种smoke，共28整算子；原31/32/33列已经覆盖本候选CT32边界，补充127/128/129能使同批各成员覆盖一致。保留28直接核、参数观察、共享分配失败、无SVE、真实窄VL、padding/L不变与通用预检。最终判定仍要求同环境收益超过波动且无超过1%的单例退化。

只有本机轻量编辑和整数推导；所有题目编译/正确性/benchmark均等待超算计算节点。无哈希、无公共工具或其他题目修改，无正式比赛提交。
