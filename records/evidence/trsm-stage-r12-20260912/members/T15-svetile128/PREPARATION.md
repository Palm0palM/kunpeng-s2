# T15：T13 大路径 CT=128

2026-09-12，T13两轮大用例分别快6.49%/6.79%，但合计均未越过当轮波动门槛，仍未晋级，最佳为T8-control12（T8-svepanel16）。本候选从T13-sve4x32复制五文件，仅将 `enum { KB = 256, CT = 64 };` 改成 CT=128；尚未编译、测试、登记或提交。相对T13只有这一处文本差异。

CT同时控制大路径 `collapse(2) schedule(static)` 的输出行块与列块。增加CT减少外层任务数量，并让同一行组连续处理更多列；假设是改变数据复用与任务开销之间的取舍。原4×32/16×8/8×8/4×8核、KB256、面板分配与地址、对角求解、隐式barrier、SVE/VL、精度、尾部和小工作集不变。不把算法预算声称为硬件cache容量，不预判实际缓存命中或性能。

对17024×512，首个更新阶段剩16768行，CT64/128的任务数为2096/524；最后非空更新剩128行，为16/4。较少任务也可能降低尾部并行度。128仍同时整除KB256，并为16、8、4、32的整数倍，现有行/列微核与面板边界对应；每输出累加次序不变。

建议下一轮在同一分配同时比较T8、未变T13、CT128及CT32，各预热一套、正式三套，完整保留所有样本。新候选的晋级父基线仍需以当时实际best为准；T13只是同环境形状对照，不把未晋级版本当作已晋级父基线。另报告相对同期T13的差值以区分CT改动。

新宽预检保留28直接核和原9个整算子维度，增加4111×127/128/129覆盖CT128两侧，完整12例分别1/4线程；smoke改4111×129，另做38线程、共享packed失败、无SVE、实际16字节窄VL，共28个整算子检查。独立入口公式 `sum_kk floor((m-min(kk+256,m))/4)*floor(n/32)` 对CT32/64/128均适用。四panel参数观察与L/B guards继续检查；通用14直接、406整算子、28空操作预检不变。所有实际编译/测试只在38 CPU单NUMA超算节点，KML25.1/GCC12不等于指定KML25.2复验。

当前仅本机轻量源码编辑和整数任务量推导，没有题目编译、测试、性能运行或哈希操作。旧候选、公共工具与其他题目未改。
