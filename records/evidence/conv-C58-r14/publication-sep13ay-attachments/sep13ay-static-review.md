# AY unbound performance template

Prepared as text only from current AW. Order C58-r14 / C65-row7balanced / C58-r15; both control run/record paths were checked absent and not created. Standard parent/control remains current C7=C58-r1.

All three required bindings remain None: AX_JOB_ID, EXPECTED_DIAGNOSTIC_CASES, EXPECTED_SOURCE_SHA256. diagnostic() first checks an actual numeric job string, positive integer total (bool rejected), and64-character lowercase hex source. Any placeholder blocks before network, reservation, creation or metadata writes. Root must bind actual accepted AX total from new38-thread/coverage-observer diagnostics; no44328 constant is inherited. Validation total/source must match both bound values and current C65 source. The reservation records bound total and source for provenance.

Only complete version strings, paths/identifiers and word-boundary round labels changed. ASSEMBLY/PASS fields remain exactly intact. AX accepted/frozen result and real review files remain prerequisite; final diagnostic schema/count are not invented. Live AX must be original bound ID with successful job/system0. Actual coverage results belong to accepted AX evidence and must be reviewed there before performance.

Original36 official samples remain three independent full suites per member in one allocation, current C7 resource/settings and strict unmodified runner, actual GCC10.3.1/aarch64/38CPU checks, source/job/settings/machine equality, wrapper0 and original-log reparse. Unique campaign/controls, source_parentC58-r1, comparison parentC58-r15, preservedcreation and pre-ay snapshot, standard record and both standard comparison gates unchanged. Failure/slow samples retained; no automatic confirmation/promotion/package. Failed scheduler group still requires root to fetch original failed logs separately, not rerun.

Only ast.parse text syntax and file existence reads were performed; no import/execution of AY, network, candidate or controls creation. No local operator compilation/tests and no reset. Final AX source/count/job are explicitly pending root binding; no outcome is prefilled.
