# AX/AY publication preparation — text only

Prepared `sep13ay-prepare-publication.py` and `sep13ay-publication.from-aw.patch`. Read the complete generated script and checked syntax with `ast.parse` on text. No publisher import/execution, export, experiment, network, Git commit or push occurred.

The script waits for the actual completed AY campaign and standard C58-r14 / C65-row7balanced / C58-r15 records. It requires the original successful job, 36 complete PASS/error-zero samples, matching settings/machine/source, and candidate/campaign comparison consistency. Totals, per-case samples, gain and qualification text are generated from those final records. Either eligibility outcome is supported; passing initial screening explicitly still requires independent confirmation. No candidate speed or qualification was prefilled.

Diagnostic identity is fixed to accepted own AX 1591086, source `a855c14b81c00f3d398ac36c5ece6726e15235f18ebf3fe4746570da25a7f874`, 80100 cases, nine VL/thread configurations and 25 stages. It checks 3156 exact-once cases per configuration, 28404 in total, 110027484 actual output values, zero address errors, dispatch entry totals 4008/6168/24010 with masks and direct 1080 with masks. GCC strict/generic/ACC3 and accepted raw/summary evidence remain required.

Assembly assertions use the actual AX schema: shared/remainder/helper 123/63/2448 instructions; shared 42 mul/42 add/6 input loads/14 broadcasts, remainder 21/21/3/7; shared and remainder zero Z stack accesses, helper 9 Z reads/15 writes. Metadata is CNTW/RET, dispatcher 205 instructions with three UDIV, and fused count is zero. Text describes the actual 720B+3VL helper frame and unchanged C7 helper counts without claiming a speed benefit. Only dispatch/source organization changed.

Attachments include all existing `sep13-c65-*`, `sep13ax-*`, `sep13ay-*` text logs/scripts plus AX root template files and both checker C sources. Concrete checked labels include **sep13-c65-prepare-driver.command.json**, new/checkpoint, AX submit/accept/summary, root entry/design derivations and AY submit. Required AX expected table, independent derivation, original acceptor and error-marker fix are explicitly retained. The final AY record-compare command must exist before export. Candidate patch, source audit/generator, source/root/independent reviews, creation originals and frozen actual diagnostics are checked after the standard sanitized exporter. No AX/AY record is invented or replaced by a historical round.

Output names are `exports/conv-sep13ax-ay-final`, `docs/CONV_SEP13AX_AY.md`, `docs/CONV_SEP13AX_AY_ROUND_RECORDS.md`, and independent AX/AY publication metadata. Existing `conv/result/README.md` is neither edited nor exempted. Every `conv/`, `zgemm/`, `trsm/`, `outputs/`, tools/config/tests path and `records/best.json` is protected; current C7/Z1/T19 ZIP bytes and best record must still match the clean publication checkout's starting commit. Only evidence/docs/index notices outside protected directories are prepared. The script itself does not commit, push, promote, confirm or package.

Pending: root reads the final script and executes it only after the original AY lifecycle completes. This static preparation is not an export or publication result.
