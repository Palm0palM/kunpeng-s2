# C54 后续边界建议：尚未执行

以下仅是未来诊断设计输入，未创建 harness/job、没有固定最终用例总数或 PASS。C54 必须关联自身源 SHA；父 T 主矩阵 kw1..3 不能覆盖本版的 factor4 主循环。建议沿用独立 scalar 逐位参考、输入/权重只读、两端 guard/canary、输出 poison 和 VL 字节16/32/64 × 线程1/4 的六配置。

|必须覆盖 kw|四列 main 轮数|原 u1 余项列数|覆盖意图|
|---:|---:|---:|---|
|1|0|1|跳过 main，单余列|
|2|0|2|跳过 main，连续两次 u1|
|3|0|3|跳过 main，连续三次 u1|
|4|1|0|恰好一轮，不能读取第5列|
|5|1|1|四列 main 后一列余项|
|7|1|3|四列 main 后三列余项|
|8|2|0|重复 main 回边，无余项|
|15|3|3|多轮后最大余项|
|81|20|1|较长循环及最后一列紧贴边界|

kw6（1 main +2 u1）建议补充，以直接覆盖进入 main 后的两列余项；上表九个宽度是本次指定必需集合，最终矩阵由根审查确定。应明确将这些 kw 与 `kh>=7, oh>=7, ow>=3L` 配对，避免只有 fallback PASS。L=svcntw，六配置对应 L=4/8/16、3L=12/24/48。

|维度/路径|建议边界|需要区分的行为|
|---|---|---|
|kh|6、7、8、9、15、81|阈值下不进入新增主循环；kh7 恰好一次 shared 外层 t；kh8 及以上有重复外层及尾部转场|
|ow|3L−1、3L、3L+1、6L−1、6L、6L+1|完整块跳过/进入、横向尾部、两块边界；配对各 kw 并保持真实 global stride|
|oh|1..15、20、21、22、28|7 行阈值、余1..6、两个/多个组及多线程组调度|
|direct 小 kh 防御|kh1..6，oh7/28，窄宽及足主块宽|保留 quad+triple 等原防御行为；不能计作 shared4 main 覆盖|
|非法维度/non-SVE|沿用原独立诊断项目|源码原字节保留仍需如实报告实际覆盖，不能由 SVE PASS 推断|

有效形状取 h=oh+kh−1、w=ow+kw−1。尽量将最后 kernel coefficient 和最后 input load 置于分配边界前，检查 main 最后一个 column=ik+3 与余项切换；输入行跨度始终为原 w。生产未插桩数值、完整分派入口和 direct 防御应分别计数。进入 rowseven 只证明 helper 入口，须从尺寸/路径证据进一步证明四列 main 及 0/1/2/3 余项均执行。

未来实际汇编应覆盖所有 compiler helper clone 的13个语义阶段、shared main（4列）及 u1 remainder（1列）的实际 region、全部转场、ABI 与 Z/Q/谓词 spill，并确认每个 accumulator 严格按列更新和实际 FMA 情况。编译器若改变展开形态或合并路径，应按真实范围解释，不能套用父 T 的 paired2 范围/指令数。没有本版汇编时，不宣称零 spill 或提速。
