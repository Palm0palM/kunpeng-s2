# C54 shared4 独立源码审查

结论：在本次限定的源码改动中，未发现静态阻碍。此结论允许 root 评估下一步独立诊断，不代表编译、数值、汇编或性能通过。没有执行或导入实验工具，没有编译/运行算子、SSH、提交作业、改动源码或记录。本次仅新增本报告。

审查前已读取项目规则并查看共享工作区，保留其他人的修改。独立完整阅读了候选 shared 区域（conv2d.c 1477–1695）、T 冻结父源相应区域（1477–1613）、完整 candidate.patch，以及入口防御、完整块条件、输出 store、横向尾部和分派相关源码。随后用轻量字节比较核对整个未改前后缀和三个其它提交文件；没有把作者 audit 的布尔值当作独立证据。

Galileo 已明确确认源码、生成器、准备说明、patch/audit 全部 final-ready 并 STOP，最终身份与独立读取的实际字节一致：

| 对象 | conv2d.c SHA256 |
|---|---|
| C54-row7x3shared4 | `fa0f5b43fc7901e9853dbe189385653ea4ae8bf697fa3b67af7f288063a4fb88` |
| C52 T1582134 冻结父源 | `8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7` |

父源取自 `.runs/conv/C52-row7x3shared2/sve-correctness-sep13t/source/conv2d.c`。候选共 2072 行，父源 1990 行；第 1–1487 行完全相同，候选第 1655 行至文件末尾与父源第 1573 行至末尾完全相同。因此差异仅为 shared 主循环 factor2→factor4 的注释、循环头和两个新增列 body；其它十二个语义阶段、原 u1 余数、21 个输出 store、kh<7 防御、横向尾部、全部 SVE/非 SVE/非法尺寸分派均保留原字节。README.md、bench_conv.c、run.sh 与 C52 生产父源逐字相同，哈希分别为 `97526cd2b7e9d54dc1fc3d83902e85fa4d729bcbc00178d243a4e22cf5912fe8`、`2548861ae7e29826e454b4c0b098d682f7f04996222e664cd1ea9bc92dd2e927`、`6aa53c3f5d3fc8ddf8ae6a898043f4f1c40f8778db5d71bb7be6087983349868`。

| 四列作用域源码范围 | column | 独立字节/语句核对 |
|---|---|---|
| 1490–1530 | ik | 去除 column 声明差别后等于父第一列完整 body |
| 1531–1571 | ik+1 | 同上 |
| 1572–1612 | ik+2 | 同上 |
| 1613–1653 | ik+3 | 同上 |

每列按 a0,b0,…,g0；a1,b1,…,g1；a2,b2,…,g2 更新原 21 个 accumulator。每列都是 7 个系数广播、3 次输入向量载入、21 次 svmul_f32_x 和 21 次 svadd_f32_x；主循环四列合计为 28/12/84/84 个源码调用。每个 accumulator 的当前值严格依次接收 ik、ik+1、ik+2、ik+3，再进入下一轮；没有 partial sum、树形合并、重关联或新的 FMA intrinsic、asm、prefetch。局部系数仍各自来自 ka..kg[column]，三个向量仍来自 row+column+0/1/2*lanes。四个词法作用域不保证编译器限制实际 live range，不能据此声明零 spill。

合法公共入口先排除 kernelWidth<=0。新循环由 ik=0 开始，维持 `0 <= ik <= kw <= INT_MAX`，所以 `kw - ik` 可表示；进入条件 `kw - ik >= 4` 推出 `ik+3 <= kw-1`、`ik+4 <= kw`，四个 column 与 ik+=4 均无新增有符号溢出。退出时 `0 <= kw-ik < 4`。原 `for (; ik < kw; ++ik)`（1655–1694）原字节保留，恰好处理剩余 0..3 列；最后一次 ++ik 到 kw 可表示。没有 paired2 余数层。

shared 的 t 从 6 到 kh−1，ka..kg 继续对应 t,t−1,…,t−6。每次 t 和每个横向 tile 都重新从 ik=0 开始。正常完整块条件 ow−i>=3L 不变，column<=kw−1，因此最后向量的最右读取在数学上满足 `i+column+3L−1 <= ow+kw−2 = inputWidth−1`。这里只证明改动没有扩展读取范围；有效缓冲区、原指针/尺寸契约仍为前提，不声称测试过极大尺寸或非法 direct 参数。

后续必须为本版建立独立数值和真实汇编证据。父 T 的 37128 PASS 与旧机器码只属于 C52，原核心 kw=1/2/3 会完全跳过 C54 四列主循环，不能充当本版主循环覆盖。以下是必要的边界设计输入，尚未固定新矩阵计数或创建 harness：

| kw | 四列主循环次数 | u1 余数列数 | 意义 |
|---:|---:|---:|---|
| 1/2/3 | 0 | 1/2/3 | 跳过主循环的连续余数 |
| 4 | 1 | 0 | 新的最小完整轮，不读第 5 列 |
| 5 | 1 | 1 | 新的完整轮→余数转场 |
| 6/7 | 1 | 2/3 | 完整轮后两个/三个连续余数 |
| 8/9 | 2 | 0/1 | 主循环回边及之后余数 |
| 15/81 | 3/20 | 3/1 | 较长循环的尾部边界 |

kw=4、5 应至少明确配对 kh=7/8、oh>=7、ow>=3L，让新增主循环确实运行；同时保留 3L/6L±1、余输出行 1..6、多个 tile 和多组输出、VL 字节16/32/64×线程1/4。kw=6 能直接覆盖 main 后两次 u1，值得补入实际矩阵。kh<7 的 direct helper 防御只验证原 quad+triple 路径，不能记作 shared4 覆盖。沿用逐位独立参考、只读输入/权重、guard/canary、全部退出和入口证据；非法维度或禁用 SVE 的覆盖须以实际新矩阵为准，不从已有 SVE PASS 推断。

新生产汇编仍需完整审查所有 helper/clone、十三个语义阶段、四列 main 和 u1 余数实际范围、转场、21 stores、fallback/dispatch、FMA、ABI 和 Z/Q/谓词及间接栈访问。源码 84/84 不是实际指令计数或动态执行次数；若编译器拆分/合并循环，须先解释真实 lowering，不能套父 T 的 paired2 PC、123 指令或 720B 栈帧。主循环回边/地址成本可能被分摊，代码体积和寄存器压力也可能上升，速度目前未知。

作者保留的两次 preflight 失败分别是查不存在的 T schema 字段，以及把生产清单与诊断清单作全字典比较；原 traceback 和成功 new/prepare 的 exit0 证据均已阅读。这些发生于 new 前的来源/元数据准备错误，不是 C54 算子 FAIL，也没有资格变成 C54 PASS。本审查未修改这些原件。

Y/C52 与 S/C51 的失败确认保持原结论，C6 仍为最佳。C54 是四列新源码假设；本报告没有重测失败确认、删除慢样本、混合轮次、创建包或晋级。审查期间使用率实读低于 40% 停止线，未使用重置卡。完成后 STOP，下一步仅由 root 决定。
