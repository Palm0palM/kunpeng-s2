# C8 publication preparation — not executed

Prepared `sep13ba-prepare-publication.py` and `sep13ba-publication.from-al.patch`. Read the full generated publisher, original AL publisher, current public README/SUBMISSIONS and actual root C8 metadata. Python syntax was parsed as text only. No publisher import, export, public checkout mutation, commit or push occurred.

The script requires root-finalized **C8 = C65-r1**, source C65-row7balanced SHA `a855c14b81c00f3d398ac36c5ece6726e15235f18ebf3fe4746570da25a7f874`, promoted record/best/lineage consistency, original AY1591187 and AZ1591241 complete campaigns, and both unchanged `e.comparison` results. It checks each round's original 36 samples independently, same group/order/machine/settings/source, successful scheduler/wrapper exits and parsed samples/medians. AZ must actually have confirmation_passed=true. It does not run promotion.

Final values come from actual records: AZ controls/candidate **437.65 / 431.87 / 437.55 ms**, and original package **1591477**, 12 PASS/error zero, **431.95 ms**. The publisher fixes the real package job identity and derives all times, gains, per-case regressions, sample tables and thresholds from records. It never uses initial screening's 432.20 ms as C8 confirmation speed. A lightweight display-value assertion initially used exact equality for 431.95 and failed because its raw sum is 431.95000000000005; the check was corrected to the intended two-decimal rendering. The publisher never used that literal equality and retains raw source values.

Before public writes it validates the exact package SHA against manifest, submitted package verification and root outputs, compute `PACKAGE_SHA256_VERIFIED` evidence, four member hashes/bytes and executable runner. Published `conv/result/C8/conv.zip` is copied from the original C65-package bytes, never recompressed. Root best ZIP must be those same bytes. The package run is validation evidence, not a substitute for independent confirmation.

The standard sanitized exporter targets the unique `exports/conv-sep13az-ba-final` and exactly four records: C58-r16, C65-r1, C58-r17, C65-package. All AZ/BA scripts, original outer command/output/exit text, bound/unbound template, comparison, package/finalization evidence and record-local logs are retained. Required actual labels include AZ root-submit/root-record-compare and BA root-submit/root-fetch/root-record/root-finalize. Public docs/manifests use independent SEP13AZ_BA names.

Current source, best metadata/ZIP/checksum and lineage are copied only after root finalization. C8 README, package member manifest, metadata/feedback fields and SHA256SUMS use real metadata; top README, SUBMISSIONS and conv/result/README point to C8. Old C7 result directory is removed so there are exactly three current result ZIPs, while the historical C7 confirmation and round reports are byte-protected and linked. Historical earlier notices are marked as historical; current submission links use C8.

Protected paths forbid any ZGEMM/TRSM/tools/config/tests mutation. Only the CONV kernel, C7 removal/C8 result files/current CONV index and explicit CONV best outputs may change in those trees. Non-CONV best entries and Z1/T19 original ZIP bytes remain unchanged. Mature exporter redaction plus final private-data scan remain in place. The clean public checkout is required; README/SUBMISSIONS replacement patterns were checked against its actual current text without modifying it.

Root must review and execute this prepared publisher, then review its exact resulting paths before commit/push. This document reports preparation only.
