# BA finalization template static review

Prepared by `/root/c61_review_resume`, 2026-09-13. Adaptation source is `sep13al-finalize.py`; full changes are in `sep13ba-finalize.from-al.patch`. Finalizer SHA256: `b2f0176decde178a54a626c7d31c5bd1a77f472a89a0ac20be39ec1ce41b5d27`. Only text creation/AST parsing and source/schema inspection were performed. The finalizer was neither imported nor executed; no package created, no raw best/source/lineage altered, and no network, compilation, operator or promotion performed.

**Prepared for root review; not an execution or promotion authorization.** At preparation time the observed AZ campaign was `running`, so BA preflight intentionally rejects finalization until the original AZ campaign becomes complete and passes both original comparisons. The independent package must additionally have its original successful job and all twelve PASS samples. No hypothetical timing or package job ID is inserted.

- Identity: imports BA package contract, whose constants are SOURCE=C65-row7balanced, CONFIRMATION=C65-r1 and VERSION=C65-package. Current state must remain C7/C58-r1; new metadata and lineage are C8 with parent C7. Closing/opening controls are C58-r17/C58-r16. AX1591086 (80100 cases, threads 1/4/38, SVE 128/256/512), AY1591187 and AZ1591241 associations are checked through the BA contract.
- Campaign schema: BA preflight reads sep13ay-campaign.json and sep13az-campaign.json, requires status=`complete`, fixed 36-case order and original per-record `cases`, recomputes actual comparison dictionaries, checks all source/settings/machine/fetched-artifact associations, and requires AZ confirmation_passed. The finalizer does not depend on the nonexistent `comparison.total_gain_pct`; metadata gain is `(closing_total-confirmation_total)/closing_total*100`, using verified totals. Initial and independent confirmation samples remain separate.
- Exact archive gate: all six package verification checks must be true, three suites/four cases/max error zero; parser output must equal recorded original samples and medians. The original fetched artifact hashes and machine profile are recomputed. Scheduler and wrapper exits must be zero, package job must be distinct from AX/AY/AZ, package and cluster manifests must agree, and the archive SHA256 must have exactly one successful compute extraction marker in original wrapper output. All four ZIP member bytes must equal measured sources and run.sh must retain execute permission. The package record is verification-only and never promoted.
- Apply guard: without `--apply`, only verified evidence summary is printed. `--apply` rechecks evidence under the standard experiment lock and checks unchanged lineage. A new `sep13ba-before-promotion` directory receives the original five AL backup files; existing backup causes failure rather than overwrite. Standard `e.promote` first installs the verified same-source parentless closing control, then the independently confirmed candidate whose parent is that control. The existing standard eligibility gate is retained. Other problems' best IDs and source hashes are checked unchanged.
- Output: copies the original verified ZIP bytes, writes C8 metadata/SHA/lineage and checks final package/source equality. It creates no replacement archive and performs no remote or Git operations. GitHub publication remains root-owned. The inherited two-step promotion is not an atomic rollback transaction: if a later step raises, root must reconcile the preserved backup and actual state instead of blindly rerunning.

Static validation: AST parse passed; no stale AL contract, C26 controls, old diagnostic/initial job IDs or total_gain_pct references remain; exactly two standard promote calls and no remote/subprocess calls are present. This is a text-level review, not a runtime dry-run. Root must independently inspect the template and run it only against real completed AZ plus original exact-package evidence.
