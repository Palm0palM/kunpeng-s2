# BA exact-package submission template — prepared only

Prepared `sep13ba-submit-package.py` (108 lines) and `sep13ba-submit-package.from-al.patch`. Read the complete original AL submitter, generic `sep12-submit-package.py`, final BA text and actual AZ campaign schema. Syntax was checked with `ast.parse` on text only. No script import/execution, local operator work, package creation, experiment creation, network or job submission occurred. `C65-package` run and record are still absent.

BA is fixed to source **C65-row7balanced**, confirmation **C65-r1**, package experiment **C65-package**. Preconditions are real AY **1591187** and independent AZ **1591241**, both complete with their own original 36 samples and both control comparisons eligible. AZ must additionally have `confirmation_passed=true`. Each round's original records, group/order, same settings/machine, fetched artifact hashes, raw sample parsing/medians, scheduler and wrapper exits are checked. Both `e.comparison` decisions are recomputed and must exactly equal stored record/campaign decisions. No AY/AZ samples are pooled and no historical recursive hash chain is used.

Accepted own AX **1591086 / 80100 cases** and source SHA `a855c14b81c00f3d398ac36c5ece6726e15235f18ebf3fe4746570da25a7f874` remain required. AY, AZ and package source must match byte-for-byte, including official runner/benchmark/README; companion files remain current C7 bytes. Current best must still be confirmed C7/C58-r1 in both registry and actual source. The tool does not promote before package validation.

The two obsolete AL reservation checks are removed. The only live prerequisite gate is the actual original AZ job's successful terminal state with both exits zero; failed/uncertain status saves evidence and aborts. The package identity is checked before the gate and again under the experiment lock, then standard `new(parent=C65-r1)` reserves the unique run before any package upload. Original creation metadata is saved, extended package-only metadata is checkpointed, and source identity is checked again. Existing run/record/ZIP/cluster manifests prevent retry or replacement.

The unchanged generic submitter builds one ZIP from the immutable snapshot and uploads those exact bytes with a SHA-256 manifest. Its compute wrapper validates the ZIP hash, entry set and each source hash, extracts into the compute source directory and calls the standard measurement wrapper. Settings stay identical to C7/AY/AZ: three full suites, 38 CPUs, 24576 MiB, one packed NUMA, 1800 seconds, generic strict GCC runner. Thus the expected package result is 12 official cases, but BA only submits and reports **validation_pending**; it does not infer PASS from submission. Root must later fetch and verify the original package job's actual logs/hash/12 cases before delivering those same ZIP bytes.

The generic submitter's argv/stdout/stderr/exit and package association are retained; uncertain submission stops with the original reservation. No automatic retry, promotion or public ZIP placement occurs. Intended root invocation after actual AZ success and review: `python3 .runs/conv/sep13ba-submit-package.py --go`, wrapped by the existing outer command logger. It has not been run.
