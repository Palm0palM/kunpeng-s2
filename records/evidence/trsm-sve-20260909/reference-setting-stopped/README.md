# TRSM SVE 同源码重试

首次作业1492036仅因串行软件long-double已知解构造过慢，由本任务在官方计时前停止。完整日志、调度状态和原因保留在前组目录；零官方计时结果，不能报告任何正确性或速度判定。

本次成员 T1-control5、T3-svepanel-r2、T3-sveupdate-r2 与前组对应源码相同；改动只在独立预检查：参考数据用整数除以2^14，点积的公共分母为2^28，最大部分和整数分子小于2^38，故double可精确构造已知解；10个尺寸、4个no-op、padding/L不变性检查和1e-12阈值未变。代码注释提供上界证明。实际SVE入口计数、直接微核FMA对照、分配失败检查仍保留。

修复预检查日志实时写入与flush，参考探针使用同步日志。三轮官方套件与源码、runner、参考库、编译配置、线程/NUMA要求未变。所有编译和测试只在超算运行，本机不执行计算测试。不额外计算独立hash清单，工具原有完整性检查保留。

第二次预检查作业1492058的实际SVE与回退测试全部通过；driver在计时前发现新建基线继承配置TEST_RUNS不等于3而拒绝开始，因此没有官方成绩。本组显式固定三成员settings.environment.TEST_RUNS=3，源码与通过预检查的候选完全相同。
