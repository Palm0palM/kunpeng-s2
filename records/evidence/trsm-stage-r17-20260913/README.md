# TRSM r17 evidence

This redacted public derivative preserves the fresh T19-control13 baseline and
the subsequent same-allocation T19/T20 comparison. Each cohort retains its own
actual job, source snapshots, commands, official/warmup logs and results.
Current preparation and submitted-payload snapshots are stored separately.

The repeat's full prior refers to baseline job 1581760 and the initial promoted
record under records/baseline-initial. Historical samples are never pooled with
the comparison. T20 changes only the SVE 4x32 update k loop; unchanged small-path
validation is explicitly historical (job 1579861), not a fresh preflight claim.
Both comparison members run the complete CT64 wide32 direct/whole/argument tests.

The archive only copies existing evidence; its creation does not validate tests,
promote a candidate or submit a contest entry. Results and failures remain as
observed. No local task compilation/execution or hash operations. Actual reference
is KML25.1/GCC12, not the specified KML25.2 revalidation. Private originals remain
available locally, and this destination must never be reused in place.
