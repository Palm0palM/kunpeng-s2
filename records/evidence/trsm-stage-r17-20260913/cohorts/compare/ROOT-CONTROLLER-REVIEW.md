# r17 比较控制器最终审查

T20源码静态审查已通过。root核对三份脚本和计划：仅两个成员，A为当前T19-control13原样repeat、prior精确1581760；B为T20新候选，parent=T19-control13，排他建立准备记录。继承r16的原策略/来源/created/history保留和五源码冻结末尾检查。元数据/record已存在或当前最佳改变均拒绝prepare。

预热AB后正式AB/BA/AB，原TEST_RUNS3，18正式/6预热，全部样本保留。两成员均先完成原CT64 wide32完整28micro/28whole/56arguments/7process检查。小路径源码和分派不变，明确只引用T19先前小路径验证，不伪造新615结果。

driver的计算节点、线程、NUMA、实际job、KML/gomp和正式runner约束保持。collect只收成功同job，finish一次排他，在任何record前核对全部原始正式/预热日志、源码、依赖和两份wide预检。只有A使用repeat-existing，唯一比较为T19-control13对T20，不自动promote。未知提交不重提，已知job marker可单独恢复。

所有Python与内嵌Python AST、bash -n通过；本机仅解析文本，没有题目执行或哈希操作。解除prepare阻断，可以只提交一次。
