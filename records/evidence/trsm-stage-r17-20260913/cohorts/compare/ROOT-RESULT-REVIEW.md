# r17 结果复核

作业 1581862：调度器 SUCCEEDED，job/system/cohort 退出码均为 0，实际运行 140 秒。两个成员在 COMPUTE_NODE、NUMA 0、CPU 0–37 的同一分配内执行；正式顺序 AB / BA / AB，各一套独立预热先行。实际 GCC 12.3.1 与 KML 25.1.0，正式和预热链接证据均使用真实 KML 库及 GCC 12 libgomp。不是指定 KML 25.2.0 复验。

登记前查看原始日志；无哈希 helper 对原始正式日志、资源、环境、退出、顺序和链接进行校验。finish_records.py 仅执行一次，18 条正式结果与 6 条预热结果全部 PASS，最大误差 1.11e-15。所有三轮样本保留；初始基线作业 1581760 的样本不加入本轮比较。repeat 的完整 prior-record 与事先保存的 baseline-record 逐字节相同。

|成员|小例三轮 ms|中例三轮 ms|大例三轮 ms|中位数合计 ms|
|---|---|---|---|---:|
|T19-control13|13.68 / 14.14 / 14.34|117.80 / 117.25 / 118.73|179.82 / 180.17 / 180.08|312.02|
|T20-wideunroll2|14.26 / 13.67 / 14.12|119.93 / 119.80 / 119.07|174.79 / 172.72 / 173.42|307.34|

T20 小/中/大例耗时变化分别为快 0.1414%、慢 1.6978%、快 3.6984%；合计快 1.4999%，未超过 4.6676% 波动门槛，中例也违反最多退步 1% 的门槛。**不晋级。** 当前源码仍逐字节等同 T19；best 测量 ID 保持 T19-control13，现有 T19 ZIP 不变。

两成员各完成 CT64/KB256 宽核预检：28 个直接微核、28 个整解用例、56 个参数检查用例、463420 次参数观察且 0 不匹配，7 个测试进程全部成功。包含 count=0/1/2/7/31/255/256、padding、已打包/跨行输入、分配失败、禁用 SVE、实际窄向量长度、KB 尾项。远程 run.sh 在完成标记前校验了逐步退出和原始结果；本机另查看 summary.tsv、summary.json 和完成标记。此次没有重跑未修改的小路径全量预检，其完整验证来自 T19 作业 1579861；不把历史计数标为新测试。没有运行本机题目或 sanitizer。

## 目标汇编观察

读取此作业计算节点生成、未插桩的 trsm-wide4x32.s。T19 update4x32_sve 的 .L25 循环每 k 为 34 条指令、16 FMLA；T20 每两个 k 为 78 条指令，26 FMLA + 6 FMAD，另有 8 MOVPRFX。两个热循环均没有 [sp] 访问；T20 增加函数外层 ABI 寄存器保存，不能误记为热循环溢出。展开保持每输出 k 累计顺序，但编译器增加寄存器搬移，因此并非简单减半循环管理开销。性能收益仍以官方测量为准，不据静态指令数推断带宽或因果结论。

下一步优先用相同 T20 源码再做独立、同分配三轮比较，确认大例收益和中例变化；不合并样本、不降低晋级门槛。若继续保留大量搬移，可另立候选控制两步之间的寄存器活跃范围。
