# T25 paired historical coefficient loads

Prepared from T19-control13/source, latest prior job1582526. Hypothesis: T24 consumed8extraADD for8LD1RD per k, preserving37instructions. Two adjacent coefficients can share one base using offsets0/+8, amortizing address calculation. This candidate does not simply reuse regressed T23: it processes each row k then k+1 with paired coefficient loads and four X vectors, preserving each original16accumulator chain. Remaining single-step tail uses original T19 body. Entire source outside history loop and support files unchanged.

Each two-instruction asm has two SVE outputs, existing all-active predicate, one GPR address and two explicit scalar-double memory inputs. It reads exactly coefficients[0] and[1], no modified inputs/flags/hiddenmemory, no blanket memory clobber or volatile. Output vector class cannot overlap input predicate/address classes; outputs remain live and distinct as consumed in C. Each row's two accumulators receive k then k+1, no split reduction. Index condition k+1<start protects second load; initialk0/evenincrement2 does not overflow for representable positive int start, final original tail handles0/1/odd. Real kernel starts are nonnegative.

Primary addressing evidence: Arm search result for its A64 ISA describes LD1RD immediate offsets as multiples8 in0..504; direct web PDF fetch failed due size, not claimed fully read. Opened/read GNU binutils maintainer test patch https://sourceware.org/pipermail/binutils/2016-September/095059.html lines281–293: offset range0..504,multiple8,noVLscaling/writeback. Offset+8 is selected. GCC asm memory/register constraints and explicit pointer+memory inputs follow https://gcc.gnu.org/onlinedocs/gcc-12.3.0/gcc/Extended-Asm.html and https://gcc.gnu.org/onlinedocs/gcc/Machine-Constraints.html (already read forT24). ActualGCC12compile/assembly verification remains required on allocated compute.

Potential costs: more live X/coefficient vectors, MOVPRFX, spills, larger ABI save or loop/tail growth. No speed claim or local compilation/testing/hash operations.
