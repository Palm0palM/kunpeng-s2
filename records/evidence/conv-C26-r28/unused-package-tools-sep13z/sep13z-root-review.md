# Root Z source/package workflow review

Root read the complete final sep13z-submit-package.py and README after the author stopped. Only main --go can create package version or call transport; imported W definitions have guarded main, and U is never imported/executed. Default preflight is read-only and requires original Y1582410 complete/confirmationtrue before record verification, then fixed W1582256 original48 and Y original36, separately parsed and checked with their own same-group C6 controls. Four strict gates retain every case/sample and max(1%,all compared spreads), individualcase regression<=1%; no W/Y mixing, retry or reference qualification.

C52-package uses the unchanged C52-r1 source8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7 and original T1582134 frozen37128/13semantic14region evidence. Source origin, diagnostic source and measured parent are distinct explicit fields. Current source, outputs and best must still be C6/C26-r1, standard runner/benchmark/settings unchanged. Sfalse/C51 reference restrictions remain read-only.

Narrow serial gate requires known W/T/S/X1582372/Y1582410 plus only existing P/Z/member reservations; missing known IDs, unknown reservations, query failure or nonterminal state block. OriginalW/T/S/Y must be succeeded0. Shared lock revalidates context before a standard new/checkpoint package-only record. No Z campaign is made; package run/record then exclusive transport cluster.json form the reservation. Any existing run/record, or uncertain upload/submission, is retained and never repeated.

The previously reviewed sep12-submit-package.py is invoked exactly once as subprocess and its full argv/stdout/stderr/exit retained. It generates one original ZIP for remote verification and extraction inside the scheduler allocation only. No local operator, promotion, publication, official submission or post-verification recompression. Successful submit only means pending validation; final actual12PASS/all exits/source/archive checks and root decision still required.

No static blocker found. This note does not assert Y confirmation passed or a ZIP/job exists. Root may grant one Z GO only after actual Ytrue and fresh account usedPercent<40; never consume reset credits.

## Finalizer full-source review

Root then read the entire final sep13z-finalize.py and its README. Default preflight reads actual Z contract plus raw original ZIP evidence only; --apply rechecks everything under the standard workflow lock before two standard promotions (C26-r29 unchanged current baseline, then confirmed C52-r1). C7 must not already exist. Package record remains reference-only and identical, other best problem keys are retained, W/S/C51 history remains unchanged. Original validated ZIP is copied without recompression; source, metadata and lineage describe separate W initial/Y confirmation/T diagnostic/package evidence. Final package source has four exact regular members with executable runner, original transport marker, all12 PASS/zeroerror and distinct succeeded job/system/wrapper exits. No local operator, network, Git, public-tree write or newjob is performed by finalizer.

No static blocker found. No preflight or apply has executed; real Y result and independent ZIP are still prerequisites, with40%-used stop policy.
