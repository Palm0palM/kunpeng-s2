--- .runs/conv/sep13ac-checks/driver.py
+++ .runs/conv/sep13ae-checks/driver.py
@@ -1,4 +1,4 @@
-"""Prepared AC diagnostic transport for C55; explicit root --go only.
+"""Prepared AE diagnostic transport for C56; explicit root --go only.
 
 No work occurs on import. status/fetch reuse a saved unique job; no local
 operator execution, automatic polling, acceptance, promotion or publication.
@@ -21,11 +21,13 @@
 sys.path.insert(0, str(ROOT / 'tools'))
 import cluster
 
-VERSIONS = ('C55-row7x3shared3',)
-SOURCE_SHA256 = 'cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138'
+VERSIONS = ('C56-row7shared3fence',)
+SOURCE_SHA256 = '6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c'
 SOURCE_NAMES = {'conv2d.c', 'check_conv_guard.c', 'check_sve_dispatch.c', 'candidate.env', 'remote_job.sh'}
-SOURCE_PARENTS = {'C55-row7x3shared3': 'C52-row7x3shared2'}
-COUNTS = {'C55-row7x3shared3': dict(full_per_configuration=5744, dispatch_per_configuration=1212,
+SOURCE_PARENTS = {'C56-row7shared3fence': 'C55-row7x3shared3'}
+FENCE_CONTRACT = dict(source_fences_per_main_iteration=3, input_accumulators_per_fence=21,
+    output_operands=0, memory_clobber=True, empty_template=True)
+COUNTS = {'C56-row7shared3fence': dict(full_per_configuration=5744, dispatch_per_configuration=1212,
     direct_per_configuration=432, configurations=6, total=44328)}
 RESOURCES = dict(cpus=38, memory_mb=24576, numa_count=1,
     numa_distribution='pack', walltime_seconds=1800)
@@ -35,7 +37,7 @@
     + [f'dispatch-vl{vl}-t{threads}' for vl in (16, 32, 64) for threads in (1, 4)]
     + ['build-assembly', 'complete'])
 TERMINAL = {'SUCCEEDED', 'FAILED', 'CANCELLED', 'CANCELED', 'TIMEOUT', 'TERMINATED'}
-# The frozen AC wrapper builds no retained production object for separate transfer.
+# The frozen AE wrapper builds no retained production object for separate transfer.
 OBJECT_ARTIFACTS = {name: set() for name in VERSIONS}
 
 
@@ -78,61 +80,58 @@
 
 
 def serial_gate(cfg):
-    """Bounded fixed AA/Y/X/T and present AB/P/AC jobs; no teammate scan.
-
-    Prepared P/AC packages without job.json do not represent active jobs.
-    Any actual unresolved reservation blocks. Only root is a compute submitter;
-    this check is not a distributed scheduling lock and never cancels work.
+    """Only fixed AD/AC/AB/AA/Y/X/T plus present P/AE reservations.
+
+    Prepared P/AE without job.json is not active. Unknown IDs/reservations,
+    missing fixed evidence, query failure or nonterminal state block. This is
+    not a distributed lock; root remains the sole submitter and never cancels.
     """
     identities = {}
     unresolved = []
     ignored_prepared = []
-    job_paths = [ROOT / '.runs/conv/sep13p-checks/C50-row4dupfencenomem/job.json',
-                 BASE / 'C55-row7x3shared3/job.json']
-    for path in job_paths:
+    for path in (ROOT / '.runs/conv/sep13p-checks/C50-row4dupfencenomem/job.json',
+                 BASE / 'C56-row7shared3fence/job.json'):
         origin = str(path.relative_to(ROOT))
         if not path.exists():
             ignored_prepared.append(origin)
             continue
-        data = read_json(path)
-        job_id = data.get('job_id')
-        if not re.fullmatch(r'[0-9]+', str(job_id or '')):
-            unresolved.append(dict(path=origin, reason='Actual package reservation lacks reconciled job ID'))
+        job_id = str(read_json(path).get('job_id') or '')
+        if not re.fullmatch(r'[0-9]+', job_id):
+            unresolved.append(dict(path=origin, reason='Actual reservation lacks reconciled job ID'))
             continue
-        identities.setdefault(str(job_id), []).append(origin)
-    required = [('.runs/conv/sep13aa-checks/C54-row7x3shared4/job.json', 'job_id', '1582656'),
+        identities.setdefault(job_id, []).append(origin)
+    required = [('.runs/conv/sep13ad-campaign.json', 'performance_job', '1582956'),
+                ('.runs/conv/sep13ac-checks/C55-row7x3shared3/job.json', 'job_id', '1582860'),
+                ('.runs/conv/sep13ab-campaign.json', 'performance_job', '1582814'),
+                ('.runs/conv/sep13aa-checks/C54-row7x3shared4/job.json', 'job_id', '1582656'),
                 ('.runs/conv/sep13y-campaign.json', 'performance_job', '1582410'),
-                ('.runs/conv/sep13t-checks/C52-row7x3shared2/job.json', 'job_id', '1582134'),
-                ('.runs/conv/sep13x-checks/C53-row7cursors/job.json', 'job_id', '1582372')]
+                ('.runs/conv/sep13x-checks/C53-row7cursors/job.json', 'job_id', '1582372'),
+                ('.runs/conv/sep13t-checks/C52-row7x3shared2/job.json', 'job_id', '1582134')]
     for origin, field, expected_id in required:
         path = ROOT / origin
         if not path.exists():
-            unresolved.append(dict(path=origin, reason='Required fixed AA/Y/X/T evidence is missing'))
+            unresolved.append(dict(path=origin, reason='Required fixed evidence is missing'))
             continue
-        job_id = read_json(path).get(field)
-        if str(job_id) != expected_id:
+        job_id = str(read_json(path).get(field) or '')
+        if job_id != expected_id:
             unresolved.append(dict(path=origin, reason='Required job differs from fixed ID ' + expected_id))
             continue
-        identities.setdefault(str(job_id), []).append(origin)
-    # A campaign is reserved before its member cluster manifests exist. Observe
-    # only these explicit AB paths; absent files are not active reservations.
-    ab_paths = [(ROOT / '.runs/conv/sep13ab-campaign.json', 'performance_job')]
-    ab_paths += [(ROOT / '.runs/conv' / name / 'cluster.json', 'job_id')
-                 for name in ('C26-r30', 'C54-row7x3shared4', 'C52-r2', 'C26-r31')]
-    ab_ids = set()
-    for path, field in ab_paths:
-        if not path.exists():
-            continue
-        origin = str(path.relative_to(ROOT))
-        job_id = str(read_json(path).get(field) or '')
-        if not re.fullmatch(r'[0-9]+', job_id):
-            unresolved.append(dict(path=origin, reason='Actual AB reservation lacks reconciled job ID'))
-            continue
-        ab_ids.add(job_id)
         identities.setdefault(job_id, []).append(origin)
-    if len(ab_ids) > 1:
-        unresolved.append(dict(path='.runs/conv/sep13ab-campaign.json',
-            reason='AB campaign/member job IDs disagree; reconcile original submission'))
+    # Only the explicitly named performance group members; never scan peers.
+    for expected_id, members in (
+            ('1582956', ('C26-r32', 'C55-row7x3shared3', 'C52-r3', 'C26-r33')),
+            ('1582814', ('C26-r30', 'C54-row7x3shared4', 'C52-r2', 'C26-r31'))):
+        for version in members:
+            path = ROOT / '.runs/conv' / version / 'cluster.json'
+            origin = str(path.relative_to(ROOT))
+            if not path.exists():
+                unresolved.append(dict(path=origin, reason='Required fixed member manifest is missing'))
+                continue
+            job_id = str(read_json(path).get('job_id') or '')
+            if job_id != expected_id:
+                unresolved.append(dict(path=origin, reason='Member reservation differs from original fixed group ID'))
+                continue
+            identities.setdefault(job_id, []).append(origin)
     evidence = [query_scheduler(cfg, job_id, origins) for job_id, origins in identities.items()]
     clear = not unresolved and all(
         row['query_exit'] == 0 and row['scheduler'] is not None
@@ -148,7 +147,7 @@
 def frozen_source(base, version):
     expected = read_json(base / 'source-hashes.json')
     require(set(expected) == SOURCE_NAMES, 'Unexpected frozen transport source set')
-    require(expected['conv2d.c'] == SOURCE_SHA256, 'AC requires its own root-reviewed C55 source')
+    require(expected['conv2d.c'] == SOURCE_SHA256, 'AE requires its own root-reviewed C56 source')
     source = base / 'source'
     require(source.is_dir() and not source.is_symlink(), 'Source must be a real directory')
     actual = {}
@@ -161,11 +160,14 @@
     require(actual == expected, 'Prepared source changed; never silently refresh its frozen manifest')
     extra_manifest = base / 'source-manifest.json'
     require(extra_manifest.is_file() and not extra_manifest.is_symlink(),
-        'Frozen AC bytes/SHA source-manifest.json is required')
+        'Frozen AE bytes/SHA source-manifest.json is required')
     require(read_json(extra_manifest) == {name: dict(bytes=sizes[name], sha256=actual[name]) for name in actual},
         'Supplementary bytes/SHA manifest mismatch')
     record = read_json(ROOT / 'records/experiments/conv' / (version + '.json'))
-    require(record.get('version') == version and record.get('source_parent') == SOURCE_PARENTS[version],
+    # Standard new/checkpoint has parent; later performance records explicitly
+    # preserve source_parent when comparison parent is changed. Never rewrite either.
+    require(record.get('version') == version
+        and record.get('source_parent', record.get('parent')) == SOURCE_PARENTS[version],
         'Candidate checkpoint identity mismatch')
     require(record['source_hashes']['conv2d.c'] == expected['conv2d.c'], 'Candidate checkpoint/source mismatch')
     candidate_source = ROOT / '.runs/conv' / version / 'source/conv2d.c'
@@ -195,9 +197,9 @@
     require(prepared.get('resources_required') == {key: value for key, value in RESOURCES.items() if key != 'walltime_seconds'},
         'Prepared resource expectations mismatch')
     require(prepared.get('expected_acc') == 3 and prepared.get('expected_checks') == counts,
-        'AC matrix schema mismatch')
+        'AE matrix schema mismatch')
     require(prepared.get('rows_per_group') == 7 and prepared.get('vectors_per_row') == 3
-        and prepared.get('accumulators') == 21, 'C55 seven-row/three-vector shape mismatch')
+        and prepared.get('accumulators') == 21, 'C56 seven-row/three-vector shape mismatch')
     require(prepared.get('expected_rowseven_entries') == dict(dispatch_per_configuration=1236, direct_per_configuration=1080),
         'Rowseven entry expectations mismatch')
     require(prepared.get('full_matrix_counts') == dict(core=3888, narrow=144, small=720, larger=32, quad_boundary=960)
@@ -207,9 +209,11 @@
         and prepared.get('shared_kernel_columns_per_iteration') == 3
         and prepared.get('shared_remainder_source_columns_per_iteration') == 1
         and prepared.get('shared_remainder_max_columns') == 2
-        and prepared.get('assembly_review_schema') == 'ac-shared3-paths-v1'
+        and prepared.get('assembly_review_schema') == 'ae-shared3-fence-paths-v1'
         and prepared.get('other_stage_kernel_columns_per_iteration') == 1,
-        'C55 shared triple/u1-remainder expectations mismatch')
+        'C56 shared triple/u1-remainder expectations mismatch')
+    require(prepared.get('column_fence_contract') == FENCE_CONTRACT,
+        'Prepared three-column input-only compiler fence contract mismatch')
     return expected
 
 
@@ -228,7 +232,7 @@
     parser.add_argument('version', choices=VERSIONS)
     parser.add_argument('config', help='Verified existing config; read only, never included in source or archive')
     parser.add_argument('action', choices=('gate', 'submit', 'status', 'fetch'))
-    parser.add_argument('--go', action='store_true', help='Explicit one-shot root AC submission authorization')
+    parser.add_argument('--go', action='store_true', help='Explicit one-shot root AE submission authorization')
     args = parser.parse_args()
     require(args.action == 'submit' or not args.go, '--go is valid only for submit')
     base = BASE / args.version
@@ -250,7 +254,7 @@
         require(gate['clear'], 'Known CONV work active/unresolved or query incomplete; retain gate evidence and stop')
         hashes = frozen_source(base, args.version)
         created = stamp()
-        remote_dir = cfg['remote_root'] + '/diagnostics/conv-sep13ac-' + args.version + '-' + created
+        remote_dir = cfg['remote_root'] + '/diagnostics/conv-sep13ae-' + args.version + '-' + created
         data = dict(version=args.version, remote_dir=remote_dir, submitted=False,
             submit_attempted=False, created_at=created, resources=RESOURCES,
             expected_checks=COUNTS[args.version], expected_stages=STAGES,
@@ -266,7 +270,7 @@
         command = 'umask 077; mkdir -p ' + shlex.quote(remote_dir) + ' && tar -xzf - -C ' + shlex.quote(remote_dir)
         result = logged_remote(cfg, command, base / 'upload.log', input=archive.getvalue(), timeout=cfg['transfer_timeout'])
         require(result.returncode == 0, 'Upload failed; keep reservation/log and do not resubmit')
-        command = cluster.submit_command(cfg, remote_dir, 'conv-sep13ac-' + args.version)
+        command = cluster.submit_command(cfg, remote_dir, 'conv-sep13ae-' + args.version)
         data.update(command=command, submit_attempted=True)
         save(manifest, data)
         result = logged_remote(cfg, command, base / 'submit.log')
@@ -330,7 +334,7 @@
             path = dest / name
             if not path.exists():
                 path.write_bytes(content)
-    print('Fetched immutable AC diagnostic text/assembly. Fetch is not acceptance or performance.')
+    print('Fetched immutable AE diagnostic text/assembly. Fetch is not acceptance or performance.')
 
 
 if __name__ == '__main__':
--- .runs/conv/sep13ac-checks/accept_returned.py
+++ .runs/conv/sep13ae-checks/accept_returned.py
@@ -1,4 +1,4 @@
-"""Accept AC returned text only. No SSH, compilation, tests or operator execution.
+"""Accept AE returned text only. No SSH, compilation, tests or operator execution.
 
 Usage: accept_returned.py VERSION [--failed]. Source/raw never change. An existing
 prepared validation is preserved verbatim before a real final record replaces it.
@@ -17,9 +17,11 @@
 sys.dont_write_bytecode=True
 BASE=Path(__file__).resolve().parent
 ROOT=BASE.parents[2]
-PLANS={'C55-row7x3shared3':dict(full=5744,dispatch=1212,direct=432,total=44328,vectors=3,rows=7,helper='conv_sve_rowseven',parent='C52-row7x3shared2')}
-SOURCE_SHA256='cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138'
+PLANS={'C56-row7shared3fence':dict(full=5744,dispatch=1212,direct=432,total=44328,vectors=3,rows=7,helper='conv_sve_rowseven',parent='C55-row7x3shared3')}
+SOURCE_SHA256='6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c'
 SOURCE_NAMES={'conv2d.c','check_conv_guard.c','check_sve_dispatch.c','remote_job.sh','candidate.env'}
+FENCE_CONTRACT=dict(source_fences_per_main_iteration=3,input_accumulators_per_fence=21,
+    output_operands=0,memory_clobber=True,empty_template=True)
 FMA=r'^\s*(?:fmla|fmls|fmad|fmsb|fnmla|fnmls|fnmad|fnmsb|fmadd|fmsub|fnmadd|fnmsub|fmlal2?|fmlsl2?|fmmla)\b'
 TERMINAL={'SUCCEEDED','FAILED','CANCELLED','CANCELED','TIMEOUT','TERMINATED'}
 
@@ -42,23 +44,25 @@
     hashes=read(base/'source-hashes.json')
     prepared=read(base/'prepared.json')
     require(set(hashes)==SOURCE_NAMES and job.get('source_hashes')==hashes,'Frozen/job source identity mismatch')
-    require(hashes['conv2d.c']==SOURCE_SHA256,'AC requires its own fixed C55 production source')
+    require(hashes['conv2d.c']==SOURCE_SHA256,'AE requires its own fixed C56 production source')
     require(prepared.get('candidate')==version and prepared.get('source_hashes')==hashes,'Prepared source identity mismatch')
     require(prepared.get('total_cases_planned')==PLANS[version]['total'],'Prepared total mismatch')
     manifest=read(base/'source-manifest.json')
     require({name:item['sha256'] for name,item in manifest.items()}==hashes,'Two original transport manifests disagree')
     require(prepared.get('source_parent')==PLANS[version]['parent'] and prepared.get('rows_per_group')==7
-        and prepared.get('vectors_per_row')==3 and prepared.get('accumulators')==21,'Prepared AC source/shape mismatch')
+        and prepared.get('vectors_per_row')==3 and prepared.get('accumulators')==21,'Prepared AE source/shape mismatch')
     require(prepared.get('expected_semantic_stage_count')==13
         and prepared.get('shared_kernel_columns_per_iteration')==3
         and prepared.get('shared_remainder_source_columns_per_iteration')==1
         and prepared.get('shared_remainder_max_columns')==2
-        and prepared.get('assembly_review_schema')=='ac-shared3-paths-v1'
+        and prepared.get('assembly_review_schema')=='ae-shared3-fence-paths-v1'
         and prepared.get('other_stage_kernel_columns_per_iteration')==1,'Prepared shared triple/u1-remainder contract mismatch')
+    require(prepared.get('column_fence_contract')==FENCE_CONTRACT,'Prepared column fence contract mismatch')
     require(prepared.get('full_matrix_counts')==dict(core=3888,narrow=144,small=720,larger=32,quad_boundary=960)
-        and prepared.get('dispatch_matrix_counts')==dict(core=972,quad_boundary=240), 'Prepared AC family counts mismatch')
+        and prepared.get('dispatch_matrix_counts')==dict(core=972,quad_boundary=240), 'Prepared AE family counts mismatch')
     record=read(ROOT/'records/experiments/conv'/(version+'.json'))
-    require(record.get('version')==version and record.get('source_parent')==PLANS[version]['parent'] and record['source_hashes']['conv2d.c']==hashes['conv2d.c'],'Candidate checkpoint mismatch')
+    # Standard source preparation uses parent; explicit source_parent takes priority later.
+    require(record.get('version')==version and record.get('source_parent',record.get('parent'))==PLANS[version]['parent'] and record['source_hashes']['conv2d.c']==hashes['conv2d.c'],'Candidate checkpoint mismatch')
     path=base/'raw/source-sha256.txt'
     verified=False
     if path.exists():
@@ -150,8 +154,26 @@
     item['derived_counts_per_kernel_column']={key:value/work for key,value in actual.items()}
 
 
+def column_fence_review(stage):
+    """Require actual scheduling interpretation; false tightening is a valid observation."""
+    fence=stage.get('column_fence_review') or {}
+    require(fence.get('source_contract')==FENCE_CONTRACT,'Missing original input-only column fence association')
+    for key in ('actual_schedule_review','cross_column_motion_review','loop_boundary_review','spill_relocation_review'):
+        require(isinstance(fence.get(key),str) and fence[key].strip(),'Missing actual fence interpretation '+key)
+    require(type(fence.get('schedule_tightened')) is bool,'Record actual tightening true or false; never infer success')
+    boundaries=fence.get('boundaries') or []
+    require(isinstance(boundaries,list) and [b.get('column_index') for b in boundaries]==[0,1,2],
+        'Review all three source column boundaries, including next iteration')
+    for boundary in boundaries:
+        require(type(boundary.get('column_index')) is int
+            and isinstance(boundary.get('actual_evidence'),str) and boundary['actual_evidence'].strip()
+            and type(boundary.get('all_accumulators_ready_before_next_column_loads')) is bool,
+            'Missing actual per-column FADD/load ordering evidence; empty asm needs no invented PC')
+
+
 def shared_review(stage,lines,outer,ranges):
     """Read actual disjoint intervals and paths; never assume a fixed block count."""
+    column_fence_review(stage)
     for key in ('source_mapping','control_flow_notes','accumulator_order_review',
                 'triple_to_remainder_review','transition_spill_review','t_and_tile_reset_review'):
         require(bool(stage.get(key)),'Missing shared interpretation '+key)
@@ -218,8 +240,8 @@
     review=read(base/'assembly-review.json')
     require(review.get('candidate')==job['version'] and review.get('job_id')==job['job_id'] and review.get('source_sha256')==source_hash and review.get('compiler_version')=='10.3.1','Actual assembly review identity mismatch')
     require(review.get('assembly_sha256')==hashlib.sha256(content).hexdigest(),'Assembly review has wrong returned artifact')
-    require(review.get('schema')=='ac-shared3-paths-v1','AC requires its own actual multiple-region review schema')
-    for key in ('review_complete','production_uninstrumented','whole_helper_stack_reviewed','all_stages_and_transitions_reviewed','abi_saves_distinguished','dispatch_reviewed'):
+    require(review.get('schema')=='ae-shared3-fence-paths-v1','AE requires its own actual multiple-region review schema')
+    for key in ('review_complete','production_uninstrumented','whole_helper_stack_reviewed','all_stages_and_transitions_reviewed','abi_saves_distinguished','dispatch_reviewed','column_fences_reviewed'):
         require(review.get(key) is True,'Incomplete assembly review '+key)
     fma=len(re.findall(FMA,text,re.M))
     require(fma==review.get('whole_source_fma_count')==0,'Unexpected FMA')
@@ -252,20 +274,20 @@
         require((item['line_start'],item['line_end'])==bounds(lines,item['symbol']),'Dispatch range mismatch')
         require(bool(item.get('control_flow_notes')) and bool(item.get('stack_notes')),'Dispatch/fallback interpretation missing')
     require(review.get('shape')==dict(rows_per_group=7,vectors_per_row=3,accumulators=21),
-        'Missing actual review association with C55 seven-row/three-vector source shape')
+        'Missing actual review association with C56 seven-row/three-vector source shape')
     require(review.get('whole_machine_code_identical_claimed') is False,
-        'T/C52 is source background, not current C55 machine-code proof')
+        'AC/C55 is source background, not current C56 machine-code proof')
     require(review.get('shared_triple_and_remainder_reviewed') is True
         and review.get('shared_unroll')==dict(main_columns=3,remainder_source_columns=1,remainder_max_columns=2,other_stage_columns=1),
-        'Missing independent C55 shared triple/u1-remainder review')
+        'Missing independent C56 shared triple/u1-remainder review')
     comparison=review.get('parent_codegen_comparison') or {}
-    require(comparison.get('candidate')=='C52-row7x3shared2' and comparison.get('job_id')=='1582134'
-        and comparison.get('source_sha256')=='8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7'
+    require(comparison.get('candidate')=='C55-row7x3shared3' and comparison.get('job_id')=='1582860'
+        and comparison.get('source_sha256')=='cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138'
         and bool(comparison.get('shared_work_and_schedule_review'))
-        and bool(comparison.get('stack_and_spill_review')),'Missing actual T parent comparison')
-    parent_asm=ROOT/'.runs/conv/C52-row7x3shared2/sve-correctness-sep13t/raw/conv2d-sve.s'
+        and bool(comparison.get('stack_and_spill_review')),'Missing actual AC parent comparison')
+    parent_asm=ROOT/'.runs/conv/C55-row7x3shared3/sve-correctness-sep13ac/raw/conv2d-sve.s'
     require(comparison.get('assembly_sha256')==hashlib.sha256(parent_asm.read_bytes()).hexdigest(),
-        'Parent comparison must identify actual frozen T assembly')
+        'Parent comparison must identify actual frozen AC assembly')
     review.update(stage_count=len(phase_ids),fma_count=fma,transitions_reviewed=True,
         arithmetic_region_counts={h['symbol']:h['actual_arithmetic_region_count'] for h in helpers},
         production_object_disassembly_available=False,performance_submission_automatic=False)
@@ -306,11 +328,11 @@
             ending=r' convolution cases; readonly input/kernel, guarded allocation edges, poisoned/guarded output, bitwise scalar reference$'
             one(r'^PASS: '+str(p['full'])+ending,guard,'Production full count/PASS missing')
             entries={name:int(one(r'SVE_'+name.upper()+r'_ACTUAL_ENTRIES=(\d+)',dispatch,'Missing legacy entry '+name)) for name in ('prefix','tail','rowpair','rowtriple','rowquad')}
-            one(r'^FULL_MATRIX_COUNTS core=3888 narrow=144 small=720 larger=32 quad_boundary=960$',guard,'AC matrix family mismatch')
-            one(r'^DISPATCH_MATRIX_COUNTS core=972 quad_boundary=240$',dispatch,'AC dispatch family mismatch')
-            require(all(value>0 for value in entries.values()),'AC legacy helper not entered')
-            one(r'^PASS: 1212 dispatch cases; exact per-case rowseven entries and bitwise scalar reference$',dispatch,'AC dispatch1212 missing')
-            one(r'^PASS: 432 direct fallback cases; kh1\.\.6, readonly/guard/canary and bitwise scalar reference$',dispatch,'AC direct432 missing')
+            one(r'^FULL_MATRIX_COUNTS core=3888 narrow=144 small=720 larger=32 quad_boundary=960$',guard,'AE matrix family mismatch')
+            one(r'^DISPATCH_MATRIX_COUNTS core=972 quad_boundary=240$',dispatch,'AE dispatch family mismatch')
+            require(all(value>0 for value in entries.values()),'AE legacy helper not entered')
+            one(r'^PASS: 1212 dispatch cases; exact per-case rowseven entries and bitwise scalar reference$',dispatch,'AE dispatch1212 missing')
+            one(r'^PASS: 432 direct fallback cases; kh1\.\.6, readonly/guard/canary and bitwise scalar reference$',dispatch,'AE direct432 missing')
             mask=(1<<threads)-1
             extra={}
             for phase,n in (('dispatch',1236),('direct',1080)):
@@ -329,7 +351,7 @@
         extra_manual_hash_audit=False,compiler_version='10.3.1',build_commands=commands,stage_exits=[dict(stage=s,exit_code=0) for s in stage_names()],
         allocation=dict(cpus=cpus,numa_node=numa),assembly=assembly,performance_measured=False,performance_requires_root_review=True,
         automatic_performance_submission=False,spill_is_correctness_failure=False,executed_locally=False,executed_remotely=True,
-        sanitizer='not_run',issues=[],note='Actual independent AC diagnostics only; parent T/C52 and older logs provide no C55 PASS. No speed or promotion claim.')
+        sanitizer='not_run',issues=[],note='Actual independent AE diagnostics only; parent AC/C55 and older logs provide no C56 PASS. No speed or promotion claim.')
 
 
 def failed_result(base,version):
--- .runs/conv/sep13ac-checks/freeze_returned.py
+++ .runs/conv/sep13ae-checks/freeze_returned.py
@@ -1,6 +1,6 @@
-"""Atomically freeze AC evidence; local file copying/text inspection only.
-
-Usage: python3 .runs/conv/sep13ac-checks/freeze_returned.py VERSION [--failed]
+"""Atomically freeze AE evidence; local file copying/text inspection only.
+
+Usage: python3 .runs/conv/sep13ae-checks/freeze_returned.py VERSION [--failed]
 This does not run acceptance, compile, test, submit, poll, SSH or publish.
 Original metadata/source/raw files are copied unchanged. Existing destination
 or staging trees are never removed or overwritten, including failed freezes.
@@ -18,10 +18,12 @@
 sys.dont_write_bytecode = True
 BASE=Path(__file__).resolve().parent
 ROOT=BASE.parents[2]
-VERSIONS=('C55-row7x3shared3',)
-EXPECTED={'C55-row7x3shared3':dict(full=5744,dispatch=1212,direct=432,total=44328)}
-SOURCE_SHA256='cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138'
+VERSIONS=('C56-row7shared3fence',)
+EXPECTED={'C56-row7shared3fence':dict(full=5744,dispatch=1212,direct=432,total=44328)}
+SOURCE_SHA256='6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c'
 SOURCE_NAMES={'conv2d.c','check_conv_guard.c','check_sve_dispatch.c','remote_job.sh','candidate.env'}
+FENCE_CONTRACT=dict(source_fences_per_main_iteration=3,input_accumulators_per_fence=21,
+    output_operands=0,memory_clobber=True,empty_template=True)
 TERMINAL={'SUCCEEDED','FAILED','CANCELLED','CANCELED','TIMEOUT','TERMINATED'}
 ATTACHMENTS={
     'driver.py':'diagnostic-driver.py',
@@ -68,9 +70,9 @@
         and job.get('source_hashes')==hashes and prepared.get('source_hashes')==hashes
         and prepared.get('source_sha256')==hashes['conv2d.c'],
         'Original prepared/job source identity missing or inconsistent')
-    require(hashes['conv2d.c']==SOURCE_SHA256 and prepared.get('source_parent')=='C52-row7x3shared2',
-        'AC must retain the original independent C55 source identity')
-    # Preserve the mandatory original AC byte-count manifest; compare existing
+    require(hashes['conv2d.c']==SOURCE_SHA256 and prepared.get('source_parent')=='C55-row7x3shared3',
+        'AE must retain the original independent C56 source identity')
+    # Preserve the mandatory original AE byte-count manifest; compare existing
     # metadata only, with no additional source digest computation.
     manifest_path=source/'source-manifest.json'
     manifest=read_json(manifest_path)
@@ -114,7 +116,7 @@
             and result.get('full_cases')==6*expected['full']
             and result.get('dispatch_cases')==6*expected['dispatch']
             and result.get('direct_cases',0)==6*expected['direct'],
-            'Accepted per-path/total counts differ from AC plan')
+            'Accepted per-path/total counts differ from AE plan')
         configurations=result.get('configurations') or []
         require(isinstance(configurations,list) and len(configurations)==6 and
             {(row.get('sve_bytes'),row.get('threads')) for row in configurations}==
@@ -141,15 +143,24 @@
         require(assembly.get('candidate')==version and assembly.get('job_id')==job_id
             and assembly.get('source_sha256')==SOURCE_SHA256
             and assembly.get('compiler_version')=='10.3.1'
-            and assembly.get('schema')=='ac-shared3-paths-v1'
+            and assembly.get('schema')=='ae-shared3-fence-paths-v1'
             and assembly.get('shared_triple_and_remainder_reviewed') is True
             and assembly.get('shared_unroll')==dict(main_columns=3,remainder_source_columns=1,remainder_max_columns=2,other_stage_columns=1),
             'Accepted independent shared triple/u1-remainder identity or review missing')
-        require(prepared.get('assembly_review_schema')=='ac-shared3-paths-v1'
+        require(prepared.get('assembly_review_schema')=='ae-shared3-fence-paths-v1'
             and prepared.get('shared_kernel_columns_per_iteration')==3
             and prepared.get('shared_remainder_source_columns_per_iteration')==1
             and prepared.get('shared_remainder_max_columns')==2,
-            'Initial AC shared contract mismatch')
+            'Initial AE shared contract mismatch')
+        require(prepared.get('column_fence_contract')==FENCE_CONTRACT
+            and assembly.get('column_fences_reviewed') is True,'Accepted column fence review missing')
+        comparison=assembly.get('parent_codegen_comparison') or {}
+        parent_asm=ROOT/'.runs/conv/C55-row7x3shared3/sve-correctness-sep13ac/raw/conv2d-sve.s'
+        require(comparison.get('candidate')=='C55-row7x3shared3' and comparison.get('job_id')=='1582860'
+            and comparison.get('source_sha256')=='cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138'
+            and comparison.get('assembly_sha256')==hashlib.sha256(parent_asm.read_bytes()).hexdigest()
+            and bool(comparison.get('shared_work_and_schedule_review'))
+            and bool(comparison.get('stack_and_spill_review')),'Accepted actual C55/AC parent comparison mismatch')
         phase_ids=[f'input_{i}' for i in range(6)]+['shared']+[f'trailing_{i}' for i in range(6)]
         helpers=assembly.get('helpers') or []
         require(helpers,'Missing accepted rowseven helpers')
@@ -161,6 +172,18 @@
                 if stage['stage']!='shared':
                     require(stage.get('kernel_columns_per_iteration')==1,'Non-shared stage work changed')
                     continue
+                fence=stage.get('column_fence_review') or {}
+                require(fence.get('source_contract')==FENCE_CONTRACT
+                    and type(fence.get('schedule_tightened')) is bool
+                    and all(isinstance(fence.get(key),str) and fence[key].strip() for key in
+                        ('actual_schedule_review','cross_column_motion_review','loop_boundary_review','spill_relocation_review')),
+                    'Accepted actual fence scheduling/stack interpretation missing')
+                boundaries=fence.get('boundaries') or []
+                require(isinstance(boundaries,list) and [b.get('column_index') for b in boundaries]==[0,1,2]
+                    and all(type(b.get('column_index')) is int
+                        and isinstance(b.get('actual_evidence'),str) and b['actual_evidence'].strip()
+                        and type(b.get('all_accumulators_ready_before_next_column_loads')) is bool for b in boundaries),
+                    'Accepted actual three-column fence boundary evidence missing')
                 blocks=stage.get('blocks') or []
                 require({block.get('role') for block in blocks}=={'triple_main','u1_remainder'},
                     'Accepted shared triple/u1 evidence incomplete')
@@ -214,8 +237,8 @@
     parser.add_argument('--failed',action='store_true')
     args=parser.parse_args()
     source=BASE/args.version
-    target=BASE.parent/args.version/'sve-correctness-sep13ac'
-    staging=target.with_name('.sve-correctness-sep13ac.preparing')
+    target=BASE.parent/args.version/'sve-correctness-sep13ae'
+    staging=target.with_name('.sve-correctness-sep13ae.preparing')
     require(source.is_dir() and target.parent.is_dir(),'Source/candidate directory missing')
     # Respect shared workflow locking, but do not modify any other evidence.
     with (ROOT/'.runs/.workflow.lock').open('a') as handle:
--- .runs/conv/sep13ac-checks/INTERFACE.md
+++ .runs/conv/sep13ae-checks/INTERFACE.md
@@ -1,59 +1,74 @@
-# AC：C55 独立诊断及 shared3 真实汇编接口
+# AE：C56 shared3 列末约束的独立诊断接口
 
-仅适用于 `C55-row7x3shared3`，source parent=`C52-row7x3shared2`。最终生产源 SHA256 `cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138`，与作者最终原件逐字节相同。父冻结为 T1582134；AA 只是三个工具及两个 C checker 的模板，不是 C55 通过证据。首次五文件 manifest/prepared 和提交规则见 `SUBMISSION_INTERFACE.md`。本次仅准备和轻量文本比较，没有导入或运行新工具、SSH、预约、编译、数值、接受或冻结。
+仅适用于 `C56-row7shared3fence`，source parent=`C55-row7x3shared3`。root 转达作者 final/STOP 并完成独立源审后，已首次复制112355字节生产源，SHA256=`6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c`；五文件/清单/三工具均完成准备。原 AC1582860 是工具、两个 checker 和父代码生成比较来源，不能提供 C56 PASS。所有示例入口均须 root 后续审查/GO，准备者未导入或执行。
 
-## 数值合同与范围
+源合同为 7 rows × 3VL、21 accumulators；13 semantic stages；shared main 三列 `ik/ik+1/ik+2`，原 u1 余数 0..2；其他十二阶段仍单列。唯一新增假设为三个完整列末的空 asm，各 21 个只读 `w` 输入、0 输出、`memory` clobber。它是编译器约束，不是硬件 fence，不能从源码推断实际调度或零 spill。
 
-两个 C checker 完整复用 AA 最终字节。`quad_boundary` 家族名、C54/four-column 注释是继承网格及来源名称；C55 shared 主循环实际是三列，不能把该名称当四列执行证据。SVE_BYTES={16,32,64}×线程={1,4}，共六配置，worker 实际 VL/team 必须匹配。
+## 数值、资源与首次源合同
 
-| 数量 | full | dispatch | direct | 合计 |
+两个 C checker 必须与原 AC 字节相同。继承 `quad_boundary` 家族名以及 C54/four-column 注释仅表示原网格/来源，不表示本版主循环四列。六配置 SVE_BYTES={16,32,64} × threads={1,4}，worker 实际 VL/team 必须匹配。
+
+| 数量 | full | dispatch | direct | 总数 |
 |---|---:|---:|---:|---:|
 | 每配置 | 5744 | 1212 | 432 | 7388 |
 | 六配置 | 34464 | 7272 | 2592 | 44328 |
 
-full 家族独立推导：core=六宽×kh6..8×kw1..3×18个oh×四分配=3888；narrow=ow1×kh6..8×kw1..3×oh1/7/13/28×四分配=144；small=三个ow×kh1..5×kw1..3×四oh×四分配=720；larger=四kernel×两oh×四分配=32；继承 quad_boundary=六宽×kh7/8×kw4..8×oh7/8/14/28×四分配=960。六宽为3L−1/3L/3L+1/6L−1/6L/6L+1，L=SVE_BYTES/4。larger 保留10×7、9×8、15×15、81×81；small 三宽为1/3L−1/3L+1，direct 三宽为3L−1/3L/3L+1，direct没有ow1。
+full 家族精确为 core3888/narrow144/small720/larger32/quad_boundary960；dispatch 为 core972/quad_boundary240。原 core：六宽 × kh6..8 × kw1..3 × 18个 oh × 四分配；18个 oh=1..15/21/22/28。narrow 是 ow1 × kh6..8 × kw1..3 × oh1/7/13/28 × 四分配；small 是三宽1/3L−1/3L+1 × kh1..5 × kw1..3 × 同四个oh × 四分配；larger 保留10×7、9×8、15×15、81×81两个oh四分配。新增于祖模板的网格为六宽3L/6L±1 × kh7/8 × kw4..8 × oh7/8/14/28；full 四分配，dispatch 固定 pad1/leading0。L=SVE_BYTES/4。
 
-dispatch 原core972+继承网格240=1212；网格与full同维度但固定pad1/leading0。rowseven 入口原756+新增六宽×两kh×五kw×(1+1+2+4)=1236。direct=三个宽×kh1..6×kw1..3×oh7/28×四分配=432，其入口=3×6×3×4×(1+4)=1080。逐case rowseven 入口增量保持；线程1/4两套worker mask应为1/15，旧prefix/tail/pair/triple/quad套内非零。这些入口和mask不是 shared 内部阶段的动态计数。
+dispatch 的 rowseven 入口1236，direct1080，worker mask1/15；原逐 case 入口增量校验不改。direct 三宽3L−1/3L/3L+1 × kh1..6 × kw1..3 × oh7/28 × 四分配，共432，仅覆盖防御fallback，不进入shared。旧 prefix/tail/pair/triple/quad 必须 suite 累计非零，不能声称每 case 的尾路线均已单独证明。
 
-完整日志必须有精确 `FULL_MATRIX_COUNTS core=3888 narrow=144 small=720 larger=32 quad_boundary=960` 和 `DISPATCH_MATRIX_COUNTS core=972 quad_boundary=240`，以及5744/1212/432各自的PASS总结；不能只匹配一个总PASS。原18个oh为1..15、21、22、28，覆盖oh余1..6。kw1/2跳main，kw3一轮main，kw4/5一轮后余1/2，kw6/7/8两轮后余0/1/2，kw15/81多轮；kh7/8覆盖shared跨t，六宽与oh28覆盖多tile及多组重置。未增加作者可选kw79/80。
+kw1/2跳过main；3一轮；4/5一轮后余1/2；6/7/8两轮后余0/1/2；15/81长循环。kh7/8覆盖跨t，六宽和oh28覆盖跨tile/组重置。没有新增可选kw79/80、direct ow1、L/2L±1网格、逐行guard或sanitizer。保留逐位 scalar reference、只读input/kernel、分配两端PROT_NONE、页内canary、output poison。没有官方runner或性能测量。
 
-保留独立 scalar 参考、逐位memcmp、input/kernel只读、分配两端PROT_NONE、页内外围canary、output poison。没有逐行guard/sanitizer/官方runner，也没有补direct ow1或L/2L±1网格。direct kh1..6验证防御fallback，不进入shared。旧helper非零和worker mask均为suite累计，不是逐case尾helper路线证明。
+资源固定38 CPU、24576 MiB、1 packed NUMA、1800秒，GCC10.3.1/generic。实际 xtrace 中三条 gcc argv 必须与原 AC 完全一致：`-O3 -std=c11 -D_DEFAULT_SOURCE -Wall -Wextra -fno-fast-math -ffp-contract=off -mcpu=generic -fopenmp -DEXPECTED_ACC=3`；full checker 与生产源分开translation unit，只有独立dispatch插桩，汇编从未插桩生产源 `-S` 获得。19阶段为allocation/compiler/manifest/build-guard、六guard、build-dispatch、六dispatch、build-assembly/complete。接受须真实scheduler SUCCEEDED、job/system/wrapper全0、19有序stage全0、完整12份数值日志；只打印编译命令或总PASS不足。
 
-要求原scheduler SUCCEEDED且job/system均0、wrapper0、19个stage-exits精确有序全0；实际GCC10.3.1、38CPU单NUMA、三条实际shell xtrace编译argv、五返回源字节/SHA与完整六配置日志都要匹配。生产full与scalar参考为分开translation unit，独立dispatch才插桩；严格flags和原checker未改。打印BUILD_COMMAND不能替代实际argv。无对象传输，只接收本版未插桩生产 `.s`。
+首次传输严格五文件：conv2d.c、两个原 AC checker、candidate.env、remote_job.sh；env/wrapper只改身份及实际源SHA。source-hashes.json和source-manifest.json分别保留实际SHA及bytes/SHA，prepared.json永远保持 prepared/complete=false/compiled=false/executed=false/verified=false/actual_job_id=null。完整源与checkpoint、原job manifest及返回五文件均须一致，首次清单不得静默刷新。
 
-prepared永远保留初始prepared/compiled=false/executed=false/verified=false。expected_checks为{full_per_configuration:5744,dispatch_per_configuration:1212,direct_per_configuration:432,configurations:6,total:44328}；expected_rowseven_entries为{dispatch_per_configuration:1236,direct_per_configuration:1080}。源形状rows_per_group=7/vectors_per_row=3/accumulators=21，expected_acc=3指vectors。expected_semantic_stage_count=13、shared_kernel_columns_per_iteration=3、shared_remainder_source_columns_per_iteration=1、shared_remainder_max_columns=2、other_stage_kernel_columns_per_iteration=1；assembly_review_schema=`ac-shared3-paths-v1`。这些是源工作合同，不是测得的机器数量。
+标准new/checkpoint的父字段为parent；未来性能record若存在source_parent则优先它。因此driver/accept使用`record.get('source_parent', record.get('parent'))`核对C55，不能改写creation metadata或record来适配。首次准备已核对候选source-audit的父C55/AC1582860和当前四文件SHA；第一次错误读取source_parent的KeyError原输出保留在preparation-failure-1.json。这是只读准备schema兼容，未放宽任何数值条件。
 
-## 真实 assembly-review.json
+prepared 的 expected_checks 为 full5744/dispatch1212/direct432/configurations6/total44328；expected_rowseven_entries 为1236/1080；expected_semantic_stage_count=13、shared_kernel_columns_per_iteration=3、shared_remainder_source_columns_per_iteration=1、shared_remainder_max_columns=2、other_stage_kernel_columns_per_iteration=1。assembly_review_schema=`ae-shared3-fence-paths-v1`。新增 column_fence_contract 精确为：
 
-准备阶段不创建带PASS/PC/行号/计数/spill的review。原作业 `.s` 返回后，审查人从完整实际函数填证据，接受器从同一原件重新派生机器计数。schema=`ac-shared3-paths-v1`；candidate/job_id/source_sha256/compiler_version/assembly_sha256必须绑定本版原件。compiler_version为10.3.1。review_complete、production_uninstrumented、whole_helper_stack_reviewed、all_stages_and_transitions_reviewed、abi_saves_distinguished、dispatch_reviewed、shared_triple_and_remainder_reviewed只有实际审完才能true；whole_source_fma_count必须实际全.s扫描为0，whole_machine_code_identical_claimed=false。
+```json
+{"source_fences_per_main_iteration":3,"input_accumulators_per_fence":21,"output_operands":0,"memory_clobber":true,"empty_template":true}
+```
 
-shape={rows_per_group:7,vectors_per_row:3,accumulators:21}；shared_unroll={main_columns:3,remainder_source_columns:1,remainder_max_columns:2,other_stage_columns:1}。不得照搬AA quad_main或T paired/odd结构，也不能固定14或其它region数量。
+## 原件返回后的 assembly-review.json
 
-helpers精确列全部实际conv_sve_rowseven及clone，symbol/line_start/line_end覆盖实际label到.size。每个helper须有非空stack_frame_description、spill_notes、transition_spill_review、tail_and_fallback_review、arithmetic_and_load_review、shared_index_reset_review、input_kernel_bounds_review、scalar_address_stack_review，以及实际非负整数vector_spill_loads/stores。完整审ABI低64位D保存、Z/Q/谓词与GPR/标量及间接栈、阶段转换、21 stores、横向尾和kh<7防御。源码局部作用域与21acc不能证明零spill。
+准备不创建 assembly-review、PC、行号、机器计数、spill 或 PASS。真实原件返回后，由完整实际函数审查填写：schema=`ae-shared3-fence-paths-v1`；candidate/job_id/source_sha256/compiler_version=10.3.1/assembly_sha256 全部绑定本版原件。review_complete、production_uninstrumented、whole_helper_stack_reviewed、all_stages_and_transitions_reviewed、abi_saves_distinguished、dispatch_reviewed、shared_triple_and_remainder_reviewed、column_fences_reviewed 只有实际完成对应审查才能为true。whole_source_fma_count必须全.s实际为0；whole_machine_code_identical_claimed=false。
 
-stages精确顺序input_0..5/shared/trailing_0..5，共13语义阶段。其他12阶段保留单实际区间字段stage/line_start/line_end、kernel_columns_per_iteration=1、source_mapping、spill_notes、vector_spill_loads/stores。各范围在helper内部且不能交叠。如果实际lowering无法忠实映射，原件停交root，不挑区间补成预期。
+shape={rows_per_group:7,vectors_per_row:3,accumulators:21}；shared_unroll={main_columns:3,remainder_source_columns:1,remainder_max_columns:2,other_stage_columns:1}。helpers须精确列实际conv_sve_rowseven及全部clone，symbol/line_start/line_end覆盖实际label到.size。每helper须有非空stack_frame_description、spill_notes、transition_spill_review、tail_and_fallback_review、arithmetic_and_load_review、shared_index_reset_review、input_kernel_bounds_review、scalar_address_stack_review及实际非负vector_spill_loads/stores。区分Z/Q/谓词、低64位D ABI和标量/间接栈，审查所有阶段转换、21输出、横向尾和kh<7防御。
 
-shared字段如下：
+stages精确为input_0..5/shared/trailing_0..5。其他12阶段沿用单真实区间、kernel_columns_per_iteration=1、source_mapping/spill_notes/vector_spill_loads/stores。shared要求非空source_mapping、control_flow_notes、accumulator_order_review、triple_to_remainder_review、transition_spill_review、t_and_tile_reset_review；其blocks/path规则保持 AC：
 
-- 非空source_mapping、control_flow_notes、accumulator_order_review、triple_to_remainder_review、transition_spill_review、t_and_tile_reset_review，解释ik→ik+1→ik+2逐acc次序、三列循环与余数转场、kh7/8跨t和每tile重置。
-- blocks非空且数量不固定，block_id唯一。role集合恰为triple_main/u1_remainder；region_kind为loop或straight_line。kernel_columns_per_iteration为一次实际块完整源列工作：triple_main=3，余数loop=1，余数straight_line可为1或2（只是编译器机器聚合，不是新增源码paired层）。至少存在一个真实triple_main loop。
-- 每block的ranges为一或多个{line_start,line_end}真实闭区间，须在helper内、和全部其他block/stage不交叠。不同区间可表示同一路径上分散的算术/地址控制，但不能加总互斥分支来凑工作量。block的source_mapping、spill_notes、entry_exit_notes、column_order_review、control_flow_evidence必填，以实际标号/分支支持；vector_spill_loads/stores为真实观察。
-- remainder_paths非空且path_id唯一。remainder_columns为整数，覆盖集合{0,1,2}，同一余数可存在多条真实路径。每path有非空path_condition/execution_order_review和按顺序的sequence；sequence项{block_id,executions}只引用u1_remainder，executions为1或2，straight_line只可1。总executions×work必须等于余数列数，余0是[]；每个实际余数block都须被至少一条路径引用。不得把静态block个数称为动态执行次数。
+- blocks 非空、block_id唯一，role集合恰为triple_main/u1_remainder，region_kind为loop或straight_line。main完整work3且至少一个真实loop；余数loop work1，straight_line work1或2。
+- 每block有一或多个真实ranges[{line_start,line_end}]，全部在helper内且互不重叠，也不与其他stage重叠。source_mapping/spill_notes/entry_exit_notes/column_order_review/control_flow_evidence必填，vector_spill_loads/stores为实际观察。分散区间须属于同一路径，不能把互斥分支相加凑工作。
+- remainder_paths的path_id唯一，remainder_columns覆盖{0,1,2}。每path有path_condition/execution_order_review与有序sequence[{block_id,executions}]；只能引用u1_remainder，executions=1或2，straight_line只可1。总executions×work等于余数，余0为[]，每个实际余数block至少被一条路径引用。
 
-接受器对每block的真实ranges累计derived_counts：instructions/fmul/fmul_ordinary/fmul_indexed/fadd/ld1w/ld1rw/ld1rqw/dup/indexed_mov/ext/movprfx，并逐项除work产生derived_counts_per_kernel_column。源映射要求完整三列实际对应63 FMUL/63 FADD、每余数工作列21/21；这些是验收工作关系，不是准备时填写的机器结果。载入数、地址数、总指令、栈/spill都不预设。区间可包含无算术的控制部分，完整block必须能解释所声明工作。若进一步展开等形态不满足当前schema，保留首次原件停止并审schema，不伪造PC/借父范围/改变数值阈值。
+接受器从实际ranges重算derived_counts与逐work的derived_counts_per_kernel_column。完整main为63 FMUL/63 FADD，每余数工作列21/21；载入/地址/总指令/spill数不预设。shared.actual_arithmetic_region_count、helper.actual_arithmetic_region_count和顶层arithmetic_region_counts由实际ranges派生，绝不固定14。若真实lowering不能表达为本合同，保留原件和首次解析失败再交root审，不能借原AC行号或挑区间。
 
-shared.actual_arithmetic_region_count为实际ranges数量之和；helper.actual_arithmetic_region_count为其他12范围加shared实际数量；顶层arithmetic_region_counts为symbol→实际数量。均由接受器派生，不能固定。语义阶段数仍13。
+每helper的shared新增 **column_fence_review**：
 
-dispatch_functions精确列全部实际conv2d及outlined worker，每项symbol/line_start/line_end/control_flow_notes/stack_notes，实际label到.size完整审。parent_codegen_comparison仍是原C52-row7x3shared2/T1582134、父源SHA `8cf5dc086d0fd6c7ce21f0f5cfd2bbf50da820a9c18e12de328e970a28712bf7`、父实际assembly_sha256、shared_work_and_schedule_review、stack_and_spill_review。父.s哈希从原冻结T核对；比较工作归一化、地址/分支、活跃期与栈，不继承父PASS或声称速度。
+- source_contract等于上述column_fence_contract；actual_schedule_review、cross_column_motion_review、loop_boundary_review、spill_relocation_review均为非空实际解释。
+- schedule_tightened为实际bool，**允许false**。boundaries依序三项column_index=0/1/2，各有非空actual_evidence和bool all_accumulators_ready_before_next_column_loads。column_index=2 的第三个边界包括下一主循环迭代。追踪全部21条本列FADD与后列LD1W/LD1RW的真实依赖/位置，不要求空asm产生指令或可见标号，也不伪造fence PC。
+- 解释三处约束是否只读21输入、是否实际限制跨列/跨迭代调度，以及主循环减少的spill是否移至列边界、转场或函数外围。审查完成不等于调度收紧；调度未收紧、spill非零也不冒充数值失败。
 
-## 接受、冻结和串行边界
+dispatch_functions须完整覆盖实际conv2d/outlined workers的label到.size及control_flow_notes/stack_notes。parent_codegen_comparison固定candidate=`C55-row7x3shared3`、job_id=`1582860`、source_sha256=`cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138`，assembly_sha256须匹配其`sve-correctness-sep13ac/raw/conv2d-sve.s`真实字节。shared_work_and_schedule_review/stack_and_spill_review必须比较原C55三列189指令、5Z读5写、全helper30/45及880+5VL框架的实际含义；这些仅是父参照，不是本版预期机器结果或提速保证。
 
-driver串行门禁固定AA1582656/Y1582410/X1582372/T1582134，加现存P/AC和明确AB campaign/四member预约；详见SUBMISSION_INTERFACE。未知ID、ID冲突、查询失败、非terminal或缺实际退出均阻塞。root独立GO唯一提交，网络不确定只对账原预约。prepared P/AC没有job不占用；不扫描其它队友。
+## 串行、接受与冻结
 
-root全文审查完成并授权实际返回的审查后，才对原作业调用accept_returned.py C55-row7x3shared3，随后freeze_returned.py C55-row7x3shared3。准备未调用。成功validation保存原job/scheduler/exit、total44328/full34464/dispatch7272/direct2592、六config及family/entries/mask、源身份、19stage_exits和实际assembly。assembly含stage_count=13、fma_count=0、transitions_reviewed=true和真实多区间/路径，production_object_disassembly_available=false。每config还保存sve_bytes/threads/lanes/block_outputs_per_row=3×lanes。无性能资格自动计算。
+driver仅检查固定AD1582956/AC1582860/AB1582814/AA1582656/Y1582410/X1582372/T1582134，以及现存P/AE job.json。AD/AB各原四成员cluster.json须与固定campaign ID相同；缺固定证据、未对账/冲突ID、查询失败、非terminal或缺真实job/system退出都阻塞。无P/AE job.json的prepared目录不阻塞。不扫描队友，不取消作业，root始终唯一提交者。
 
-首次prepared原件不变。accept拒绝覆盖已final validation；若原为prepared先原字节保存prepared-validation.json，解析异常另存timestamped acceptance-failure。--failed只接受实际scheduler/wrapper/stage失败，未知实际检查数为null；准备/解析问题不当算子FAIL。
+提交示例（准备者未执行）：
 
-freeze在共享workflow lock下检查原身份/终态/已接受schema并复制原件，附三工具/本接口；原.s同字节另存.assembly.txt，目标`.runs/conv/C55-row7x3shared3/sve-correctness-sep13ac`以原子rename完成。已有target/staging拒绝覆盖，保留失败现场；freeze-source标记实际passed/failed及原job。不运行accept、不改源码record/best、不压比赛包。
+```text
+python3 .runs/conv/sep13ae-checks/driver.py C56-row7shared3fence config/conv-sep12.local.json submit --go
+python3 .runs/conv/sep13ae-checks/driver.py C56-row7shared3fence config/conv-sep12.local.json status
+python3 .runs/conv/sep13ae-checks/driver.py C56-row7shared3fence config/conv-sep12.local.json fetch
+python3 .runs/conv/sep13ae-checks/accept_returned.py C56-row7shared3fence
+python3 .runs/conv/sep13ae-checks/freeze_returned.py C56-row7shared3fence
+```
 
-C55当前仅准备。数值与汇编审查不等于速度，真实spill不自动等同数值失败。C6最佳保持；Yfalse/Sfalse不改写、不重测失败确认。主额度达到40%停止，不用重置卡。工具全部完成交root审查后STOP。
+submit须显式--go且无job.json；先排他open('x')预约，再传输/提交。不确定或失败保留预约/日志，禁止重投，只对账原ID。status/fetch不自动轮询；外层间隔至少45秒，每次stdout/stderr/exit独立保存。fetch只在真terminal后返回文本/.s，拒绝覆盖异字节；不传对象。
+
+accept保留原prepared；拒绝覆盖final validation。解析问题另存timestamped acceptance-failure，不捏造算子FAIL；--failed仅处理原调度/wrapper/stage真实失败，未知实际次数为null。普通validation记录44328完整六配置、19stage/全部退出、实际argv/源/assembly，performance_measured=false，无自动性能提交。
+
+freeze仅在共享workflow锁内核对既有接受结果/原父比较/实际fence字段，复制原件到`sve-correctness-sep13ae`，附三工具及本接口；原.s同字节生成.assembly.txt，原子rename完成。已有target/staging一律拒绝覆盖，失败现场保留；不执行accept、算子、性能、包、晋级或发布。Yfalse/Sfalse与C6历史保持。额度达到40%停止，不用重置卡。
--- /dev/null
+++ .runs/conv/sep13ae-checks/README.md
@@ -0,0 +1,11 @@
+# AE 准备入口
+
+候选 `C56-row7shared3fence`；父 `C55-row7x3shared3` / 原 AC1582860。此目录只用于本版独立诊断，未授权计算、性能或晋级。完整合同见 [INTERFACE.md](INTERFACE.md)。
+
+三工具、接口和五文件首次清单已完成静态准备，候选生产源为112355字节、SHA256=`6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c`。最终文件身份与完整差异列在 PREPARED_FILES.json、AC-to-AE.patch；它们不是通过证据。首次准备schema KeyError原输出保留，record采用source_parent优先、缺省parent的兼容读取，未改历史。
+
+唯一未来预约为 `C56-row7shared3fence/job.json`，冻结目标为候选原树的 `sve-correctness-sep13ae`。driver/accept/freezer 均只由 root 审查后另行授权；失联只恢复原ID，已有预约禁止重投。
+
+复用两个原 AC checker 的44328矩阵、六VL/线程配置和19stage，不继承父PASS；实际汇编审查必须覆盖三列边界和整个helper栈。schema `ae-shared3-fence-paths-v1` 允许如实记录调度未收紧或仍有spill，不把它们等同数值失败，不推导提速。
+
+本机只编辑/读文件，没有工具导入、编译、数值、SSH、预约或接受/冻结。AD原C55false、Yfalse/Sfalse与C6保持。用量40%停止，不用重置卡。准备完成，FINAL/STOP，等待root独立审查。
--- .runs/conv/sep13ac-checks/STATIC_REVIEW.md
+++ .runs/conv/sep13ae-checks/STATIC_REVIEW.md
@@ -1,11 +1,16 @@
-# AC 作者静态复核
+# AE 作者静态审查
 
-2026-09-13，只读差异、轻量文件复制与文本/manifest整理；未导入/执行任何新诊断工具，未编译、测试、SSH、预约或运行accept/freezer。源作者final/STOP由root确认，root源审查见C55目录INDEPENDENT_SOURCE_REVIEW.md。独立工具审查与唯一GO属于root。
+root 转达作者final/STOP后已完成首次传输清单。生产源112355字节，SHA256=`6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c`。准备FINAL/STOP；这里没有运行结果。
 
-- C55副本与最终源逐字节cmp相同，SHA cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138；source parent仍C52/T。两个checker逐字节cmp与最终AA相同，保留44328矩阵/1236与1080入口/masks1与15；quad_boundary仅继承名称。wrapper差异只有candidate和源SHA，env只有candidate。
-- 已完整读三工具相对AA差异。driver只改身份、源契约main3/maxrem2和有界serial gate；固定AA/Y/X/T实际ID，加入AB明确campaign/四member，未知/冲突ID阻塞，实际ID必须查到terminal和完整退出。原exclusive预约先于网络、--go、原ID恢复与不覆盖异字节fetch行为保持。
-- accept/freezer仅必要身份及shared4→shared3映射：schema ac-shared3-paths-v1、triple_main work3、u1余0..2；range数量动态、不固定PC或机器count。实际21×work算术归一化与ordered remainder paths检查保持；其他12阶段u1、所有真实helper/clone和dispatch全范围、原T来源比较、全.s FMA、完整栈/转场说明保持。
-- 实际数值仍须GCC10.3.1、严格原argv、38CPU单NUMA、19阶段和wrapper/scheduler/job/system全成功、六完整配置独立通过及源身份核对。failed只能来自真实远程失败；解析不当算子FAIL。原件不覆盖，冻结保持workflow lock与atomic rename。
-- 没有实际job/validation/assembly-review/机器PC/派生计数或PASS文件；没有性能、包或晋级。AA/T通过不继承到C55。Y/S失败确认和当前C6均未改动。
+- 三工具基于最终 AC 原文本。身份改 C56/AE，父源与代码生成比较改原 C55/AC1582860；数值矩阵、strict flags、原日志PASS/FAIL、19stage/全部退出检查保持。
+- driver 固定 AD1582956/AC1582860/AB1582814/AA1582656/Y1582410/X1582372/T1582134，并只读 AD/AB 明确四成员及 P/AE 预约；排他创建、未对账保留、status/fetch原ID和异字节保护不变。不扫描队友。
+- main3/u1余0..2、多真实ranges/path工作计数来自 AC。实际region数量动态，13为语义阶段数；无旧PC、固定14或预填机器结果。
+- 新增 column_fence_contract 和真实 column_fence_review；全部21条链与三个源边界须有实际FADD/后列load解释，schedule_tightened允许false。正确性通过不承诺fence有效。
+- freeze复核上述fence结果及原C55实际父汇编身份；原文件/失败保留、共享锁、原子rename、已有target/staging拒绝覆盖保持。
+- 标准C56 new/checkpoint记录实际使用parent且没有source_parent；首次准备只读检查KeyError原输出保留，root批准driver/accept优先source_parent、缺省parent。候选source-audit中的C55/AC1582860/当前源SHA也已核对；未改C56 creation metadata或record，数值门槛不变。
+- 原五文件首次复制成功。C56副本与作者最终源相同；两个checker与AC source及AC冻结raw分别字节相同；env/wrapper仅版本名与实际源SHA改变。生产四文件与candidate record一致，README/bench_conv.c/run.sh与父原字节相同；父四文件与父record一致，父conv与原AC冻结raw相同。生产四文件集合与诊断五文件集合分开核对。
+- 已逐项阅读三工具相对 AC 的文本diff；仅静态文件处理，未import、AST、py_compile、dry-run或执行任一新工具，也没有算子/SSH/作业/accept/freeze。
 
-未见本轮静态准备阻碍。编译器实际形态若超出schema，保留首次返回产物停交root处理，不能预填、改阈值或再跑以掩盖问题。完整文件身份在PREPARED_FILES.json，AA-to-AC.patch保存三工具、wrapper/env及生产源/初始元数据相对模板的完整差异；两checker无差异。
+首次文本替换草稿中发现普通字符串替换会触碰大写标识符，在任何执行或清单生成前，已重新从原AC按词边界生成三工具，并在完整diff中确认 EXPECTED_ACC、OBJECT_ARTIFACTS 及数值正则保持。没有把草稿执行或解析失败写成算子失败。
+
+prepared所有编译/执行/verified字段为false且实际job为null。没有job、raw、validation或assembly-review；原AD C55false和Y/Sfalse未改，不做失败确认重试。全部工具仍待root全文/独立审查和唯一GO。
--- .runs/conv/sep13ac-checks/PREPARATION_COMMANDS.md
+++ .runs/conv/sep13ae-checks/PREPARATION_COMMANDS.md
@@ -1,12 +1,14 @@
-# AC 准备操作留痕
+# AE 准备操作来源
 
-2026-09-13。本说明记录实际轻量准备操作类别和工具返回，不是执行AC诊断命令的日志。
+本说明记录真实轻量准备操作及工具返回摘要，不冒充诊断/编译raw日志。所有操作仅限AE新目录和只读既有模板。
 
-1. 读取AGENTS、已读项目规则及AA成熟模板，查看共享工作区；确认AC路径原先不存在。root转达C55作者final/STOP后才复制。没有读取账号凭据。
-2. 原生mkdir/cp创建AC目录、三工具和五文件；只对AC三工具及wrapper/env作身份与schema文本替换（外层exec chunk902593，exit0）。两个C checker完整复制，不修改。
-3. apply_patch改driver固定AA/Y/X/T与明确AB reservation门禁；perl改accept/freezer的main3/maxrem2/path合同。shasum/wc及driver diff为纯文件检查；chunk1c32ff退出1由预期有差异的diff产生，不是工具或算子失败。
-4. Python标准库仅对小文件read_bytes/hash/json/datetime/open('x')生成首次五文件SHA/bytes清单和初始prepared；没有import AC/AA工具或candidate，未编译/解析执行工具（chunke82d6a，exit0）。prepared中的时间是该文件操作实际UTC时间。
-5. apply_patch写提交接口；shasum与三个cmp核对C55副本和两个AA checker，chunk52f10b退出0。此后已向root声明提交子集final/STOP，不再编辑其driver/source/首manifest/prepared/提交接口。
-6. 读取accept/freezer完整diff和shared/schema核心；差异退出1是diff约定，未运行二者。只新增INTERFACE、STATIC_REVIEW、本说明及完整差异/文件身份清单，最后交root审。
+1. 读取AGENTS、工作区状态、AC三工具/接口/原prepared及env/wrapper；确认新目录不存在。首读chunk754ee0、实际git状态chunk a3afd6；没有改动其中其他协作者文件。
+2. 纯Python标准库文本操作首次建立三工具草稿，chunk ab8094，exit0。发现无边界替换会误改普通大写标识符后，重新从原AC按词边界生成并替换固定serial函数，chunk829cf3，exit0；没有导入或执行这些草稿。
+3. apply_patch仅新增fence合同/实际审查字段及freezer父比较，写接口/README/静态说明。source SHA在收到root最终身份前明确占位，未复制候选/建立manifest。
+4. 人工读取三个全文diff。driver差异chunk a6b371、accept差异chunk6c5b5a，各exit1表示git diff --no-index发现预期文本差异；freezer差异及定位chunk6ee9fa退出0（最后rg）。这不是算子、编译或工具运行失败。
 
-没有SSH、调度预约、新job、编译、题目执行、基准、accept、freeze、包或晋级。没有AST/py_compile/导入/dry-run，也没有本机测试替代超算。后续示例入口只在SUBMISSION_INTERFACE供root审核，准备者不执行。当前主额度读12%，40%即停，不用重置卡。
+5. root转达作者final/STOP与最终SHA后，首次复制前的只读record检查遇到不存在的source_parent键；chunk7b0724，exit1，尚无传输目录/清单或工具SHA改动。原输出逐字保存preparation-failure-1.json；随后只读实际record/experiment/audit，chunkd3c6f2、65b0ba各exit0。root明确批准优先source_parent、缺省parent的schema兼容，driver/accept同步最小修改，未修改原record。
+6. 纯Python标准库读取父/候选四文件及source-audit，分别核对其record和父冻结conv；root确认源SHA/112355B匹配后，首次排他写五文件与SHA/bytes/prepared，chunke44667，exit0。两个checker另与原AC source和冻结raw字节比较相同。该命令仅文本/JSON/复制/哈希操作，未导入或执行AE工具。
+7. apply_patch完成接口、README和作者静态说明。最终完整差异/文件身份清单由标准库difflib/hashlib读取实际字节生成，独立检查无job/raw/validation/assembly-review；不制造PASS。
+
+所有实际job/汇编审查保持缺席，待独立授权。准备完成STOP。
--- /dev/null
+++ .runs/conv/sep13ae-checks/preparation-failure-1.json
@@ -0,0 +1,13 @@
+{
+  "phase": "read_only_before_first_transport_copy",
+  "tool_output_chunk": "7b0724",
+  "exit_code": 1,
+  "output_verbatim": "Traceback (most recent call last):\n  File \"<stdin>\", line 10, in <module>\nKeyError: 'source_parent'\n",
+  "operation": "Lightweight Python read of the candidate standard record; no AE tool imported or executed.",
+  "failed_expression": "record['version']==version and record['source_parent']==parent",
+  "transport_copy_created": false,
+  "manifest_created": false,
+  "tool_source_hash_placeholder_changed": false,
+  "operator_executed": false,
+  "raw_output_origin": "Exact text returned by exec_command; this metadata is a preparation failure record, not a diagnostic raw log."
+}
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/source/conv2d.c
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/source/conv2d.c
@@ -1528,6 +1528,16 @@
                         g2 = svadd_f32_x(pg, g2, svmul_f32_x(pg, v, gk));
                     }
                 }
+                __asm__ __volatile__(""
+                    :
+                    : "w"(a0), "w"(a1), "w"(a2),
+                      "w"(b0), "w"(b1), "w"(b2),
+                      "w"(c0), "w"(c1), "w"(c2),
+                      "w"(d0), "w"(d1), "w"(d2),
+                      "w"(e0), "w"(e1), "w"(e2),
+                      "w"(f0), "w"(f1), "w"(f2),
+                      "w"(g0), "w"(g1), "w"(g2)
+                    : "memory");
                 {
                     const int column = ik + 1;
                     const svfloat32_t ak = svdup_n_f32(ka[column]);
@@ -1569,6 +1579,16 @@
                         g2 = svadd_f32_x(pg, g2, svmul_f32_x(pg, v, gk));
                     }
                 }
+                __asm__ __volatile__(""
+                    :
+                    : "w"(a0), "w"(a1), "w"(a2),
+                      "w"(b0), "w"(b1), "w"(b2),
+                      "w"(c0), "w"(c1), "w"(c2),
+                      "w"(d0), "w"(d1), "w"(d2),
+                      "w"(e0), "w"(e1), "w"(e2),
+                      "w"(f0), "w"(f1), "w"(f2),
+                      "w"(g0), "w"(g1), "w"(g2)
+                    : "memory");
                 {
                     const int column = ik + 2;
                     const svfloat32_t ak = svdup_n_f32(ka[column]);
@@ -1610,6 +1630,16 @@
                         g2 = svadd_f32_x(pg, g2, svmul_f32_x(pg, v, gk));
                     }
                 }
+                __asm__ __volatile__(""
+                    :
+                    : "w"(a0), "w"(a1), "w"(a2),
+                      "w"(b0), "w"(b1), "w"(b2),
+                      "w"(c0), "w"(c1), "w"(c2),
+                      "w"(d0), "w"(d1), "w"(d2),
+                      "w"(e0), "w"(e1), "w"(e2),
+                      "w"(f0), "w"(f1), "w"(f2),
+                      "w"(g0), "w"(g1), "w"(g2)
+                    : "memory");
             }
             for (; ik < kw; ++ik) {
                 const svfloat32_t ak = svdup_n_f32(ka[ik]);
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/source/candidate.env
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/source/candidate.env
@@ -1,4 +1,4 @@
-CANDIDATE=C55-row7x3shared3
+CANDIDATE=C56-row7shared3fence
 EXPECTED_ACC=3
 EXPECTED_FULL=5744
 EXPECTED_DISPATCH=1212
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/source/remote_job.sh
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/source/remote_job.sh
@@ -46,7 +46,7 @@
 print('NUMA_NODE=' + nodes[0])
 CHECK_ALLOCATION
 source candidate.env
-[[ "$CANDIDATE" == C55-row7x3shared3 && "$EXPECTED_ACC" == 3 ]]
+[[ "$CANDIDATE" == C56-row7shared3fence && "$EXPECTED_ACC" == 3 ]]
 [[ "$EXPECTED_FULL" == 5744 && "$EXPECTED_DISPATCH" == 1212 && "$EXPECTED_DIRECT" == 432 && "$EXPECTED_TOTAL" == 44328 ]]
 [[ "$EXPECTED_DISPATCH_ROWSEVEN_ENTRIES" == 1236 && "$EXPECTED_DIRECT_ROWSEVEN_ENTRIES" == 1080 ]]
 printf 'CANDIDATE=%s\n' "$CANDIDATE"
@@ -60,7 +60,7 @@
 stage compiler compiler_check
 write_manifest() {
     sha256sum conv2d.c check_conv_guard.c check_sve_dispatch.c remote_job.sh candidate.env > source-sha256.txt || return "$?"
-    printf '%s  conv2d.c\n' cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138 | sha256sum -c - || return "$?"
+    printf '%s  conv2d.c\n' 6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c | sha256sum -c - || return "$?"
     cat source-sha256.txt
 }
 stage manifest write_manifest
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/source-hashes.json
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/source-hashes.json
@@ -1,7 +1,7 @@
 {
-  "candidate.env": "751790be9095e0e92b10a5e11e3952a75d65c9df54d7dcb735b0fcc6a4f1685e",
+  "candidate.env": "ac725bd0f173565500cc006fa1d89ef02c4fdca51b2e415650fc975b799c87b7",
   "check_conv_guard.c": "ccbba2637255b5d2733dfbfdddd88afd0d09815d5759ca86c9e8c498475990d5",
   "check_sve_dispatch.c": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d",
-  "conv2d.c": "cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138",
-  "remote_job.sh": "d187d0016c986df296fdb42357282de9acdfd8d29db8716ef1a2b3bbeafb561e"
+  "conv2d.c": "6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c",
+  "remote_job.sh": "3ac920b75a16e15eafeafb5bc42da4b3cab4b5943297f08c459430e5cff5d2de"
 }
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/source-manifest.json
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/source-manifest.json
@@ -1,7 +1,7 @@
 {
   "candidate.env": {
-    "bytes": 204,
-    "sha256": "751790be9095e0e92b10a5e11e3952a75d65c9df54d7dcb735b0fcc6a4f1685e"
+    "bytes": 207,
+    "sha256": "ac725bd0f173565500cc006fa1d89ef02c4fdca51b2e415650fc975b799c87b7"
   },
   "check_conv_guard.c": {
     "bytes": 8698,
@@ -12,11 +12,11 @@
     "sha256": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d"
   },
   "conv2d.c": {
-    "bytes": 111044,
-    "sha256": "cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138"
+    "bytes": 112355,
+    "sha256": "6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c"
   },
   "remote_job.sh": {
-    "bytes": 3940,
-    "sha256": "d187d0016c986df296fdb42357282de9acdfd8d29db8716ef1a2b3bbeafb561e"
+    "bytes": 3943,
+    "sha256": "3ac920b75a16e15eafeafb5bc42da4b3cab4b5943297f08c459430e5cff5d2de"
   }
 }
--- .runs/conv/sep13ac-checks/C55-row7x3shared3/prepared.json
+++ .runs/conv/sep13ae-checks/C56-row7shared3fence/prepared.json
@@ -1,22 +1,22 @@
 {
-  "candidate": "C55-row7x3shared3",
+  "candidate": "C56-row7shared3fence",
   "status": "prepared",
   "complete": false,
-  "prepared_at": "2026-09-12T18:37:26.695703+00:00",
-  "source_parent": "C52-row7x3shared2",
-  "source_experiment": "C55-row7x3shared3",
-  "source_sha256": "cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138",
-  "production_source_sha256": "cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138",
+  "prepared_at": "2026-09-12T19:19:34.566012+00:00",
+  "source_parent": "C55-row7x3shared3",
+  "source_experiment": "C56-row7shared3fence",
+  "source_sha256": "6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c",
+  "production_source_sha256": "6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c",
   "source_hashes": {
-    "candidate.env": "751790be9095e0e92b10a5e11e3952a75d65c9df54d7dcb735b0fcc6a4f1685e",
+    "candidate.env": "ac725bd0f173565500cc006fa1d89ef02c4fdca51b2e415650fc975b799c87b7",
     "check_conv_guard.c": "ccbba2637255b5d2733dfbfdddd88afd0d09815d5759ca86c9e8c498475990d5",
     "check_sve_dispatch.c": "fa47796ed01ca2a7d925176b25a6b18d6d124385bf21c1e452a3c2f9afc0f81d",
-    "conv2d.c": "cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138",
-    "remote_job.sh": "d187d0016c986df296fdb42357282de9acdfd8d29db8716ef1a2b3bbeafb561e"
+    "conv2d.c": "6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c",
+    "remote_job.sh": "3ac920b75a16e15eafeafb5bc42da4b3cab4b5943297f08c459430e5cff5d2de"
   },
-  "source_hash_scope": "First five-file AC transport byte-count/SHA manifest; final C55 source and exact final AA checker bytes",
-  "guard_template": ".runs/conv/sep13aa-checks/C54-row7x3shared4/source",
-  "guard_change": "No checker byte changes. quad_boundary and C54/four-column comments are inherited AA grid/origin labels, not C55 four-column work.",
+  "source_hash_scope": "First five-file AE transport byte-count/SHA manifest after root confirmed final C56 source; two original AC checker bytes.",
+  "guard_template": ".runs/conv/sep13ac-checks/C55-row7x3shared3/source",
+  "guard_change": "No checker byte changes. quad_boundary/C54 comments retain inherited grid and origin labels, not C56 four-column work.",
   "rows_per_group": 7,
   "vectors_per_row": 3,
   "accumulators": 21,
@@ -148,18 +148,25 @@
   "executed": false,
   "verified": false,
   "local_compilation_or_tests_run": false,
-  "production_assembly_review": "Pending own actual C55 GCC10.3.1 uninstrumented .s. Thirteen semantic stages, triple3 and direct u1 remainder0..2 across actual intervals and paths; all helpers/clones, transitions, dispatch, FMA, scalar and vector stack/spills. Neither T nor AA PASS/codegen inherited.",
-  "source_parent_frozen_directory": ".runs/conv/C52-row7x3shared2/sve-correctness-sep13t",
-  "source_parent_diagnostic_job_id": "1582134",
+  "production_assembly_review": "Pending own actual C56 GCC10.3.1 uninstrumented assembly. Thirteen semantic stages and triple3/u1 remainder0..2 dynamic intervals/paths; actual three empty-input-fence boundaries, all helpers/clones, dispatch and complete Z/Q/predicate/D ABI/scalar stack. Parent AC PASS/PC/spill result not inherited.",
+  "source_parent_frozen_directory": ".runs/conv/C55-row7x3shared3/sve-correctness-sep13ac",
+  "source_parent_diagnostic_job_id": "1582860",
   "expected_semantic_stage_count": 13,
   "shared_kernel_columns_per_iteration": 3,
   "shared_remainder_source_columns_per_iteration": 1,
   "shared_remainder_max_columns": 2,
   "other_stage_kernel_columns_per_iteration": 1,
-  "assembly_review_schema": "ac-shared3-paths-v1",
+  "assembly_review_schema": "ae-shared3-fence-paths-v1",
   "preparation_authorizes_compute": false,
   "performance_measured": false,
-  "scheduling_scope": "Fixed AA1582656/Y1582410/X1582372/T1582134 plus present AB campaign/four member reservations and P/AC jobs only. Missing prepared P/AC jobs ignored; unknown, conflicting or nonterminal actual jobs block.",
-  "inherited_grid_name": "quad_boundary names the exact AA boundary grid only; C55 shared source main performs three columns.",
-  "shared_width_coverage": "kw1/2 skip main; kw3 one main; kw4/5 one main and remainder1/2; kw6/7/8 two main iterations and remainder0/1/2; kw15/81 multiple iterations. No optional kw79/80 cases added."
+  "scheduling_scope": "Fixed AD1582956/AC1582860/AB1582814/AA1582656/Y1582410/X1582372/T1582134, exact AD/AB four member manifests and present P/AE reservations only. Missing or conflicting fixed identities, unknown/unreconciled or nonterminal actual jobs block.",
+  "inherited_grid_name": "quad_boundary names the original unchanged boundary grid only; C56 shared main remains three columns.",
+  "shared_width_coverage": "kw1/2 skip main; kw3 one main; kw4/5 one main and remainder1/2; kw6/7/8 two main iterations and remainder0/1/2; kw15/81 multiple iterations. No optional kw79/80 cases added.",
+  "column_fence_contract": {
+    "source_fences_per_main_iteration": 3,
+    "input_accumulators_per_fence": 21,
+    "output_operands": 0,
+    "memory_clobber": true,
+    "empty_template": true
+  }
 }
