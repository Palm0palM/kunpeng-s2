# C56-row7shared3fence：源码候选

source parent为C55-row7x3shared3，父原诊断AC1582860冻结于 `.runs/conv/C55-row7x3shared3/sve-correctness-sep13ac`。父源SHA `cc6b51603928d7f7825f86ac09d139ec3e3846c8c40946e16fa9d110eb1a8138`。本候选最终源SHA `6c48bff4086a137d5fa425936a96e776b08ffdbd86f8a9e2816feda0f673224c`，112355字节。

唯一假设来自final `.runs/conv/sep13-c55-next-hypothesis.md`：在shared三个完整列作用域之后，各加21个只读w输入、memory clobber的empty volatile asm，尝试减少跨列输入/系数同时活跃造成的寄存器压力。父AC实际三列循环189条指令、5次Z栈读/5次写，完整helper30读45写（其中5次零初始化），frame880+5VL。这些只是父版实际背景，不证明压力唯一原因或新候选改进。

三次插入严格对应父行1530/1571/1612之后，当前行1531–1540、1582–1591、1633–1642。无输出、无寄存器clobber，空模板不包含硬件指令。所有原算术、kw-ik>=3、ik+=3、直接u1余数0..2、另外12阶段、dispatch/tail/kh<7fallback及其他三个生产文件保持原字节。删除且仅删除三段插入即可恢复父源全部字节。

本实验与C55仅调整展开因子不同；本次因子仍3，改动只有编译器依赖边界。它也不同于C49四行packed寄存器的+w输出约束：本版七行三VL的21个累加器均为只读输入，没有packed输出；C50删除memory clobber的方向也不同。没有添加预取、复制输入、改变形状或另一层余数。

只读输入要求每个边界处21个已计算值可供asm使用；memory clobber约束编译器对内存访问的安排。无输出依赖，不能声称所有寄存器运算完全隔离，volatile也不是硬件fence。编译器仍可能插入重装、移动或改变全函数分配。

风险包括GCC10.3.1拒绝这些约束、每列边界导致更多spill、降低有效预载/复用、增加标量栈和循环等待。21输入且0输出的源码形式不等于编译成功。即使热循环spill减少，也可能因调度限制而更慢。

后续仅在root另行审查授权后，才做本候选自己的编译、六配置44328数值、全部退出证据和实际完整汇编审查。重点列出每列21个FADD完成与后列LD1W/LD1RW的相对位置、63/63主循环工作关系、热循环/转场/整个helper栈；不只看空asm标记。空模板可以没有可单独识别的标记，不能拟造指令或PC。父AC PASS不继承，本准备不授权性能、包或晋级。

当前只有源码准备和checkpoint，编译、数值、汇编、性能均未验证，没有新诊断目录或job。C6最佳及历史Sfalse/Yfalse保持，不重试失败确认。40%用量停止，禁重置卡。
