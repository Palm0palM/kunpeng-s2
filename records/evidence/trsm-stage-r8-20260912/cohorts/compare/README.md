# TRSM r8：大路径 8×16 SVE 更新

T8-control12 的独立新 run 与 T11-sve8x16 在同一分配比较。T11 只改变大路径完整 16 列块的更新核形状，保留每输出的运算顺序、原 KB256/CT64、共享打包、38 线程和尾部。新微核与大路径宽列、尾行及共享分配失败另做预检，不能用原 4097×9 检查代替。

在查看本轮任何性能数据前固定协议：每成员一套原官方 run.sh 完整 warm-up，随后 AB/BA/AB 三套正式计时，每套 TEST_RUNS=3。所有 warm-up 原始结果与链接证据单独保存；正式三套全部保留，不删除慢样本。

只在 38 CPU、单 NUMA、24GiB、1800 秒调度计算节点编译、生成汇编、测试及计时。实际 KML25.1/GCC12，不是指定 KML25.2 复验。不计算哈希，不改公共工具，不本机测试，不用重置卡，不关机，不取消别人作业，不正式提交比赛。

先冻结 payload 再提交，保存唯一 job ID 恢复查询。预检和完整 warm-up 通过后才开始三轮；收集后登记 T8 同版本新 run（保存 prior-record），再登记 T11 比较。只有超过波动门槛且没有单例超过 1% 退化才考虑晋级，最终 ZIP 需晋级后单独计算节点验证。
