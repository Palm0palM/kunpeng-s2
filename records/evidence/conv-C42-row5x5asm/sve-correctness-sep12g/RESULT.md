# C42 目标编译失败

唯一诊断作业 1579627 已终止：调度 FAILED，job exit 1、system exit 10001、wrapper exit 1。GCC 10.3.1 在第一条生产 guard 构建命令中，对五段内联汇编均报告 `invalid operand prefix '%Z'`；位置为 conv2d.c 的 1454、1475、1496、1517、1538 行。

计划为六配置共 27,408 项，实际数值测试执行 **0 项**。未生成生产汇编，不能判断实际寄存器约束、spill 或 FMA，也没有性能结论。调度资源和自动传输源清单正常，失败日志完整保留。

未修改候选源码、未重提作业、未在本机编译测试、未使用重置卡。C40 与 C41 已独立通过并冻结；是否调整后续性能组由主代理决定，原三候选全部通过的 gate 应保持失败关闭。
