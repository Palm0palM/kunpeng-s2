# T7-diagpanel：对角块直接在共享打包面板求解

来源 T5-sve16rows；同环境父测量 T5-control10 待建立。仅改变大工作集 solve_blocked 的对角块求解/打包：每列面板独占共享 packed 区，先复制当前块 B，再复用既有 4×8 NEON panel_sums 前代，写回 B，直接留下已解 X。合并原先对角求解和单独打包两次 omp for，依靠一次隐式 barrier 发布数据。

保留 KB=256、CT=64、64 MiB 算法选择预算、SVE16更新、所有运行时分派和分配失败时原来的逐行求解/跨行更新。小工作集路径未改。NEON FMA 可能改变对角块的舍入，必须通过官方 1e-12 精度；输入/输出面板最多 KB×8，列尾补零，行尾走既有标量逻辑。需要覆盖 4095/4096 算法边界、256块尾1/3/4/7、列尾1/7/8/9/65、1/4/38线程及强制分配失败、禁用SVE回退。

尚未执行性能或正确性测试；本机不编译、不测试，不计算或验证哈希。只在超算统一验证后决定晋级。
