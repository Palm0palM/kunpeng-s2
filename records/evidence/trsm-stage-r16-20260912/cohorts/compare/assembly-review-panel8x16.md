# T19 实际目标汇编审查

来源为作业1579861、GCC12.3.1对未插桩T19源码生成的trsm-panel8x16.s。新solve8x16_panel_sve函数始于2666行；历史循环.L168为2707–2744行。

循环包含16条SVE fmla、8个L标量载入及各自广播、2个RHS向量载入，共37条指令；循环内无栈载入、栈存储或结果写回。函数入口保存d8–d11属于调用约定，不是历史循环溢出。历史完成后逐行减和、除对角、写回两份面板；没有把不同输出累加链混合。没有从指令数推算实际周期或声称总输入带宽减半。

l_trsm的安全历史大小比较采用cmp x2,4194304 / bhi .L701，超预算分支进入solve_panel_wide8x16的OpenMP worker；预算内posix_memalign失败仍进入原solve_panel worker。前面的tst w0,4194304是HWCAP SVE位，不能当成预算检查。原源码的整数保护允许编译器在已知m范围内简化计算；静态源审查与实际边界/分配/入口预检一起支撑分派结论。

只读取计算节点取回的汇编文本；没有本机编译、汇编生成、题目执行或哈希操作。
