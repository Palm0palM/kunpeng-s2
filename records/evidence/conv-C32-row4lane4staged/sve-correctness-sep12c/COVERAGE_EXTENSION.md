# 四系数路径的余数覆盖

本候选原样复用已通过的 C29 独立 guard（不是18,052项共享模板）。扩展 kernel 形状保留 (4,5)/(4,6)，以 kh=4 确保进入 quad 主体，再分别经过四系数主循环后的余1/余2；kw7覆盖余3、kw4/8覆盖整除。未删除原用例。

每配置 full=3,268+24宽度×16 kernel形状×11高度×4保护页/对齐组合=20,164，smoke=96。1/4线程×SVE128/256/512位，共121,560项。只复制检查器及候选快照，没有更改算法、参考实现、容差或任何已测证据。

本包为 prepared，等待根代理确认当前性能组结束后另行 GO；尚未提交任务或运行程序。初次打包自动生成传输 manifest，不做额外重复人工哈希审计。
