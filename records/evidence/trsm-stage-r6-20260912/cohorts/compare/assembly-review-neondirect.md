# T9-neondirect 超算汇编审查

审查对象是作业 `1579365` 取回的 [T9-neondirect.s](assembly-review-inputs/T9-neondirect.s)，对应 [候选源码](../T9-neondirect/source/trsm.c)。完整四行块确实消除了 `sums` 数组的栈上物化；历史项仍使用逐列 FMA，块内项仍是独立乘法、加法，随后做减法和除法。热历史循环没有发现新增溢出到栈或函数调用。代价是 OpenMP worker 的栈帧及被调用者保存寄存器增加。以上属于代码生成结论，不代表已证明性能提升。

## 实际指令与源码对应

源码第 93 行的 `panel_solve4x8_neon` 没有独立汇编函数标签，也没有调用指令；它已内联到汇编第 2945 行的 `solve_panel._omp_fn.0`。

| 检查项 | 实际汇编证据 | 结论 |
| --- | --- | --- |
| 完整四行与尾块分流 | `.L394`，第 3118–3119 行先判断并跳到 `.L473`；第 3120–3127 行的八次 `stp q17,q17` 只在不足四行的尾块执行 | 完整四行跳过 `sums` 清零；尾块仍保留原数组路径，不能声称整个 worker 都消除了 `sums` |
| 历史项 FMA 链 | `.L414`，第 3488–3514 行；每次迭代有 16 条 `fmla`，累加到 `v8`、`v16`、`v18` 至 `v31` | 保持 16 条累加链；packed X 指针每次前进 64 字节，L 列指针前进 8 字节，对应源码第 111–140 行的递增 `k` |
| 历史循环栈访问和调用 | `.L414` 内没有 store、`[sp]` 访问或 `bl` | 未发现该热循环的新累加器溢出、临时栈写回或内核调用 |
| 块内乘加分离 | `.L413` 第 3515–3639 行没有 `fmla`。例如 row 1 的 `q=0` 在第 3545/3549 行分别执行 `fmul`/`fadd`；row 2 的后一个块内项见第 3582/3585 行；row 3 最后一个块内项见第 3617–3626 行 | 与源码第 153–211 行的独立乘法、加法一致；编译器交错安排不同输出的指令，但没有把这些项收缩成 FMA |
| 减法和除法 | row 0 见第 3524–3544 行，row 1 见第 3559–3573 行，row 2 见第 3596–3609 行，row 3 见第 3627–3635 行 | 使用实际 `fsub`、`fdiv`，没有在该段替换成倒数近似 |
| `sums` 物化 | 完整块 `.L413` 的向量写入目标是 packed X；没有向 SP 或尾块 sums 缓冲区写入累加器 | 完整四行在寄存器内把历史和块内项连接到求解；packed X 输出写回仍然存在，是算法要求的结果存储 |

完整块不是完全无栈访问：第 3516 行有一次 `ldr x1,[sp,168]` 整数地址/偏移重载，块间推进 `.L415` 也有整数栈槽读取。这些没有落入 `.L414` 历史循环，不应与向量累加器溢出混为一谈。第 3639 行直接跳回块间推进，没有调用独立四行内核。

## 与已有 T7 汇编的有限对照

对照仅使用已有 [r5 T7-control11 汇编](../optimization-20260912-r5/diagnostics/preflight-results/T7-control11/trsm-panel16.s)，来自较早作业，用于确认代码生成机制，不用于本轮性能比较。

- T7 的 `.L57`（第 652–678 行）已有同样的 16 条历史 FMA 链，因此本候选的主要变化不在历史循环的乘加数量。
- T7 第 615 行将 `sums` 指向 `sp+288`，第 622–629 行在完整块分支之前清零 256 字节。历史项结束后的第 680–687 行把 16 个向量累加器写到 `sp+288` 至 `sp+512`；第 692–693 行立即开始重载。T9 完整四行块没有对应的数组往返，尾块仍有。
- T7 worker 第 460 行分配 544 字节栈帧，T9 第 2948 行分配 592 字节，增加 48 字节。T7 第 546 行只保存 `d8`；T9 第 3039、3042、3045 行保存 `d8` 至 `d12`，并在第 3417、3420、3423 行恢复。这是 worker 入口/出口的 ABI 保存成本，不是逐 `k` 的热循环溢出；不能把全部栈帧增量都归因于这几条 SIMD 保存。
- T9 worker 仍有分配、OpenMP runtime、packing/writeback 的 `memcpy`/`memset` 及释放操作。例如第 2980 行 `posix_memalign`、第 3079 行 `memcpy`、第 3088 行 `memset`、第 3426 行 `GOMP_barrier`。完整块历史与求解区间本身没有新增函数调用。

## 审查边界

本次仅阅读真实目标汇编与源码，没有本机编译、执行题目或测试，也没有计算或验证哈希。实际数值对照的完成状态由作业日志审计负责；本报告不以指令外观代替逐元素数值证据。性能结论必须等待本轮同一分配下的 T8 repeat 与 T9 三轮官方用例比较，不能从消除数组写回直接推导提速。
