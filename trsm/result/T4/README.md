# TRSM T4 提交包

直接提交 [trsm.zip](trsm.zip)。该包来自已晋级的 **T4-sve8rows**，与仓库 `outputs/trsm-best.zip` 完全相同，未重新压缩、修改源码或调整测试参数。包内顶层为 `trsm/`，包含算子、官方 benchmark、运行脚本、兼容头和 README 五个文件。

最终包已在超算分配的计算节点解压并复跑：作业 **1507701**，三轮完整套件 **9/9 PASS**，最大误差 **1.11e-15**，调度状态 SUCCEEDED，作业、系统和包装脚本退出码均为 0。

| M×N | 三轮耗时 ms | 中位数 ms |
| --- | --- | ---: |
| 512×19968 | 22.39 / 22.59 / 22.77 | 22.59 |
| 2432×17024 | 253.97 / 259.59 / 253.08 | 253.97 |
| 17024×512 | 244.39 / 243.14 / 243.71 | 243.71 |

合计 **520.27 ms**，是三个用例中位数之和；每次打印值为 TEST_RUNS=3 的平均值。它用于交付包复验，不是官方分数，也不替代同分配优化对照。优化对照与具体环境见[包级报告](../../../docs/trsm-package-20260911.md)和[八行 SVE 优化记录](../../../docs/trsm-sve8rows-20260911.md)。

实测参考库是 **OpenBLAS 0.3.28 静态库**，官方 KML 25.2.0 仍待复验。本包保留原来的运行脚本和使用说明；正式平台提交由队友完成。

`SHA256SUMS` 和 `metadata.json` 中的包/成员哈希是本次本地轻量字节检查生成的分发身份值。历史远端流程按当时要求没有执行哈希验证，这一状态原样保留；不能把本次本地检查写成历史远端已验证。本次没有在本机编译或执行测试，也没有新增超算成绩。

提交后请反馈“TRSM T4 + 平台分数 + 提交时间 + 记录编号”，再将真实结果补入 `metadata.json` 的 competition 字段。当前分数和排名均为空。
