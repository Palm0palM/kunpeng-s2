# CONV AA：C54 shared 四列展开

当前状态：独立超算数值检查、完整真实汇编审查、首次验收与冻结均已完成。AB性能作业1582814已完成48/48 PASS，C54总中位706.99ms，对两端C6均慢约56%，不晋级。正式最佳仍为 C6（C26-r1）；没有生成新提交包。

C54-row7x3shared4 从原 C52-row7x3shared2 出发，仅把 shared 的两列主循环改为四列：`kw - ik >= 4`、`ik += 4`，依次完整处理 ik/ik+1/ik+2/ik+3，随后进入原 u1 余数循环（0..3列）。七行×3VL、21个累加器、其他十二阶段、分派/尾部/防御与其余三个提交文件保持。假设是分摊循环与地址控制开销；源码展开本身不证明提速。

原 C52 的 Y1582410 确认失败，原 C51 的 S1582067 确认失败及全部慢样本保留。本轮是不同源的新假设，不重新解释或重复失败确认。父 T1582134 的诊断仅用于来源/背景，本轮使用自己的独立源和作业。

Root 唯一提交 AA 作业 **1582656**。submit、status、fetch 各一次退出0；真实调度器 SUCCEEDED，job/system/wrapper 均0，十九阶段精确有序全0。实际 GCC10.3.1，38 CPU 同一个 NUMA，严格浮点与三条诊断编译 argv 均符合既定条件；全部计算在调度分配节点完成，本机仅编辑与处理原始文本。

| 实际路径 | 每配置 | 六配置 |
|---|---:|---:|
| 未插桩生产 full | 5744 | 34464 |
| 插桩 dispatch | 1212 | 7272 |
| direct kh<7 防御 | 432 | 2592 |
| 合计 | 7388 | **44328** |

六配置为 VL字节16/32/64×线程1/4，实际VL/线程头全部相符。full五家族为3888/144/720/32/960，dispatch两家族972/240；每配置的实际rowseven入口分别1236/1080，worker mask为1/15，五个旧helper入口均非零。全部逐位scalar比较通过，输入/权重只读、分配边界guard、页内canary、output poison保持。没有官方runner、sanitizer、逐行guard或非法/强制非SVE完整覆盖。

相较原T矩阵，新增六宽3L/6L邻界×kh7/8×kw4..8×oh7/8/14/28；full四分配增加960，dispatch增加240。kw4/5/6/7验证四列主循环后0/1/2/3余项，kw8验证重复主循环；原kw1/2/3和larger kw15/81保留。入口计数是helper调用数，不冒充内部主循环计数。

完整实际未插桩汇编已由 helper 与 dispatch 两部分独立审阅并合并：rowseven 全函数5705–7770，共1908条静态指令，栈帧816+30×VL字节。完整helper有92次Z向量栈读取、150次写入（其中14次为零初始化）；ABI D8..D15保存另列。四列主循环6450–6779有84 FMUL/84 FADD、12次输入载入、28次权重广播，以及35次Z栈读取/30次写入。全部21条累加链保持ik→ik+1→ik+2→ik+3顺序，余数循环按0..3次执行；十三个语义阶段对应十四个实际算术区域。全部源FMA为0，完整转场、边界、重置、输出、尾部与防御已审。

父C52的两列主循环没有Z栈溢出，处理四列的静态指令归一化为246条，C54为329条。这个差异提示寄存器压力和栈访问风险，不直接证明运行速度；AB仍按原计划做同环境实测，不能仅因spill判正确性失败。

Root 首次调用接受器退出0：status=passed、complete=true、44328项。随后首次冻结器退出0，完整快照位于 `.runs/conv/C54-row7x3shared4/sve-correctness-sep13aa/`。首次prepared和传输清单、原job、提交/状态/取回/验收外层stdout/stderr/exit、scheduler、全部raw源/日志/.s及审查记录保留。冻结外层日志单独保存，冻结树不回写。

AB1582814顺序为C26-r30/C54/C52-r2参考/C26-r31，每版三套完整官方测试，共48样本；两端C6门槛及逐case退化限制保持，全部慢样本保留。C52参考不能晋级，既有Yfalse/Sfalse不变。AB实测结果未通过，见[全部48样本](CONV_SEP13AB.md)。当前C6仍为唯一CONV提交包。
