# CONV AE：三列末约束已通过专项诊断

C56-row7shared3fence 从 C55-row7x3shared3 出发，仅在三列的21个累加器全部更新后各增加一个空 asm，包含21个只读向量输入和 memory clobber。其余算术、主循环三列及余数0..2、另外十二阶段、分派和三个配套文件未变。假设是限制跨列调度、减少外围寄存器栈流量，需实测速度。

原作业1583102仅提交一次。首次status已SUCCEEDED，job/system/wrapper均0，19个阶段全部0；首次fetch成功。六配置VL16/32/64×线程1/4，每组5744 full、1212 dispatch、432 direct，共44328项通过。原checker、逐位参考、只读输入/权重、分配guard和canary保持；继承网格名quad_boundary并不表示本版主循环四列。实际GCC10.3.1、strict/generic三条编译命令、38CPU/24576MiB/单NUMA与计划一致。只在分配计算节点运行。

唯一rowseven helper的实际范围5705–7259，1555行/1391条指令；十三语义阶段对应十四个实际算术区域。共享三列主循环190条指令、63FMUL/63FADD、9输入载入/21广播，余数u1每列63条指令。原父C55主循环189条，静态指令数量不直接代表速度。

| 栈访问观察 | C55/AC | C56/AE |
|---|---:|---:|
| 主循环向量栈读/写 | 5/5 | 5/5 |
| 全helper向量栈读/写 | 30/45 | 5/5 |
| 其中零初始化写栈 | 5 | 0 |
| 栈帧 | 880+5VL | 848+5VL |

C56的五个向量槽只保存第二列的b2/c2/d2/f2/g2，第三列重新载入后返回活跃寄存器。外围阶段、余数入口/出口和输出不再有这些向量栈访问；主循环的五读五写仍存在。没有Q/谓词栈访问，D8..D15低64位ABI保存另列，不能宣称零spill。

全部21条累加链、三列末及跨主循环边界已按返回汇编审查：本列最后FADD分别为6406/6463/6531，下一列或下一迭代的首次载入位于其后。三处空asm没有独立可辨的指令或PC；这里的调度分隔是编译结果，不是硬件栅栏或CPU完成顺序保证。全源FMA0。全部stage、转换、边界/尾部/防御路径及完整栈均审完。

四个完整dispatch函数文本与已审AC逐字节相同，root据完整等同性复用既有控制流和栈结论，不声称重新手动读完当前四函数或整个机器代码相同。helper片段与root分派片段保留后组合，首次accept退出0，status=passed/complete=true/44328；首次freeze退出0，原件冻结到C56的sve-correctness-sep13ae。原job、source/prepared/manifest和全部数值/汇编/命令输出继续保留。

本专项没有官方性能或平台分数，未产生C7或C56提交包。AF同分配原样C6/C56/C55参考/原样C6各三套已完成48/48 PASS；C56合计467.46ms，比结束C6的452.33ms慢3.344903%，未通过初筛。见[全部性能样本](CONV_SEP13AF.md)。当前最佳提交包仍CONV C6、ZGEMM Z1、TRSM T19，原C55AD退化、C52Y及C51S确认失败不改写。
