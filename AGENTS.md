# Agent 工作规则

先读 `README.md`、`docs/BASELINES.md` 和 `docs/PLAYBOOK.md`，再开始工作。目标是独立完成候选实现、超算验证和实验记录，不必每轮索要确认。执行前查看当前分支与工作区变更；这里有其他协作者，不要覆盖或撤销他人的改动。

## 运行位置（用户于 2026-09-09 明确要求）

- 题目编译、正确性测试、sanitizer、benchmark 和性能压力测试全部在超算调度分配的计算节点执行。
- 本机仅做轻量文件编辑、连接、传输和日志整理，不运行题目编译或测试；超算不可用时保留待执行实验。

## 允许的工作范围

- 可以读写本地候选、建立 Git 分支，使用配置好的 SSH 连接与调度器提交测试、查询状态、取回结果。
- 每个实验使用唯一版本和独立目录，源码改动限制在 `.runs/<problem>/<version>/source/`。三题主目录通过晋级流程更新。
- 正式比赛平台提交由用户负责。GitHub 推送、合并、通知其他人应依据用户当前授权，不能把一次本地优化自动扩大为对外发布。
- 不把账户密码、私钥或令牌写进代码、记录、命令日志、提交信息；认证使用本机 SSH 配置或 agent。缺少网络或凭据时，保留已完成的候选与可执行命令，明确哪一步未验证。
- 优先复用本机已有的 SSH 认证会话。会话失效时按用户在当前任务中已授权的方式恢复；`python3 tools/connect.py` 是可选登录辅助工具，不要求用户必须操作终端。不要把密码写入代码、日志或仓库，也不要为已授权的常规测试反复要求批准。

## 测量与正确性

- 不修改官方 benchmark、参考实现、输入尺寸、校验逻辑、精度容差或计时区来改善结果；不超出题目线程与资源限制。
- 修改 `run.sh` 后必须复核实际编译 flags、线程与绑定设置、验证逻辑，确认评测条件仍合规且可比较。优化参数应固化到候选 `run.sh`，或完整保存环境设置；最终提交包必须用这些设置再次验证，不能只保留临时环境下的好成绩。
- 不伪造成绩、替换真实日志或把编译成功当作正确性通过。官方程序可能在 FAIL 时返回 0，必须检查所有预期用例的 PASS/FAIL。
- 调度器成功、进程退出码和逐用例正确性都要检查。断线、未完成、超时、日志缺失和校验失败均不能判为成功。
- 基线与候选必须在可比环境测试：相同硬件、编译器与选项、线程数、绑定方式、输入、重复次数和参考库。环境不同则分别记录，不直接宣称提速。
- 重要候选重复测试，优先用各用例中位数与波动判断。benchmark 只打印多次运行平均值时，标为平均值；需要跨独立运行收集样本才能报告中位数。
- 历史 C0/Z0/T0 不能直接晋级为当前测量基线。先无 parent 创建同名基线，保持代码不变，经 wrapper 三轮完整测试、`record` 和 `promote` 后再创建子候选。三轮指独立完整套件，不等于 `TEST_RUNS`。
- 总耗时与 GFLOPS 是内部指标。确认官方评分规则前，不将它们称为官方分数、排名或获奖概率。

## 每次实验必须留痕

使用 `tools/experiment.py` 创建、登记、比较实验。记录版本、父版本、单一可检验假设、改动策略、源码哈希、完整命令、环境、重复次数、原始日志、每用例结果和结论。

失败、退化、无变化、超时、回退也要保存：写清尝试了什么、看到了什么、下一步为何改变。若工具不能解析失败日志，保留原始日志与失败原因，不编造成通过记录。

提交超算任务后先保存并复用 job ID。轮询可恢复；避免重复提交、并发覆盖日志或同时更新当前最佳版。性能结果不明确时继续验证，不凭理论判断晋级。

## 提交包与比赛反馈

- 每题仅将当前最佳版本放进 `题目/result/版本/题目.zip`，例如 `conv/result/C4/conv.zip`。文件必须与该版本已验证的测量快照逐字节一致，保留题目顶层目录和 `run.sh` 可执行权限。
- 每个版本目录保存 `README.md`、`metadata.json` 和 ZIP 校验值，记录正式版本与实验 ID 的映射、优化策略、完整逐次耗时、正确性、源码哈希和比赛反馈字段；维护根目录 `SUBMISSIONS.md`。
- 新最佳版使用新编号，不将不同源码覆盖成同一版本。更新当前提交目录时，旧源码与实验记录继续保留，历史提交包由 Git 历史追溯；未晋级、未验证和退化候选不进入当前提交清单。
- `result/` 是交付归档，不进入候选源码扫描、超算 payload 或晋级时的源码清理。打包只读取该实验明确记录的源码文件，不递归压缩整个题目目录。
- 比赛由队友手动上传；获得实际分数后更新对应版本的反馈字段和 `SUBMISSIONS.md`，未反馈时保持空值或“待反馈”。

## 两人协作

同一轮从共同基线开始，每人一个分支、一种不同优化思路。候选名可用 `C1-alice`、`C1-bob`，ZGEMM 用 `Z`，TRSM 用 `T`。协调一人执行晋级并更新共同基线，随后双方同步后再进行下一轮。

只有在正确性与同环境测量支持提升后才执行 `promote`。保留来源候选名和父版本；如果使用 `C1` 等正式编号，在实验记录中明确其对应候选。没有提升就继续使用旧基线。

收尾时简洁报告：改了什么、实际测试到哪一步、速度证据、当前基线、保留了哪些失败尝试、是否有待用户处理的连接或平台提交事项。
