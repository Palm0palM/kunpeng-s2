# TRSM SVE 公开副本说明

本次发布 T3-sveupdate-r3 已晋级源码、两种 SVE 候选的完整结果和准备阶段的失败尝试。27 条官方结果全部 PASS；同一分配的三轮合计中位数由756.94降至535.06 ms。完整性能与波动说明见 [TRSM SVE记录](docs/trsm-sve-20260909.md)。

计算使用 OpenBLAS0.3.28 静态参考库，尚未完成官方 KML复验。基线中等用例存在15.31%范围波动；候选总改善29.31%仍超过原统计门槛。小路径SVE版本退化，未晋级。

记录中的个人路径、内网地址、计算账号、内部节点名及文件所有者标识替换为一致占位符；数值、用例、精度、线程/NUMA、编译配置及作业ID保持。成功作业采用无哈希验证策略，既有历史哈希字段只保留原始含义；本次导出也没有计算或验证哈希。文件列表见 [发布清单](records/trsm-sve-publication.json)，不包含校验和。

**公开日志已脱敏，与原始日志不再逐字节相同。原始证据和可运行实验目录保存在本地。** 不应使用这些公开副本声称远端源码或传输完整性已通过验证。保留的verified字段表示来源记录在其明示策略下的原始判定，不代表公开脱敏文件重新通过验证。原始命令中的路径占位符需要在授权环境替换后使用；归档脚本是当时执行流程的证据，不能从归档位置直接运行。

后续编译与测试只在超算执行，不在本机执行。此次发布没有重新编译或运行测试，没有正式提交比赛。该批次未修改CONV、ZGEMM或公共工具；共享索引仅更新TRSM条目。
